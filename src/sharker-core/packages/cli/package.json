{
  "name": "sharker-agent",
  "version": "0.2.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "exports": {},
  "bin": {
    "sharker": "./dist/cli.js"
  },
  "sharker": {
    "managedRuntimeHostUpdateCompatibility": 1
  },
  "scripts": {
    "clean": "node ../../scripts/clean-paths.mjs dist tsconfig.tsbuildinfo",
    "build": "tsc -p tsconfig.json && node scripts/chmod-bin.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm --workspace @sharker/core run build && npm --workspace @sharker/storage run build && npm --workspace @sharker/mcp run build && npm --workspace @sharker/runtime run build && npm --workspace @sharker/runtime-host run build && npm --workspace @sharker/eval run build",
    "test": "npm run clean && npm run build && npm run test:dist",
    "test:dist": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.4.0",
    "@earendil-works/pi-tui": "0.84.2",
    "@sharker/core": "0.1.0",
    "@sharker/eval": "0.1.0",
    "@sharker/mcp": "0.1.0",
    "@sharker/runtime": "0.1.0",
    "@sharker/runtime-host": "0.1.0",
    "@sharker/storage": "0.1.0"
  }
}
