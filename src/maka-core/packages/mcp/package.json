{
  "name": "@maka/mcp",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Provider-neutral MCP client manager for Maka.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./dist/index.js",
    "./test-only/stdio-server": "./dist/__fixtures__/stdio-server.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "node ../../scripts/clean-paths.mjs dist tsconfig.tsbuildinfo",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:dist": "node --test \"dist/**/*.test.js\"",
    "test": "npm run clean && npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@maka/core": "0.1.0",
    "@modelcontextprotocol/client": "2.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/node": "2.0.0",
    "@modelcontextprotocol/sdk": "1.30.0",
    "@modelcontextprotocol/server": "2.0.0",
    "@modelcontextprotocol/server-legacy": "2.0.0"
  }
}
