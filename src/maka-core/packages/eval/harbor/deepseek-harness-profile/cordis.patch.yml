# Portions derived from DeepSeek Harness:
#   Copyright (c) 2026 DeepSeek
#   Licensed under the MIT License.
#
# Apache Maka changed the plugin composition, provider configuration,
# reasoning strength, and command lifetime. Those modifications are licensed
# under the Apache License, Version 2.0. See LICENSE, THIRD-PARTY COMPONENTS,
# for both license texts and the pinned upstream revision.

# The DeepSeek Harness arm's complete plugin composition.
#
# `package.json` declares no bundles, so the harness composes this tree over an
# empty entry list: every entry the model can observe is named here, and an
# upstream bundle gaining a plugin cannot change this arm's tool surface. A
# missing service is a hard boot failure, not a silent downgrade.
#
# Derived from the harness's own examples/jsonrpc-agent/minimal.cordis.yml —
# the composition BENCHMARK.md names for benchmark runs — with the SDK JSON-RPC
# entry replaced by the CLI's one-shot startup pair, and `agent-spine-demo`
# replaced by the equivalent core services the CLI uses. Verified against that
# composition request-for-request: identical tool names, byte-identical tool
# schemas, byte-identical system prompt, identical message sequence.
#
# Source: https://github.com/deepseek-ai/deepseek-harness
#   commit 47f943859bef60e4160492346772ded9b24f765a, MIT license.
#   Reproduced text: the persistent-bash description and the persona below.
#
# The deliberate deviations are reasoning strength and command lifetime. The
# upstream composition configures neither `thinking` nor `reasoningEffort`, and
# the adapter default resolves to reasoning_effort=high on the wire. Its
# five-minute Bash timeout can also interrupt package installation before the
# benchmark deadline and leave the task environment unusable for verification.

- insert:
    # --- core services ---
    - id: llm
      name: '@deepseek-ai/dsh-llm'

    - id: session
      name: '@deepseek-ai/dsh-session'

    - id: agent
      name: '@deepseek-ai/dsh-agent'

    # The model this arm reports. The upstream composition selects it through a
    # `!!js process.env.DSH_MODEL` expression; naming it directly keeps the
    # frozen spec the only place a model is declared.
    - id: agent-default-model
      name: '@deepseek-ai/dsh-agent-default-model'
      config:
        provider: deepseek-official
        model: deepseek-v4-flash

    - id: tools
      name: '@deepseek-ai/dsh-tools'

    # `includeRuntimeContext: false` matches the upstream composition, which
    # sets it on agent-spine-demo. Without it the sandbox contributes an extra
    # user-role snapshot that upstream never sends.
    - id: system-prompt
      name: '@deepseek-ai/dsh-system-prompt'
      config:
        includeHarnessIdentity: false
        includeRuntimeContext: false
        persona: You are a helpful software engineer assistant.

    - id: agent-loop
      name: '@deepseek-ai/dsh-agent-loop'
      config:
        agents: []

    # --- provider ---
    - id: llm-deepseek
      name: '@deepseek-ai/dsh-llm-deepseek'
      config:
        apiKeyEnv: DEEPSEEK_API_KEY
        thinking: enabled
        reasoningEffort: max
        models:
          - id: deepseek-v4-flash
            contextWindow: 1000000

    # --- execution substrate ---
    - id: sandbox
      name: '@deepseek-ai/dsh-sandbox-local'

    - id: sandbox-policy
      name: '@deepseek-ai/dsh-sandbox-policy'
      config:
        mode: danger-full-access

    - id: subprocess
      name: '@deepseek-ai/dsh-subprocess-local'

    - id: pty
      name: '@deepseek-ai/dsh-terminal'

    - id: terminal-bash
      name: '@deepseek-ai/dsh-terminal-bash'
      config:
        timeoutMs: 3900000

    - id: fs-local
      name: '@deepseek-ai/dsh-fs-local'

    # --- the two tools the model sees ---
    - id: persistent-bash
      name: '@deepseek-ai/dsh-tool-bash-persistent'
      config:
        timeoutMs: 3900000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

    - id: str-replace-editor
      name: '@deepseek-ai/dsh-tool-str-replace-editor'
      config:
        maxOutputChars: 16000

    - id: sessions
      name: '@deepseek-ai/dsh-session-persistence-jsonl'
      config:
        root: ./.sessions
        compression: none

    # --- CLI one-shot entry ---
    - id: headless-startup
      name: '@deepseek-ai/dsh-headless/startup'

    - id: headless-runner
      name: '@deepseek-ai/dsh-headless'
      inject: [headlessStartup]
      config:
        task: !!js ctx.headlessStartup.task
