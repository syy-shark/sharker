# src/components — UI 组件

## 职责

- 聊天主界面、执行轨道、侧栏、标题栏、右侧面板与设置子面板等可复用组件
- 成对的 `.tsx` + `.css` 为组件本体与样式；材质遵循 [docs/ui-style.md](../../docs/ui-style.md)

## 同级目录

| 目录 | 说明 |
|------|------|
| [panel/](./panel/ARCH.md) | 右侧面板：文件树、审查、终端、浏览器、子 Agent 活动 |
| [settings/](./settings/ARCH.md) | 设置页各 Tab 与通用设置原语 |

## 同级文件

| 文件 | 说明 |
|------|------|
| `Sidebar.tsx` / `.css` | `memo`：直播 token 不重绘侧栏（回调与进行中 id 在 App 固定引用，对标 Codex sidebar jitter）；导航/项目/对话（`data-conversation-id/title` 便于自动化恢复）；主导航铃铛开关 Activity（⌘⌥U，未读对话徽标）；Skills 入口（对标 Codex open Skills in the sidebar）；审查队列入口与未读徽标；**进行中**任务行（并行线程置顶）；**置顶**分组；对话旁 Codex 式筛选（按时间 / 进行中 / 等待回复 / 未读 / 定时 / 置顶）；筛选菜单在有未读时提供「全部标为已读」（只清对话未读）；⌘⌥U 开关 Activity（默认等待回复）；进行中 / 未读点占同一 `sidebar-row-status` 槽，开停回合不挤标题；双击 / ⌘⌥R 行内改名；隔离 Worktree 线程小标；项目菜单含编辑项目（附加文件夹）、创建永久 worktree、归档对话（一并归档该项目对话，进行中跳过，对标 Codex Archive chats）；enter/exit 统一 180ms 卸载；展开时顶栏收起按钮；收起后左缘热区 peek（pointer/mouse）滑入；设置导航含通用 / 外观 / 通知 / 个性化 / MCP / 建议提示 / 用量 |
| `ChatView.tsx` / `.css` | 聊天主视图：消息列表、滚动；长线程先挂最近一段，上滑到顶先揭示内存窗再向主进程取更早一页进独立 `historyHead`（不 prepend 尾页），并滚到与尾页相接处（对标 Codex older history fetched as needed / scroll-to-top 分页，不预挂全量、无「加载更早」按钮；⌘↑ 只进最旧一页，下翻 `nextHeadRange` 再接回尾页，不把瘦身全文灌进 `messages`；直播中不取头页，收束后再取以免卡住贴底）；⌘F 查找只扫用户/助手正文，盘上命中不回放整段，跳到未加载处再揭开命中起的有界一段；直播命中单独算，token 不重挂 `historicalRows`（对标 Codex #33907）；点开瘦身后的命令输出 / 思考走 `onNeedFullMessage` 取一条全文（对标 Codex #38653）；直播行用本轮预留助手 id（`msg-${id}`）收束后同一 key 留下，历史助手不播 `list-item-in`；直播体已空且历史已挂同一 id 时只留历史行，避免空直播窗（对标 Codex preserved streamed activity）；排队条在输入框上方（不进 `.messages`，预览限高以免挤视口，直播贴底不受排队行影响）；Composer 提交后只有空闲 `'send'` 才贴底并离开 `historyHead`（排队 / 注入保持阅读位置，对标 Codex #13698 / #38220）；`memo` 且不接收 `streaming` / `liveSegments`（16ms flush 走 `useLiveStreamUi`，直播行过程/回答分订切片，只重绘回答 / 查找 / 「新消息」芯片，对标 Codex #22860）；输入区交给 `ChatComposerInputs` + `ComposerDock`（不接收 `streaming` / `liveSegments`，直播 token 不重绘输入框与排队条）；流式贴底用 ResizeObserver 盯内容、滚动视口与 `composer-stage`（输入区与「新消息」芯片都在 flex 流里占位不浮盖正文，排队 / 芯片变高同一帧跟；柱尾 `LIVE_TAIL_SAFE_PX` 与输入区顶距留给操作条，对标 Codex #40788 / #38220 / #41155），布局后、绘制前写 scrollTop；切对话 / 离开聊天页把滚动快照交给 App（窗口内按会话恢复，贴底的仍贴底，读历史的钉 scrollTop 与窗口起点，内容未画高先按距底占位，对标 Codex 26.406 Preserved thread scroll position per conversation）；审批出现时已贴底才跟（读历史不解锁、不 `scrollIntoView`，Enter/Esc 仍走输入框，对标 Codex #38220）；远离底部的旧消息才 `content-visibility`，贴底附近用 `message-row--near-live`；离开窗口的行写入实测 `contain-intrinsic-size`（第一帧也用已测量高度，避免 160px 估高跳，对标 Codex #38220）；收束换消息只有距底 ≤16px 才强制滚到底（对标 Codex #37849）；历史行 `useMemo` + 用户气泡 `memo`；用户气泡可就地编辑并重发；历史气泡悬停「从此条分叉」只拷到该条（含），直播行不分叉（对标 Codex fork from an earlier message / 拒绝 in-progress lastTurnId）；气泡与输入框 `pre-wrap` + `overflow-wrap:anywhere`，CRLF 先归一再画，长 URL 不撑开直播柱（对标 Codex #37709 / #38380 / #38704）；空输入 Esc+Esc 回编上一条并分叉；划选历史正文出「插入输入框 / 旁路提问」（对标 Codex send selection to composer 与 Ask in side chat，不抢直播行/输入框）；`/copy` 有代码块或引用时在输入框上方列出整段 / 代码 / 引用（对标 Codex /copy picker，Esc 关闭，不进对话）；「回到底部」在 `composer-wrap` 顶部占位（读 `historyHead` 时即使页短也显示，点了回尾页，不 absolute 盖直播尾）；读历史时直播又长高则改成「新消息」，不改 scrollTop（对标 Codex #38220 new message / #40788）；「新消息」芯片只在离开底部时订直播 store，贴底时不跟 token；⌘F 查找条在滚动层外占位（不 sticky 盖正文，对标 Codex #40788 / #38220）；`.composer-box` 内仍可触发，有划选则预填（对标 Codex Find starts with current text selection；审查面板聚焦时不抢，改走审查 diff 查找）；⌘G / ⌘⇧G / F3 / ⇧F3 始终 Find next / previous（未开先开再跳，同一条多处各算一次，正在直播的回答也算，CSS Highlight 只扫气泡正文；命中只改对话柱 scrollTop 并锁贴底，不 scrollIntoView，以免直播增高把镜头拽回底部）；直播中点「回到底部」用即时滚动，不跟 ResizeObserver 抢 smooth；⌘↑ / ⌘↓ / Home / End 跳到对话顶/底（输入框 / 查找 / 预览 / 终端 / 浏览器不抢，对标 Codex #39181；End 回到贴底跟直播）；点对话柱后 ↑↓ / Page / Space 原生滚动且上翻锁贴底（对标 Codex 桌面 #39851）；空对话建议芯片（可关）；隔离目录被清理时显示恢复横幅；主线程子 Agent 步骤点开活动；完成后改文件芯片打开审查并切到本轮；直播廉价尾与收束 Markdown 共用块边距 / 任务列表 / 图片样式；渲染图可复制或另存，直播图先占位再成图以免贴底被撑开；直播与项内表格 `table-layout:fixed` 以免长单元格撑开对话柱 |
| `GoalProgressRow.tsx` / `.css` | 输入框上方的线程目标进度行（对标 Codex Goal）：暂停 / 继续 / 编辑 / 清除；`/goal edit` 用 `editTick` 打开编辑；`startedAt` 用独立秒表显示耗时；不接收直播 token |
| `ComposerDock.tsx` | 输入区独立树：`/` `@` `$`（`@` 菜单含 Skill，发送前 `$name` 芯片确认）、模型旁思考档位条（对标 Codex composer gauge）、历史（标题/正文/分支扩匹配）、项目选择器、附件、听写/语音、忙时 Enter 按 `followUpBehavior`（默认排队）/ ⌘⇧Enter 反转（注入失败改排队，不 abort）/ Tab 排队（⌃Tab / ⌘Tab 不补全也不排队；忙时 `/` 与 `!` 先排队、收束后再解析，对标 Codex Tab queue slash）、无菜单时 Shift+Tab 切计划模式（对标 Codex Best practices，不抢 `/` `@` `$` 补全）、`composerEnterBehavior` 三档（对标 Codex Enter 发送）、审批打开时 Enter 允许一次 / Esc 拒绝、计划 / 本地 / 隔离芯片（计划不跟直播 token 变、不自动开一轮）；`/` 列表并入已安装 Skill（选中写入 `$name`，不盖内置命令）；切对话 / 工作区时恢复未发送草稿与附件（对标 Codex restore unsent prompts）；空输入 ↑ 恢复刚提交或上一条（取消运行 / 取消 worktree 创建后即使还没进对话也能恢复）；空输入 Esc+Esc 回编上一条用户气泡（不把草稿填回输入框）；粘贴优先 text/plain（避开 Office 图片层），超长收成 `Pasted text.txt` 可预览/回插；深链 `prompt=` 经 `composerSeed.nonce` 覆盖写入，划选插入走 `mode:'append'` 接在草稿后；`onSubmitted` 带提交 mode；不接收 `streaming` / `liveSegments`；直播中 `speechHint` 置空以免跟 token 重绘；有目标时在输入框上方挂 `GoalProgressRow`；中止直播走 `shouldInterruptTurn`（默认 `Esc`，设置可改绑/解绑；IME 选词不触发） |
| `ComposerQueue.tsx` / `.css` | 输入框上方注入预览 + 排队条（对标 Codex pending steer / queued follow-ups）：注入在上可编辑删除、排队可重排；预览最多 3 行 / 240 字且整条队列限高（对标 Codex #39864 / #40788，长排队不挤直播视口）；直播中芯片主操作是「注入」推进当前回合（对标 Codex queued chip Steer），空闲才「发送」，失败不中止直播；CRLF 归一并折断长 URL；不进对话滚动区 |
| `ChatToolbar.tsx` / `.css` | `memo`：直播 token 不重绘顶栏；侧栏展开/收起、新对话、分叉整段（对标 Codex conversation fork / `/fork`）、分享只读快照（对标 Codex Share）、弹出对话、弹出窗 Always on top、Hand off 交接本地/隔离（对标 Codex header）、打开隔离 worktree、在此创建分支、当前分支 PR 芯片、右侧面板 |
| `LiveAssistantParts.tsx` | 直播行：过程 / 闭合回答 / 增长尾 / 操作条分订切片；token 不重绘 TurnFlow、已闭合正文与复制条（对标 Codex #22860） |
| `AssistantMessage.tsx` / `.css` | 助手消息：`memo` 避免直播拖着历史行重绘；点开瘦身后的思考 /「查看输出」再取完整消息（对标 Codex #38653）；直播思考/工具在上（对标 Codex 时序），收束后过程芯片仍留在回答上方以免整块对调拽走贴底；过程区间距固定，不在正文出现时再加分隔；过程片段引用没变时复用同一数组，避免回答 token / status 心跳重挂 TurnFlow；Cursor 式可折叠 Thought（无灰卡片）；正文/内联演示/写盘 diff 在下（演示一开始占同一槽，未可绘先骨架；写入一开始占同一 diff 槽，参数流填已解析 +/-，完成后换核实行（对标 Codex 约 0.5s 逐文件变更）；直播 `LiveFileDiff` memo，`reuseAnswerParts` 在预览 token 下保住已闭合 diff 对象以免重挂；`live={streaming}` 时不折 20 行、内层跟尾，收束后同一实例保持展开以免跳（历史重挂才预览 20 行）；含脚注收束后仍用廉价树）；完成后「已思考 · Ns」可展开，真实工具另有过程行；写盘一开始就挂「已改 N 个文件」（对标 Codex 回合内 N files edited）；正文槽（散文 / diff / demo）上屏就挂复制操作条，无可复制正文时先占位，收束不再整块冒出躲进输入框（对标 Codex #40788 / #41155）；历史行可从此条分叉，直播占位条不分叉；直播秒表不在本组件计时 |
| `LiveDuration.tsx` | 直播耗时独立组件，500ms tick 只重绘秒表 |
| `TurnFlow.tsx` / `.css` | 直播过程：思考默认折叠成「思考中」（对标 Codex，避免增长正文顶回答）；连接中一行状态字+耗时（`LiveDuration`）；生成演示时改头标签；有工具才展开时间线；正文已上屏或回合结束后把过程收成「工作中 / 工作了」（对标 Codex Worked for，点开才看步骤；审批/失败仍露出；回答刚上屏时收回已展开的 Thought / Worked for）；`memo` 过程行与整棵树，已完成步骤保持同一对象，避免回答 token / status 心跳重挂时间线；命令输出按 `toolOutputDisplay` 截尾/折叠，`<pre>` 只在 `<details>` 打开后挂载；占位输出点开再取全文（对标 Codex #38653）；直播中不挂「查看输出」/ 退出码 / 「执行中… Ns」摘要与过程行/直播头秒表心跳 detail、也不自动展开 verbose，对标 Codex #19260）；秒表预留 `1h 59m` 宽以免跨分钟挤过程区；工具间隙直播头停在最后一条实质步骤，不闪「规划下一步」（对标 Codex flashing thinking summaries）；正文已上屏时仍隐藏命令输出以免过程区顶回答；子 Agent 步骤可点开活动 |
| `ProcessTimeline.tsx` / `.css` | 旧消息回退过程时间线；子 Agent 步骤可点开活动 |
| `InlineApproval.tsx` / `.css` | 过程内高危操作审批块；出现时 view-enter + 呼吸，`focus({ preventScroll: true })` 不 `scrollIntoView`（贴底由 ChatView 跟，读历史不抢镜头）；参数长行换行以免横向撑开直播柱 |
| `ThinkingIndicator.tsx` / `.css` | 兼容旧路径的轻量「思考中」；秒表同样预留 `1h 59m` 宽；直播主路径用 TurnFlow 状态行 |
| `MarkdownBody.tsx` | Markdown 渲染；代码/diff / mermaid 分流；本地文件引用点开右侧预览；http(s) 与工作区相对路径图走 `ChatImage` 可复制/保存；保住 GFM 任务列表 class；元素子节点不套 span，避免收束跳动 |
| `StreamingMarkdown.tsx` | 直播正文：`React.memo` + 整段散文廉价增量（空行收段不再换 remark 树）；含脚注收束后仍用本树，不再换 remark 跳贴底；CRLF 按 LF 拆；未闭合围栏与已闭合围栏都在顶层 `live-fence-N` 槽（闭合不搬进散文尾重挂）；围栏之间的散文共用 `prose-run-N` LiveProseTail，空行收段不换 key；`LiveFenceTail`（开闭至少三连，更长围栏可包住内部 \`\`\`）；散文尾廉价 ATX/Setext 标题（含行尾 `#`；下划线不到三连先保持段落）/列表（含 `1)`、`ol start`、缩进嵌套、续行硬换行与松散 `li>p`、项内表格 / 围栏 / 引用 / ATX / Setext / HR / 嵌套围栏 / 围栏后后缀 / 松散项内缩进代码（嵌套层自己松））/任务项/嵌套引用（`blockquote>p`、引用围栏与懒续行含硬换行；未闭合围栏不吃懒续行）/表格对齐与单列 / 无两侧 `|` 与 `\\|` / 分隔行未到先画表（左侧 `|` 未闭合也先画，未写完 `|` / `|:` 不当数据行；无两侧 `|` 的表头打 `---` 不升成 Setext）/分隔线（含 `* * *`）/缩进代码/脚注区（缩进续行与多段）/http 图（含 title、`![alt][id]` 相对 dest、alt 去标记，悬停复制/保存）/mermaid 开闭都挂 `MermaidBlock`（直播 `mer` 起就挂，未闭合不解析，成图前后共用 `CodeArtifactShell`）/```demo 开闭都挂 `InlineDemo`（未可绘先骨架，不先画 `LiveFenceTail` 再换）/dest 内成对括号 / 未闭合 `](` 先画链接/未闭合 `**` / `*` / `~~` / `` ` `` / `***` / `<https://` 先画/`[![img]](url)`/多反引号代码/链接标签内强调与代码/HTML 实体/删除线套粗体（两侧无空白才画）/标记内混排与链接/下划线强调/`***` 嵌套强调/反斜杠转义/引用式链接含定义 title 与次行标题/邮箱/`www.`（不当文件芯片）/危险协议清空/硬换行与可点 http / 文件引用；全文引用定义挂到已闭合块；任务项用 GFM `contains-task-list` / `task-list-item`，未写完的 `[x` / `[ ]` 先占 checkbox（`[x](url)` 不当任务）；表体始终挂 `tbody` 以免首行才挂载；列表项从第一项就 `li>p`（对标松散 `li>p`），空行变松时只加后续段、不把已画项从裸文本换成 p；未写完的 `-` / `1.` 不并进上一项；`continueCheapProseBlocks` 中间块类型变了也保住后面已闭合项；行内 key 用类型+前缀长度，闭合标记不换已画节点；块 key 按类型计数，中间 `p` 变 table 不换后面标题/列表；不每 token 跑 remark |
| `FileCiteLink.tsx` / `.css` | 对话文件引用按钮，派发打开右侧预览 |
| `CodeArtifactBlock.tsx` / `.css` | 代码与命令输出编辑器外壳；`LiveFenceTail` 与收束后共用 `CodeArtifactShell` + 行节点（已闭合行 memo），直播即可复制；长行在对话柱内换行（不再 `min-width: max-content` 横向撑开直播贴底）；头栏同为语言标签 + 复制按钮，开闭不换一套 DOM；直播 diff 无行时也占住复制位，避免填行后头栏挤一下；未闭合围栏 `followTail` 把内层滚到最新行（外壳 `max-height` 后新行不再顶对话柱，用户上翻不抢，对标 Codex #32030） |
| `CodeDiffBlock.tsx` / `.css` | 行级 diff；无行时按 stats 占位以免写入完成才整块冒出；直播 `live` 不播 `view-enter`，同一 `.code-diff-code` 从占位填行且 `minHeight` 只升不降（对标 Codex #38695 / #22860）；直播中不折 20 行、内层 `followTail` 跟最新 +/-，超过预览行数时先占「收起变更」页脚，收束后同一实例保持展开（历史重挂才预览）；直播 +/- 统计预留 `+999` 宽，0 也占位以免数字出现时挤头栏（对标 Codex animated diff stat alignment）；`memo` 以免直播 token 重绘未变的 diff；默认换行长行（对标 Codex Wrap long diff lines）；`--wrap` 把行网格收成 `minmax(0,1fr)`，不再 `max-content` 横向撑开直播柱；审查模式 hunk 暂存/还原 + 行内评论；⌘/Ctrl+单击行打开预览；审查查找传入 `findQuery` / 当前行时高亮并展开预览外命中、`scrollIntoView` 跳到屏外行（对标 Codex long review file search） |
| `CommandPalette.tsx` / `.css` | Codex 式 ⌘K / ⌘⇧P 命令面板 |
| `ShortcutsHelp.tsx` / `.css` | ⌘/ 快捷键一览 |
| `CompareBlock.tsx` / `.css` | 旧/新对比行布局 |
| `MessageActions.tsx` / `.css` | 消息复制 / 从此条分叉（对标 Codex 气泡 Hover Fork / Branch earlier）/ 用户气泡编辑重发 / 失败重试；助手行在正文槽（散文 / diff / demo）上屏就挂操作条，尚无正文可复制时先占同一高度，避免写盘后第一句回答再冒出躲进输入框（对标 Codex #40788 / #41155）；直播可用 `getContent` 点按时再读，占位条不分叉 |
| `ChatImage.tsx` / `.css` | 对话渲染图悬停复制 / 保存（对标 Codex Save or copy rendered images）；附件走原文件，Markdown http(s) 图由主进程拉取；工作区相对路径经 `readFileDataUrl` 成图并可点开右侧预览（对标 Codex 在同一工作区打开图片），不认 `file://`；命中尺寸缓存或 data URL 文件头则带 `width`/`height` 与 `aspect-ratio` 首帧占位，未测到前 48px 高水位且 `eager` 加载，成图后高度只升不降，避免 8rem / 小图塌贴底 |
| `MermaidBlock.tsx` / `.css` | ```mermaid / ```mmd 开闭都挂本组件（对标 Codex transcript Mermaid）；未闭合不解析；`securityLevel:strict`；失败仍留同一 `CodeArtifactShell` 代码尾（不换 `CodeArtifactBlock`）；成图前与成图后共用外壳（只换体内代码行 → SVG）；成图前按源码估高占位，成图后 `minHeight` 只升不降，避免换成 SVG 时塌/涨把贴底顶跳；命中 SVG 缓存则首帧直接成图并按 viewBox 占位；源码改了先留上一张图再换，避免闪回源码撑开贴底 |
| `ModelPicker.tsx` / `.css` | 输入区按接入展开全部 knownModels；触发器与菜单均用短名；点选同时切换 provider + model；弹层关闭与 history 对齐；思考档位改由旁路 `ReasoningGauge` 调节（菜单里仍可点选） |
| `ReasoningGauge.tsx` / `.css` | 输入框旁思考档位条（对标 Codex compact composer gauge / 官方「model and reasoning control」）；点格或左右键升降；只展示当前模型官方档位；不跟直播 token 重绘 |
| `PlanBuildBar.tsx` / `.css` | 计划就绪后的 Build 操作栏 |
| `RightPanel.tsx` / `.css` | `memo`：不接收直播 token，App 把划选/关面板回调固定引用后 16ms flush 不重绘文件树/审查（对标 Codex sidebar jitter / review panel scroll jumps）；右侧可调宽面板（文件/审查/终端/浏览器/活动）；宽度按窗口比例写入 localStorage，缩放窗口时按比例重算（对标 Codex percentage-based file tree resizing，旧像素值仍能读）；审查传入 `gitBranchPrefix`、`/review` 对比焦点与项目附加文件夹（跨仓库审查）；文件树传入对话引用预览、项目附加文件夹与写盘 `revision`（树与打开的预览跟着 Agent 改）；终端按线程挂载（开过一次后切 Tab 不卸）；终端 / 文件预览划选可插入输入框或旁路提问；全屏时隐藏下层防叠字；`right-panel--compact` 抽屉 + 遮罩 enter/exit（遮罩自带 motion token，不依赖 panel 变量） |
| `InlineDemo.tsx` / `.css` | 对话内联演示：无外框、透明底、iframe 按内容真实底边撑高（只升不降）；直播未可绘先 96px 骨架叠在同一 iframe 上，可绘只换 srcDoc 不卸壳；首帧用声明高度 / 块数估高并缓存实测，避免 48px 猛涨把直播贴底顶跳 |
| `ProviderBrandIcon.tsx` / `.css` | 模型厂商官方标识图标（DeepSeek / xAI / OpenAI / Kimi / 智谱 / OpenCode） |
| `FeedbackDialog.tsx` / `.css` | `/feedback` 对话框（对标 Codex：分类 / 说明 / 附带会话）；只复制本机诊断，不上传 |
| `ShareDialog.tsx` / `.css` | `/share` 只读快照（对标 Codex Share）：打开时拍一帧，预览脱敏 Markdown，复制到剪贴板，不上传、不含工具 I/O |
| `ReviewScopeDialog.tsx` / `.css` | `/review` 空参数先选未提交 / 相对基线 / 指定提交（对标 Codex Choose Review against a base branch or Review uncommitted changes）；选定前不派发回合 |
| `MemoryChatDialog.tsx` / `.css` | `/memories` 空参数先选本对话使用 / 写入 / 关闭 / 跟随全局（对标 Codex chat-level memories；不改全局 Enable memories） |
| `ProjectFoldersDialog.tsx` / `.css` | 编辑项目：主文件夹 + 附加文件夹（对标 Codex Edit project）；附加夹可「设为主文件夹」，旧主路径留下；Git / AGENTS.md / Skill 仍走主路径 |
| `ErrorBoundary.tsx` | 渲染错误捕获与降级 |
| `ARCH.md` | 本层架构说明 |
