# src/components — UI 组件

## 职责

- 聊天主界面、执行轨道、侧栏、标题栏、右侧面板与设置子面板等可复用组件
- 成对的 `.tsx` + `.css` 为组件本体与样式；材质遵循 [docs/ui-style.md](../../docs/ui-style.md)

## 同级目录

| 目录 | 说明 |
|------|------|
| [panel/](./panel/ARCH.md) | 右侧面板：文件树、审查、终端、浏览器、Subagents |
| [settings/](./settings/ARCH.md) | 设置页各 Tab 与通用设置原语 |

## 同级文件

| 文件 | 说明 |
|------|------|
| `Sidebar.tsx` / `.css` | `memo`：直播 token 不重绘侧栏（回调与进行中 id 在 App 固定引用，对标 Codex sidebar jitter）；导航/项目/对话（`data-conversation-id/title` 便于自动化恢复）；对话行 last-active 相对时间（`6 min` / `3 days ago`，每条用自己的 `updatedAt`，对标 Codex recents / #21960，不对标 #29175 全表同一时间，无 1s 心跳）；主导航铃铛开关 Activity（⌘⌥U，title leftover When Activity is available, select the bell…；未读对话徽标）；**Search chats** 入口打开输入框历史选择器（title 官方 Use Search chats to reopen a past chat…；对标 Codex Open Search chats from the sidebar，官方 Not assigned by default，⌘G 留给 Find next）；Skills 入口（对标 Codex open Skills in the sidebar）；Scheduled 入口与未读徽标（对标 Codex desktop sidebar Scheduled）；**进行中**任务行（并行线程置顶）；**置顶**分组；对话旁 Codex 式筛选（Chronological / Running / Waiting / Unread / Scheduled / Pinned；按钮 aria 用官方 If you're missing chats… Chronological，不发明 Filter chats；Activity 空态用官方 Activity view brings together…）；**Add new project** 在 Chats 旁（对标 Codex troubleshooting）；项目三点 / Remove 的 title 官方 To remove a project from the sidebar… three dots… Cmd+O；Projects 分区 title/aria 官方 The Projects view brings ChatGPT projects and local projects into one place.；空态 **No projects**（对标 Codex #19290，不把 Add new project 当成空行）；置顶项目 title 官方 Pin a project to keep it near the top of the sidebar. / Pinning doesn't add context… / Pin a chat when you return to it often…；筛选菜单在有未读时提供 **Mark all as read**（只清对话未读；Activity 开时 filter title leftover Use the view's options…）；⌘⌥U 开关 **Activity**（默认 Waiting）；Running / Unread 点占同一 `sidebar-row-status` 槽，开停回合不挤标题；进行中点滚出视口停脉冲（对标 Codex #16857）；双击 / ⌘⌥R 行内改名；Worktree 线程小标；对话右键线程菜单含 Open in Finder、Copy as Markdown、Rename、Pin / Unpin、Archive（对标 Codex Open in Finder / Copy as Markdown / #25646 / #33217）；项目菜单含在访达中显示、Edit project、Pin / Unpin、Rename、创建永久 worktree（title 官方 If you want a long-lived environment… three-dot menu…）、Archive chats（一并归档该项目对话，进行中跳过，对标 Codex Archive chats）；enter/exit 统一 180ms 卸载；展开时顶栏收起按钮；收起后左缘热区 peek（pointer/mouse）滑入；设置导航含 Permissions / 通用 / Worktrees / 浏览器 / 外观 / 通知 / 个性化 / MCP / 建议提示 / Profile |
| `ChatView.tsx` / `.css` | 聊天主视图：消息列表、滚动；无本地项目空对话用官方 Start a chat without a project… / Create a project first…；长线程先挂最近一段，上滑到顶先揭示内存窗再向主进程取更早一页进独立 `historyHead`（不 prepend 尾页），并滚到与尾页相接处（对标 Codex older history fetched as needed / scroll-to-top 分页，不预挂全量、无「加载更早」按钮；⌘↑ 只进最旧一页，下翻 `nextHeadRange` 再接回尾页，不把瘦身全文灌进 `messages`；直播中不取头页，收束后再取以免卡住贴底）；⌘F 查找只扫用户/助手正文（↑↓ aria Find previous match / Find next match，× Close；计数官方 x/y，无命中 `0/0`，nowrap 以免挤矮直播柱），盘上命中不回放整段，跳到未加载处再揭开命中起的有界一段；直播命中单独算且只订 streaming 正文，`nextLiveFindHits` 追加无新命中不重扫，当前命中已画完且只追加时不重标 DOM，token 不重挂 `historicalRows`；loading 中 store 闪空也留下直播槽，不因直播体显隐塌高（对标 Codex #33907 / #22860）；点开瘦身后的命令输出 / 思考走 `onNeedFullMessage` 取一条全文（对标 Codex #38653）；直播行用本轮预留助手 id（`msg-${id}`）收束后同一 key 留下，历史助手不播 `list-item-in`；收束后 store 还留着本轮体时 `shouldMountLiveAssistantSlot` 仍挂直播槽并藏预留行，同一直播实例留下；跟进发送 `liveHandoffId` 先保住该行并开轮冻进 retired 环，立刻预留新 id 挂新槽，准备中 seed 等 freeze 提交后再发，已有新 id 不另挂 Thinking，`isStreaming` 在 hold 已冻结时跟新槽，首枚 harness chunk adopt 复用已预留 id、不再另挂槽（`pinActiveLive` 从开轮钉当前 id，hideReserved 翻转不搬槽；pinned 槽 `key={id}`，行根不另挂 `key={liveRowId}`；`nextPinnedTranscriptGaps` / `reusePinnedTranscriptGaps` / `nextPinnedLiveAssistantIds` / `nextFrozenPinnedLiveSlots` / `nextActivePinnedLiveSlots` / `shouldAttachLiveApprovalToPinnedSlot` / `nextPinnedLiveRowNodes` / `nextPinnedAfterGaps` / pin 列、`frozenPinnedSlots`、`activePinnedLiveSlots` 与 `pinnedLiveRows` `useMemo`，persist 入列复用同一 gap 与 pin id，冻结槽与行节点不跟 loading，活动槽只让当前预留 id 接审批与 loading，handoff 未 adopt 前 hold 也不跟、不重挂上一轮；直播槽不订 `historyHasReserved`，历史行只跟 `historicalSource`，`nextHistoricalRowNodes` / `nextPinnedAfterRowNodes` 挤出 / 归档冻结行只换该 id，贴底 setState / persist 入列 / 挤出环不重建其余 `historicalRows`、after 行与 pinned 槽；无 pin 才走 `shouldMountUnpinnedLiveSlot`，不先挂 `key=streaming`；当前行 `shouldStreamPinnedLiveAssistant` 仍跟 `liveStreaming`，retired / handoff 不跟秒表；`pinnedLiveAssistantIds` / `splitTranscriptAroundPinnedLive` / `shouldMountActiveLiveSlot`）；`retiredLiveArticles` 环留下最近两轮，挤出的行进 `ejectedLiveArticles` 仍用冻结正文画并记下真实行高，掉出 ejected 环的行进 `archivedLiveArticles`（当前对话不截断，切对话清掉）仍按冻结 part 与过程快照画（不抬 ejected / retired 上限）；loading 已关且直播体已空且历史已挂同一 id 时才换历史行（对标 Codex preserved streamed activity）；排队条在输入框上方（不进 `.messages`，预览限高以免挤视口，直播贴底不受排队行影响）；Composer 提交后只有普通空闲 `'send'` 才贴底并离开 `historyHead`（Queue / Steer 不改锁；划选 Add to chat 即使当时贴底也锁阅读位置，直播长高只出 Jump to latest / New response，对标 Codex #13698 / #38220 / #41391；已发送 annotation 芯片回跳原文段落并用 `sharker-selection` 短暂高亮（不盖 ⌘F））；`memo` 且不接收 `streaming` / `liveSegments` / `liveTurnMeta`（16ms flush 与工具心跳走 `useLiveStreamUi`，直播行过程/回答/元信息分订切片，历史列只在预留行入列后才订直播体布尔，只重绘回答 / 查找 / 未标未读前的「新消息」芯片 / 直播行元信息，对标 Codex #22860；直播行不盯行高 ResizeObserver，MutationObserver 只收新挂上的历史行，对标 #39120）；输入区交给 `ChatComposerInputs` + `ComposerDock`（不接收 `streaming` / `liveSegments`，直播 token 不重绘输入框与排队条）；流式贴底用 ResizeObserver 盯内容、滚动视口与 `composer-stage`（输入区与「新消息」芯片都在 flex 流里占位不浮盖正文，排队 / 芯片变高同一帧跟；柱尾 `LIVE_TAIL_SAFE_PX` 与输入区顶距留给操作条，对标 Codex #40788 / #38220 / #41155），布局后、绘制前写 scrollTop；开轮/收束不拆这只 observer（`shouldRebuildLiveStickObserverWhenLoadingChanges`）；未贴底也记下 lastHeight（`shouldRecordLiveStickHeightWhenUnstuck`，对标 Codex #37849 / #37872）；切对话清掉历史 demo iframe 池与历史回答 hold；空闲时按切片预热窗口外一页（`TRANSCRIPT_PAGE`）助手行 hold，每帧一条，直播中不跑；靠近顶预取更早盘页并 idle 预热，揭开套 hold；离开聊天页把滚动快照交给 App（窗口内按会话恢复，贴底的仍贴底，读历史的钉 scrollTop 与窗口起点，内容未画高先按距底占位，对标 Codex 26.406 Preserved thread scroll position per conversation）；审批 / Ask User 出现时已贴底才跟（读历史不解锁、不 `scrollIntoView`，Enter/Esc 仍走输入框，对标 Codex #38220）；远离底部的旧消息才 `content-visibility`，贴底附近用 `message-row--near-live`；远窗历史围栏缓存未命中不在揭示帧跑 highlight.js（`FenceImmediateHighlightContext`）；`rememberNearLiveHighlightPreference` 进过贴底窗的行本会话保持立刻着色（换会话清空）；离开窗口的行写入实测 `contain-intrinsic-size`（第一帧也用已测量高度；远窗 RO 量到真高后 rAF 写在行上、不抬 React state，避免 160px 估高跳也不重建 `historicalRows`；切对话 `sessionKey` effect 清测量表后立刻 `mergeSeededRowHeights` 灌回挤出真高与滚动快照 `rowHeights`，flush 用 `resolvePreviousRowIntrinsicSize` 信已灌真高、不把空表当成 160px，对标 Codex #38220 / #39120）；行 fork / edit / retry 用 `cachedIdCallback` 稳住引用，刷高不拆 `UserMessageRow` / `AssistantMessage` memo；刷高只补完全在视口上方的行（`nextAboveFoldHeightScrollTop`）；揭示更早/更新按内容柱 `scrollHeight` 差补 scrollTop（`nextRevealPreserveScrollTop`），不按未测行高卸顶；收束换消息只有距底 ≤16px 才强制滚到底；`shouldIgnoreLeaveBottomDuringCommit` 直播中思考收回或收束换行时不把距底突增误判成上翻锁，并连跟几帧贴底（对标 Codex #37872 / #37849）；历史行 `useMemo` + 用户气泡 `memo`；用户气泡可就地编辑并重发；历史气泡悬停「从此条分叉」只拷到该条（含），直播行不分叉（对标 Codex fork from an earlier message / 拒绝 in-progress lastTurnId）；气泡与输入框 `pre-wrap` + `overflow-wrap:anywhere`，CRLF 先归一再画，长 URL 不撑开直播柱（对标 Codex #37709 / #38380 / #38704）；空输入 Esc+Esc 回编上一条并分叉；划选历史正文出「加入对话 / 旁路提问」（对标 Codex Add to chat 与 Ask in side chat，不抢直播行/输入框）；用户气泡把 `# Selected text:` 收成可再预览的 annotation 芯片，只露请求以免长划选撑开贴底（对标 Codex #22670 / #20294）；`/copy` 有代码块或引用时在输入框上方列出 Whole response / `{lang} code` / Blockquote（对标 Codex #39997 /copy picker，Esc 关闭，不进对话）；「回到底部」在 `composer-wrap` 顶部占位（读 `historyHead` 时即使页短也显示，点了回尾页，不 absolute 盖直播尾）；读历史时直播又长高则改成「新消息」，不改 scrollTop（对标 Codex #38220 new message / #40788）；「新消息」芯片只在离开底部时订直播 store，贴底时不跟 token；⌘F 查找条在滚动层外占位（不 sticky 盖正文，对标 Codex #40788 / #38220）；`.composer-box` 内仍可触发，有划选则预填（对标 Codex Find starts with current text selection；审查面板聚焦时不抢，改走审查 diff 查找）；⌘G / ⌘⇧G / F3 / ⇧F3 始终 Find next / previous（未开先开再跳，同一条多处各算一次，正在直播的回答也算，CSS Highlight 只扫气泡正文；命中只改对话柱 scrollTop 并锁贴底，不 scrollIntoView，以免直播增高把镜头拽回底部）；直播中点「回到底部」用即时滚动，不跟 ResizeObserver 抢 smooth；⌘↑ / ⌘↓ / Home / End 跳到对话顶/底（输入框 / 查找 / 预览 / 终端 / 浏览器不抢，对标 Codex #39181；End 回到贴底跟直播）；点对话柱后 ↑↓ / Page / Space 原生滚动且上翻锁贴底（对标 Codex 桌面 #39851）；空对话建议芯片（可关）；隔离目录被清理时显示 Restore 横幅（官方 worktrees：reopen 后 restore from snapshot，不对标 #33814 Couldn't check worktree status；文案 nowrap+ellipsis 以免英文折行挤高直播贴底）；主线程子 Agent 步骤点开活动；完成后改文件芯片打开审查并切到本轮；直播廉价尾与收束 Markdown 共用块边距 / 任务列表 / 图片样式；渲染图可复制或另存，直播图先占位再成图以免贴底被撑开；直播与项内表格 `table-layout:fixed` 以免长单元格撑开对话柱 |
| `GoalProgressRow.tsx` / `.css` | 输入框上方的线程目标进度行（对标 Codex Goal）：region aria **Goal mode**，title 用官方 When a goal is active, the app shows its progress above the composer…（不把说明画进直播输入区）；Pause / Resume / Edit / Clear / Save，状态 Active / Paused；`/goal edit` 用 `editTick` 打开编辑；`startedAt` 用独立秒表显示耗时，Paused 用 `pausedAt` 冻结（对标 Codex #29370），满 24h 走 `1d 0h 0m` 并预留 `GOAL_ELAPSED_CLOCK_RESERVE_CH`（`99d 23h 59m`）宽以免跨日挤 composer-stage（对标 Codex #20558）；不接收直播 token |
| `ComposerDock.tsx` | 输入区独立树（Send / Stop 对标 Codex 直播输入区）：`/` `@` `$`（`/` 列表 aria **Slash commands**（官方 `/review` 与 Worktree 仅在当前项目是 Git 仓库时出现，无仓或非 Git 不列；探测未完成不把已有 worktree 对话打回 Local），`$` 列表 aria **Skills**；命令面板 **Force reload skills** 重扫盘上 SKILL.md 再灌 `$` 列表，`@` 列表 aria **Type @ to search for a file in the workspace and add its path to the prompt.**，命中文件 → Skill → 对话以免先扫线程史，空标题回退 New chat、不发明「对话」行说明；`@` 菜单含 Skill，发送前 `$name` 芯片只露官方 `$skill-name`、描述进 title，以免长说明挤高直播贴底）、模型旁思考档位条（**Pick a reasoning effort**：Light / Medium / High / Extra High / Max，不发明 Ultra）、Fast 芯片与可选上下文用量环（对标 Codex composer gauge / `/fast` / Show context window usage，不发明 service tier）、历史（标题/正文/分支扩匹配，空标题回退 New chat、空列表 No chats）、项目选择器（listbox aria Open project picker / Search projects，空列表 No projects，对标 Codex Search projects from the Projects view；不发明 Search projects… 占位长句）、附件（`composerSeed.attachments` 收官方 Appshot）、听写（失败 Unable to transcribe audio）/ Voice 芯片（可见 Voice；空闲新对话 aria Start new voice chat，已有对话 Start voice chat，进行中 End，nowrap 以免挤直播贴底；不发明 Settings → Voice）、忙时 Enter 按 `followUpBehavior`（默认 Queue）/ ⌘⇧Enter 反转（Steer 失败改 Queue，不 abort）/ Tab Queue（⌃Tab / ⌘Tab 不补全也不排队；忙时 `/` 与 `!` 先排队、收束后再解析，对标 Codex Tab queue slash）、无菜单时 Shift+Tab 切计划模式（对标 Codex Best practices，不抢 `/` `@` `$` 补全）、`composerEnterBehavior` 三档（对标 Codex Enter 发送）、审批打开时 Enter 允许一次 / Esc 拒绝、Ask User 打开时禁用输入并提示 Answer the questions to continue.；空闲已有对话占位 Ask for follow-up changes（对标 Codex #27583），空新对话仍用本机提示；placeholder / Ask for approval / Local / Worktree 芯片 nowrap 以免挤高直播贴底、划选芯片备注占位 Comment（nowrap）、输入框下方 Permissions 组 aria + Ask for approval / Full access 芯片（对标 Codex permissions control beneath the composer，不发明 Approve for me / Auto / 命名 profile）、Plan mode / Local / Worktree 芯片（组 aria **Codex environments**，芯片 title 用官方 Local: work directly… / Worktree: isolate changes…，不把顶栏 Hand off 写进 composer；Plan mode 不跟直播 token 变、不自动开一轮）；**Starting branch** 在 composer 下选起点（对标 Codex Select the starting branch），title 用官方 By default, Codex works in a detached HEAD. / If you chose a branch with local changes…，标签 nowrap 以免英文折行挤高直播贴底；可搜索本地与远程跟踪分支，远程保留 `origin/…` 完整 ref（对标 Codex local branch search / #22635）；`/copy` 选择器（Copy from response：Whole response / `{lang} code` / Blockquote）与 `/` 一样挂 `composer-popover-slot`（绝对定位），不占 composer-stage 高度以免直播贴底被挤矮；`/` 列表并入已安装 Skill（选中写入 `$name`，不盖内置命令）；切对话 / 工作区时恢复未发送草稿、附件与划选芯片（对标 Codex restore unsent prompts / selected-text previews）；划选进 `Selection N` 芯片不灌 textarea，可预览并加备注，发送收成 `# Selected text:`，`/` 与 `!` 仍清芯片（对标 Codex selected-text previews / #22670 / #33763，不发明 #22677 跟进评论条）；空输入 ↑ / Ctrl+R 提示列表 aria **Restore previous composer prompt**（title leftover If you started a chat with the wrong target…；对标 Codex Commands：When the composer is empty；不发明 TUI Prompt history 标题）；Worktree 芯片 title leftover If you cancel worktree creation by mistake…；空输入 Esc+Esc 回编上一条用户气泡（不把草稿填回输入框）；粘贴优先 text/plain（避开 Office 图片层），Finder/Explorer 源码文件按原名收成附件（对标 Codex non-image file pasting），超过 10,000 字收成 `Pasted text.txt`，芯片 **Show in text field**（nowrap，对标官方 Pastes longer than 10,000 characters）可预览/收回输入框，关闭 aria **Remove**；深链 `prompt=` 经 `composerSeed.nonce` 覆盖写入，划选插入走 `mode:'append'` 接在草稿后；`onSubmitted` 带提交 mode；不接收 `streaming` / `liveSegments`；直播中 `speechHint` 置空以免跟 token 重绘；有目标时在输入框上方挂 `GoalProgressRow`；中止直播走 `shouldInterruptTurn`（默认 `Esc`，设置可改绑/解绑；IME 选词不触发） |
| `ComposerQueue.tsx` / `.css` | 输入框上方 Steer 预览 + Queue 条（对标 Codex pending steer / queued follow-ups）：Steer 在上可 **Edit** / **Delete**、Queue 可重排 / **Send** 或直播 **Steer**；按钮 **Edit** / **Save** / **Delete**（对标 prompting edit, reorder, send, or delete them；Save 用官方 Settings Save；不发明 Move up / Move down）；短标签 `Steer` / `Queue n`（不把 TUI 长标题画进芯片）；动作 nowrap 以免挤高直播贴底；预览最多 3 行 / 240 字且整条队列限高（对标 Codex #39864 / #40788，长排队不挤直播视口）；直播中芯片主操作是 Steer 推进当前回合，空闲才 Send，失败不中止直播；CRLF 归一并折断长 URL；不进对话滚动区 |
| `ChatToolbar.tsx` / `.css` | `memo`：直播 token 不重绘顶栏；侧栏 **Toggle sidebar**（⌘B，对标 Codex Commands）、New chat、Fork（对标 Codex conversation fork / `/fork`）、Share（对标 Codex #40832；对话框仍是本机只读快照）、三点 Copy 子菜单（Copy working directory / Copy session ID / Copy deeplink / Copy as Markdown，对标 Codex threadHeader Copy / #25201 / #28233）、Open in Popup Window（弹出窗顶栏同一文案，nowrap）、弹出窗 Always on top、Hand off（可见文案始终 Hand off，title 官方 Use Hand off in the chat header…，不发明 Hand off to Local）、worktree 顶栏 **Open**（对标官方 open your IDE to the worktree；`file_opener` 走 vscode/cursor URI，none 走系统打开目录；访达揭示仍是 Open in Finder）、Create branch here、当前分支 PR 芯片（可见 PR #N，aria **Open a pull request**，title 官方 When Codex has GitHub access…，nowrap 以免长标题挤高顶栏）、Local environment `[[actions]]`（顶栏芯片可见 toml 名，aria **Run environment action 1**，title 官方 Use actions to define common tasks… / If you run a command regularly…，多条时菜单；⌘⇧D 跑第一条，对标 Codex Commands；不发明 Environment actions 菜单标题或 Settings 环境编辑器）、Open Terminal 图标（title 官方 Terminal appears stuck. Close the terminal panel… Ctrl+`，aria 仍 Open Terminal；对标 Codex #30659 / learn.chatgpt.com Integrated terminal，先开终端不关）、右侧面板 title/aria **Toggle bottom panel**（官方 Commands） |
| `LiveAssistantParts.tsx` | 直播行：过程时间线 / 思考旁白 / 回答列表 / 操作条分订 `live-stream-core` 切片；闭合与尾同一列表，封口按 `part.id` 留下 `StreamingMarkdown`；正文或思考 token 不重跑过程 / 回答 `buildAnswerParts`；`nextLiveProcessTimeline` 不含 `thinkText`，思考增长不抬 TurnFlow；旁白折叠时不跑 `liveThoughtBody`（对标 Codex #22860）；收束后同一行留 Thought，`frozen` 停订旁白 store；写盘 +/- 即使被正文挤进 closed 也保持 `live`（不折 20 行、内层跟尾，对标 Codex #32030 / #38695）；收束关 loading 后过程 `isStreaming` 停秒表，回答 diff 仍 live 以免折 20 行跳；跟进 adopt 后 `frozen` 停订 store，按 adopt 前 part 引用留下树；历史重挂 `frozenProcess` 保住 Thought / 时间线；挤出环后的历史冻结行 `liveDiff={false}` 以免 followTail 抢滚动；直播 `StreamingMarkdown` 标 `live`，闭合围栏直播 token 中不跑 Prism，收束后命中预热缓存则同一帧着色，否则 effect 再着色；`streaming` 跟 loading，收束后命中缓存则同一帧画 mermaid / KaTeX；`present_inline_demo` 标 `live`，收束不重写 srcDoc 灌套壳；本轮路径列表交给 `FilesChangedCard`，+/- 另订片段切片、正文加长或无 +/- 新工具不扫指纹、不重跑合计；操作条用回合开始时刻做 hover 时间戳，空槽不挂 |
| `FilesChangedCard.tsx` / `.css` | 对话写盘卡：头栏官方 Edited basename / Edited N files，标题打开本轮审查，展开列短标签（默认 basename，同名带最短父路径）、文档/图片种类与 +N −M，点文件 Open，右键 Open / Open in Finder / Copy path（对标 Codex render_changes_block / #20700 / #21426）；直播头栏 +/- 预留等宽；不订直播 token，不发明回合 Undo |
| `AssistantMessage.tsx` / `.css` | 助手消息：`memo` 避免直播拖着历史行重绘；点开瘦身后的思考 /「查看输出」再取完整消息（对标 Codex #38653）；直播思考/工具在上（对标 Codex 时序），收束后过程芯片仍留在回答上方以免整块对调拽走贴底；过程区间距固定，不在正文出现时再加分隔；过程片段引用没变时复用同一数组，避免回答 token / status 心跳重挂 TurnFlow；收束后历史行 `seedHistoricalAnswerHold` 带回正文部件 / 过程切片 / `frozenSteps`，窗口重挂不再 `buildAnswerParts` / `deriveProcessPhases` / `deriveChronologicalSteps`；Cursor 式可折叠 Thought（无灰卡片）；正文/内联演示/写盘 diff 在下（演示一开始占同一槽，未可绘先骨架；写入一开始占同一 diff 槽，参数流填已解析 +/-，完成后换核实行（对标 Codex 约 0.5s 逐文件变更）；直播 `LiveFileDiff` memo，`reuseAnswerParts` 在预览 token 下保住已闭合 diff 对象以免重挂；`live={streaming}` 时不折 20 行、不折行、内层跟尾，收束后同一实例保持展开以免跳（历史重挂刚离开直播槽时 `preserveLiveDiffs` 仍 `defaultExpanded`，但不 `live` 以免 followTail 抢滚动）；含脚注收束后仍用廉价树）；完成后过程芯片官方 Worked for / You stopped after / Edited N files（不发明浏览 N 个文件）；Thought 可展开（秒表走旁路时钟），真实工具另有过程行；写盘一开始就挂「已改 N 个文件」卡（对标 Codex Files changed / 回合内 N files edited；可展开短标签与种类、头栏与文件行 +N −M、右键打开 / 访达 / 复制路径）；正文槽（散文 / diff / demo）上屏就挂复制操作条，无可复制正文时先占位，收束不再整块冒出躲进输入框（对标 Codex #40788 / #41155）；历史行可从此条分叉，直播占位条不分叉；中止卡只露官方 You stopped after（不发明「已保留停止前」附文）；遗留无片段行过程芯片官方 Worked for / Read basename（不发明已处理 / 已浏览 N 个文件）；直播秒表不在本组件计时 |
| `LiveDuration.tsx` | 直播耗时独立组件，只在官方时钟文案会变时重绘秒表（秒档 1s，分/小时档对齐下一分钟）；`endedAt` / `paused` 冻结不再 tick（对标 Codex #29370）；Goal 行可传入 `GOAL_ELAPSED_CLOCK_RESERVE_CH` 预留宽 |
| `TurnFlow.tsx` / `.css` | 直播过程：思考默认折叠成 Thinking（对标 Codex 桌面，避免增长正文顶回答）；收束后同一直播行改 Thought（`shouldShowLiveThought`），无步骤也留下折叠条；过程区 aria 用官方 Working / Worked for，旁白正文 aria 用 Thinking / Thought；直播旁白 `LiveThoughtFromStore` 另订已发布的 `turnThinking`（不跑 `liveProcessViewFromSnap`），时间线不接收思考原文；`frozen` 保住收束前旁白，历史重挂用 `frozenThinkText` / `frozenSteps`，不再 `deriveChronologicalSteps`；连接中一行状态字+耗时（`LiveDuration`）；生成演示时改头标签；有工具才展开时间线；正文已上屏或回合结束后把过程收成 Working / Worked for（对标 Codex，点开才看步骤；审批/失败仍露出；回答刚上屏时收回已展开的 Thought / Worked for）；直播头只用官方 Awaiting approval / Working，不另挂「审批」芯片或「处理中」以免挤高贴底；`memo` 过程行与整棵树，已完成步骤保持同一对象，同一工具只改详情、写盘 +/- 或收束不重扫整条时间线；关 loading 走 `remapProcessPhaseStepsOnStreamEnd`（收束不必是末步；同一帧多条只读并行 complete_call 也只换这些步，不发明 Exploring 分组格；写盘收束同时新开工具时 remap 并追加，写盘收束同时新开 status / 思考 / 散文 / ```demo / compress / 错误 / present_inline_demo / status+思考 / 思考+散文 / status+散文 时 remap，status / compress 再追加该行；无新写盘收束同时新开 status+思考 / 思考+散文 / status+散文 / status+思考+散文 / 思考+```demo / status+```demo / status+思考+```demo / status+工具 / 思考+工具 / status+思考+工具 / status+present_inline_demo / 思考+present_inline_demo / status+思考+present_inline_demo / status+错误 / 思考+错误 / status+思考+错误 / status+compress / 思考+compress / status+思考+compress / status+Stop / 思考+Stop / status+思考+Stop / 思考+Ask/Awaiting / status+思考+Ask/Awaiting 时 remap，有 status 再追加该行与新工具（思考、演示与错误正文不进过程；compress 追加；Stop 追加 cancelled status））、前缀没变或只收束散文或无新写盘的工具（正文/思考可先加长再标 done）时新开一或多个工具（可带一条 Awaiting / Question requested 行）或同一帧已收束的 tool_start+tool_done（可夹 think / 首枚 token）只追加这些步并封回答尾、新思考只换旁白（无新写盘的工具收束后同一帧也走这条；think 尾 + 首枚 token 可先加长再标 done）、新散文只开回答尾、新演示或正文 ```demo 只开回答槽且过程不追加、新 status 只追加一步、审批挂上或收束只换工具步与 Awaiting 行，挂起后同一帧可再夹 think / 首枚 token / ```demo / compress / 错误（Deny 后同一帧 resolved + tool_done error 只把该行与工具收成 error，可再追加 规划下一步或下一工具；Allow once 后同一帧 resolved + 首枚 tool_preview 只换该行与写盘 +/-，回答只换 diff 槽，可再追加 规划下一步或下一工具；Allow once 后同一帧 resolved + tool_done 只把该行与工具收成 done，可再追加 规划下一步或下一工具；不复制 #38695）、Ask User 挂上或改写规划下一步为 Question requested / 第一题 header 或单条 status 收口只换工具步与该行，挂起后同一帧可再夹 think / 首枚 token / ```demo / compress / Stop（写盘收束或 Reconnecting / 规划下一步后同一帧也走 remap，可再夹 think / 首枚 token / ```demo / 下一工具 complete_call / compress / Stop，回答只换 diff 槽），作答后同一帧 resolved + tool_done 只把 Question requested 行与工具收成 done、Stop 把多条 active 收成 cancelled 只换这些步、错误收口 status 或无新写盘的工具后只开错误回答尾，避免回答 token / status 心跳重挂时间线；命令输出按 `toolOutputDisplay` 截尾/折叠，`<pre>` 只在 `<details>` 打开后挂载；占位输出点开再取全文（对标 Codex #38653）；直播中不挂「查看输出」/ 退出码 / 「执行中… Ns」摘要 / 命令末行直播头与过程行/直播头秒表心跳 detail、也不自动展开 verbose，对标 Codex #19260）；秒表预留 `1h 59m` 宽以免跨分钟挤过程区；工具间隙直播头停在最后一条实质步骤，不闪「规划下一步」（对标 Codex flashing thinking summaries）；正文已上屏时仍隐藏命令输出以免过程区顶回答；子 Agent 步骤可点开活动；官方 `view_image` 过程行标题 Viewed Image，完成后画 ImageView（工作区路径走 `ChatImage` 占位高，直播中不挂 <img> 解码，不灌 base64 / 不发明画布）；`web_search` 完成后挂 title+url 来源花片（对标 Codex webSearch results，点进内置浏览器）；`update_plan` 在过程区画 pending / in_progress / completed 清单，行 key 用稳定 `plan-N`（对标 Codex PlanUpdate，不发明底栏 Step N/5 徽章）；MCP 过程行用 Calling / Called `server.tool(args)`，不倾倒 JSON（对标 Codex #20677，不抄 #22300）；`run_terminal_cmd` 用 Running / Ran + 命令（对标 Codex exec_cell）；探索工具用 Read / List / Search + basename（对标 Codex parsed Read/List/Search，不发明 Exploring 分组）；写盘过程行用 Edited / Deleted / Edited N files（对标 Codex render_changes_block） |
| `ProcessTimeline.tsx` / `.css` | 旧消息回退过程时间线；`key={step.id}` 用稳定 `think` / `tool-N`，命令加长或 Running→Ran 不重挂行；探索/写盘/命令标题走官方 Read / List / Search / Edited / Deleted / Ran；子 Agent 步骤 **Open**（对标官方 open a subagent thread） |
| `InlineApproval.tsx` / `.css` | 过程内高危操作审批块，眉题 Awaiting approval（nowrap），按钮 Allow once / Allow for session / Deny（nowrap，对标 Codex 桌面）；外壳 title 官方 If a chat appears stuck: Check whether Codex is waiting for an approval…；出现时 view-enter + 呼吸，`focus({ preventScroll: true })` 不 `scrollIntoView`（贴底由 ChatView 跟，读历史不抢镜头）；参数长行换行以免横向撑开直播柱；摘要只露官方 Calling `server.tool({compact})`，不另挂「查看操作参数」以免挤高直播卡；提交只转 spinner，窄屏不另占一行；不发明 always-allow 配置 |
| `InlineUserInput.tsx` / `.css` | Codex 桌面 Ask User：最多 3 题叠放，互斥选项 + 铅笔 Other（不再加自由作答副文，以免挤高直播卡），提交后继续；眉题 Question requested、标题 N questions requested / 第一题 header、提示 Answer the questions to continue.（nowrap 以免英文折行顶高直播卡）；提交中只转 spinner，不另挂「正在提交」以免挤高直播卡；不发明选项备注、分页问卷或 60s/90s 空答；`focus({ preventScroll: true })`，等待时按审批同样不抢贴底 |
| `ThinkingIndicator.tsx` / `.css` | `memo`：贴底 setState 不重挂扫光；兼容旧路径的轻量 Thinking；跟进发送保住上一轮直播行时单独挂一行（官方 Thinking）；秒表同样预留 `1h 59m` 宽；直播主路径用 TurnFlow 状态行；屏外停扫光 |
| `MarkdownBody.tsx` | Markdown 渲染；代码/diff / mermaid 分流；本地代码引用点开右侧预览；http(s) 与 `file://` HTML 默认进内置浏览器（对标 Codex clicking a URL / #32773）；http(s) 与工作区相对路径图走 `ChatImage` 可点开灯箱 / 复制/保存；保住 GFM 任务列表 class；元素子节点不套 span，避免收束跳动 |
| `ChatMath.tsx` / `.css` | 对话闭合公式画 KaTeX（对标 Codex 桌面 / #14985）；非法 TeX 回退原文；不认 `$...$`；`LiveMarkdownStreamingContext` 直播 token 中先画原文，闭合后 `shouldWarmLiveChatMath` 开工 KaTeX 写缓存、不 setHtml；收束后命中预热缓存则同一帧着色，否则 effect 再着色以免收束帧同步卡贴底；缓存命中重挂 `shouldStartChatMathPaintJob` 为假则不再 setHtml；远窗 `FenceImmediateHighlightContext` 为假时未命中成图 `shouldDeferChatMathPaintJob` 推到下一帧（近窗仍同步着色）；展示公式直播中也占 block 槽，收束不从行内跳成块；不订直播 token；不发明文件预览数学或围栏 tex 开关 |
| `StreamingMarkdown.tsx` | 直播正文：`React.memo` + 整段散文廉价增量（空行收段不再换 remark 树）；闭合 `\\(...\\)` / `$$` / `\\[` 挂 `ChatMath`（未闭合不吃；直播 token 中先画原文，收束后再跑 KaTeX）；含脚注收束后仍用本树，不再换 remark 跳贴底；CRLF 按 LF 拆；未闭合围栏与已闭合围栏都在顶层 `live-fence-N` 槽（闭合不搬进散文尾重挂）；直播 token 中闭合围栏不跑 Prism，闭合后 `shouldWarmLiveFenceHighlight` 开工 highlight.js 写缓存；收束后命中预热缓存则同一帧着色，否则 effect 再着色；`streaming` 跟 loading，收束后再画 mermaid / KaTeX；收束后的拆分 / 槽 / 闭合廉价块写入 hold，窗口卸下再揭示不整段重拆；直播中闭合公式 / mermaid 同样只预热不画；```demo 继承 `live` / `streaming` 上下文，直播实例不重写 srcDoc，历史重挂再灌套壳；已收散文按块成闭合 LiveProseTail（`prose-${id}`），增长尾固定 `prose-run-0`，空行收段不换尾 key，文末单独换行不收段；已画廉价块 / 列表项 / 表行 / 行内按对象身份 `memo`（新表行不换已画表头 / 旧行 cells），引用定义 Map 与渲染槽对象在 blob/正文没变时复用（`split.blocks` 同一数组时不重拆已收段，对标 Codex #22860）；`LiveFenceTail`（开闭至少三连，更长围栏可包住内部 \`\`\`）；散文尾廉价 ATX/Setext 标题（含行尾 `#`；下划线不到三连先保持段落）/列表（含 `1)`、`ol start`、缩进嵌套、续行硬换行与松散 `li>p`、项内表格 / 围栏 / 引用 / ATX / Setext / HR / 嵌套围栏 / 围栏 / 标题 / HR / 表后后缀 / 松散项内缩进代码（嵌套层自己松））/任务项/嵌套引用（`blockquote>p`、引用围栏与懒续行含硬换行；未闭合围栏不吃懒续行）/表格对齐与单列 / 无两侧 `|` 与 `\\|` / 分隔行未到先画表（左侧 `|` 未闭合也先画，未写完 `|` / `|:` 不当数据行；无两侧 `|` 的表头打 `---` 不升成 Setext）/分隔线（含 `* * *`）/缩进代码/脚注区（缩进续行与多段）/http 图（含 title、`![alt][id]` 相对 dest、alt 去标记，点开灯箱 / 悬停复制/保存）/mermaid 开闭都挂 `MermaidBlock`（直播 `mer` 起就挂，未闭合不解析，成图前后共用 `CodeArtifactShell`）/```demo 开闭都挂 `InlineDemo`（未可绘先骨架，不先画 `LiveFenceTail` 再换）/dest 内成对括号 / 未闭合 `](` 先画链接/未闭合 `**` / `*` / `~~` / `` ` `` / `***` / `<https://` 先画/`[![img]](url)`/多反引号代码/链接标签内强调与代码/HTML 实体/删除线套粗体（两侧无空白才画）/标记内混排与链接/下划线强调/`***` 嵌套强调/反斜杠转义/引用式链接含定义 title 与次行标题/邮箱/`www.`（不当文件芯片）/危险协议清空/硬换行与可点 http / 文件引用；全文引用定义挂到已闭合块；任务项用 GFM `contains-task-list` / `task-list-item`，未写完的 `[x` / `[ ]` 先占 checkbox（`[x](url)` 不当任务）；表体始终挂 `tbody` 以免首行才挂载；列表项从第一项就 `li>p`（对标松散 `li>p`），空行变松时只加后续段、不把已画项从裸文本换成 p；未写完的 `-` / `1.` 不并进上一项；`continueCheapProseBlocks` 最后一块（含段落软换行、嵌套项内引用 / 围栏、围栏 / 标题 / HR / 表闭合后的项后缀（标题后的表行另起项内表；闭合并栏后再起表 / 标题 / 引用 / 段落时围栏不动；缩进代码后的标题不并进 pre）、引用内围栏 / 标题 / 分隔线 / 缩进代码闭合后再起的后续段、闭合段落 / 表 / 列表 / 分隔线 / 缩进代码 / Setext 标题后再起的后续块（列表 / 表后的 Setext 用正文+下划线定位；前面已有同型引用 / 列表 / 表 / 围栏 / 缩进代码时从文末量最后一块）、缩进代码 / 脚注续行 / 引用内换行后的列表项、围栏 / 表 / 列表 / 引用 / 段落后的增长段）只重解析增长段，前面的标题/段落保持不动（对标 Codex #39061），中间块类型变了也保住后面已闭合项；行内 key 用类型+前缀长度，闭合标记不换已画节点；块 key 按类型计数，中间 `p` 变 table 不换后面标题/列表；不每 token 跑 remark |
| `FileCiteLink.tsx` / `.css` | 对话文件引用按钮；点开预览或官方 URI；右键打开 / Open in Finder / Copy path（对标 Codex file citation Open menu / #13123），不接自定义 Open with，不订直播 token |
| `ChatLink.tsx` / `.css` | 对话 / 直播 http(s) 与 `file://` HTML 链接：点进内置浏览器，⌘/Ctrl+点进系统浏览器，右键内置 / 系统 / 复制（对标 Codex clicking a URL / #41122 / #32773）；不订直播 token，不发明默认打开设置 |
| `ContextUsageDonut.tsx` / `.css` | 输入框模型旁上下文用量环；直播增量按整百分桶订阅，不跟每枚 token 重绘、不抬 ComposerDock（对标 Codex Show context window usage / #22860） |
| `CodeArtifactBlock.tsx` / `.css` | 代码与命令输出编辑器外壳；`LiveFenceTail` 与收束后共用 `CodeArtifactShell` + 行节点（已完成行单独 memo，传入上一帧 code 只加长末行 / 追加新行，对标 Codex #39061 / #22860），直播即可复制；头栏相对对话柱 sticky，块还在视口里就能复制整段（对标 Codex #20593，按钮官方 Copy，不发明换行开关 / Copied）；闭合围栏才语法着色（对标 Codex 桌面 / #18966）；直播 token 中不着色，围栏闭合后 `shouldWarmLiveFenceHighlight` 开工 highlight.js 写缓存；收束后命中预热缓存则同一帧着色，否则 effect 再着色，贴底窗口历史首帧着色，远窗缓存未命中交给 effect（`FenceImmediateHighlightContext`）；未闭合围栏保持纯文本；长行在对话柱内换行（不再 `min-width: max-content` 横向撑开直播贴底）；头栏同为语言标签 + 复制按钮，开闭不换一套 DOM；直播 diff 无行时也占住复制位，避免填行后头栏挤一下；未闭合围栏 `followTail` 把内层滚到最新行（外壳 `max-height` 后新行不再顶对话柱，用户上翻不抢，对标 Codex #32030）；只盯滚动壳与一层增高节点，不因 children 每枚 token 重挂跟尾 effect（对标 #22860 / #39120） |
| `CodeDiffBlock.tsx` / `.css` | 行级 diff；无行时按 stats 占位以免写入完成才整块冒出；直播 `live` 不播 `view-enter`，同一 `.code-diff-code` 从占位填行且 `minHeight` 只升不降；已画 +/- 行按对象身份 memo，只重绘增长行（对标 Codex #38695 / #22860）；直播中不折 20 行、内层 `followTail` 跟最新 +/-，超过预览行数时先占「收起变更」页脚，收束后同一实例保持展开（历史重挂才预览）；直播 +/- 统计预留 `+999` 宽，0 也占位以免数字出现时挤头栏（对标 Codex animated diff stat alignment）；`memo` 以免直播 token 重绘未变的 diff；默认换行长行（对标 Codex Wrap long diff lines）；`--wrap` 把行网格收成 `minmax(0,1fr)`，不再 `max-content` 横向撑开直播柱；审查模式 hunk Stage / Unstage / Revert（nowrap）+ 行内评论（列表 aria 官方 Review findings appear as inline comments…，title 另挂官方 If you ask Codex to apply the fixes it finds…，表单说明官方 Because comments are line-specific… After you finish leaving feedback… / Codex treats inline comments as review guidance. / Write your feedback and submit it. / Hover… / Select the + button that appears.，取消 / 留下评论仍本机）；⌘/Ctrl+单击行打开预览（title 官方 Clicking a single line while holding Cmd pressed…）；审查查找传入 `findQuery` / 当前行时高亮并展开预览外命中、`scrollIntoView` 跳到屏外行（对标 Codex long review file search） |
| `CommandPalette.tsx` / `.css` | Codex 式 ⌘K / ⌘⇧P Open command menu（对话框 aria 用官方 Open command menu，关闭 Close；含官方 Previous chat or tab / Next chat or tab / Go to chat 1–9 / Open recent chat 1–6 / Find next match / Find previous match / Restore previous composer prompt / Toggle review panel；搜索占位仍本机，不发明 Search commands） |
| `ShortcutsHelp.tsx` / `.css` | ⌘/ 叠层标题 Keyboard Shortcuts、aria Open keyboard shortcuts、关闭 Close（对标 Codex Commands / Settings → Keyboard Shortcuts；不发明快捷键副文） |
| `CompareBlock.tsx` / `.css` | 旧/新对比行布局 |
| `MessageActions.tsx` / `.css` | 消息 Copy / Fork / Edit（对标 Codex 气泡 Hover Copy / Fork / Edit / #20643）；用户气泡编辑提交用官方 Send；失败重试仍本机「重新运行」（不发明 Retry）；悬停操作行显示静态时间戳（对标 Codex #23849 / #20643，不发明 #28091 常显开关、不每秒刷新）；助手行在正文槽（散文 / diff / demo）上屏就挂操作条，尚无正文可复制时先占同一高度，避免写盘后第一句回答再冒出躲进输入框（对标 Codex #40788 / #41155）；直播可用 `getContent` 点按时再读，占位条不分叉、空槽不挂时间戳 |
| `ChatImage.tsx` / `.css` | 对话渲染图悬停 Copy / 保存图片（对标 Codex Save or copy rendered images；Copy 与围栏同一官方标签，不发明 Saved / View larger）；点图开视口自适应灯箱，Esc / 点背景 / 工具栏关闭用官方 Close，尺寸用 CSS 像素 contain、不跟界面字号放大裁切（对标 Codex image preview / #26851，不发明画布 / 拖出 / 多图廊）；右键页内菜单查看大图 / Copy / 保存图片，工作区图再打开 / 揭示 / Copy path（对标 Codex #17591 / #40778，不订直播 token）；附件走原文件，Markdown http(s) 图由主进程拉取；工作区相对路径经 `readFileDataUrl` 成图，灯箱可再打开右侧预览（对标 Codex 在同一工作区打开图片），不认 `file://`；命中尺寸缓存或 data URL 文件头则带 `width`/`height` 与 `aspect-ratio` 首帧占位，收束预取与重挂共用 `prefetchRemoteChatImageSize`；直播 token 中只占位、不挂 `<img>`（`shouldRenderLiveChatImage` / `LiveMarkdownStreamingContext`，过程 `view_image` 传 `streaming`），闭合 dest 后 `shouldWarmLiveChatImage` 开工尺寸 / 工作区 data URL 写缓存、不 setState；收束后命中预热则同一帧成图；尺寸缓存命中重挂 `shouldStartChatImageSizeTick` 为假则不再 setSizeTick；已在画同一工作区 data URL `shouldApplyCachedWorkspaceImage` 为假则不再 setState；远窗 `FenceImmediateHighlightContext` 为假时未命中预取 `shouldDeferChatImagePrefetch` 推到下一帧；未测到前 48px 高水位且 `eager` 加载，成图后高度只升不降，避免 8rem / 小图塌贴底 |
| `MermaidBlock.tsx` / `.css` | ```mermaid / ```mmd 开闭都挂本组件（对标 Codex transcript Mermaid）；未闭合或直播 token 中不解析（`shouldRenderLiveMermaid` / `LiveMarkdownStreamingContext`），收束后再成图；直播中围栏闭合后 `shouldWarmLiveMermaid` 开工 `renderMermaidSvg` 写缓存、不 setSvg；成图走共用 `renderMermaidSvg`，围栏闭合预热与收束预取能先开工模块与同一次 `mermaid.render`，立刻跟进的重挂不取消；缓存命中重挂 `shouldStartMermaidPaintJob` 为假则不再 setSvg；远窗 `FenceImmediateHighlightContext` 为假时未命中成图 `shouldDeferMermaidPaintJob` 推到下一帧（图仍会画）；`securityLevel:strict`；失败仍留同一 `CodeArtifactShell` 代码尾（不换 `CodeArtifactBlock`）；成图前与成图后共用外壳（只换体内代码行 → SVG）；成图前按源码估高占位，成图后 `minHeight` 只升不降，避免换成 SVG 时塌/涨把贴底顶跳；收束后 `resolveLiveMermaidSvg` 命中 SVG 缓存则同一帧成图并按 viewBox 占位 |
| `ModelPicker.tsx` / `.css` | 输入区按接入展开全部 knownModels；触发器与菜单均用短名；点选同时切换 provider + model；弹层关闭与 history 对齐；listbox **Open model picker**，档位头 **Pick a reasoning effort**（Light / Medium / High / Extra High / Max）；思考档位改由旁路 `ReasoningGauge` 调节（菜单里仍可点选）；不发明 Ultra |
| `ReasoningGauge.tsx` / `.css` | 输入框旁思考档位条（对标 Codex compact composer gauge / 官方 Pick a reasoning effort）；点格或左右键升降；只展示当前模型官方档位；不跟直播 token 重绘 |
| `PlanBuildBar.tsx` / `.css` | 官方计划模式收束 Action Menu：Proposed Plan / Implement this plan? / Yes, implement this plan / No, tell Codex how to adjust（对标 Codex 桌面 #10561 / TUI，不发明 Clear context） |
| `RightPanel.tsx` / `.css` | `memo`：不接收直播 token，App 把划选/关面板回调固定引用后 16ms flush 不重绘文件树/审查（对标 Codex sidebar jitter / review panel scroll jumps）；右侧可调宽面板（Files / Review / Terminal / Browser / Subagents）；Terminal 页签 title 官方 Terminal appears stuck. Close the terminal panel… Ctrl+`；关闭用官方 Close，全屏按钮用官方 Toggle Full Screen；宽度按窗口比例写入 localStorage，缩放窗口时按比例重算（对标 Codex percentage-based file tree resizing，旧像素值仍能读）；审查传入 `gitBranchPrefix`、`/review` 对比焦点与项目附加文件夹（跨仓库审查）；文件树传入对话引用预览、项目附加文件夹与写盘 `revision`（树与打开的预览在文件树内跟着 Agent 改，不抬 App）；终端按线程挂载（开过一次后切 Tab 不卸）；终端 / 文件预览划选出 Add to chat / Ask in side chat；浏览器当前 URL 经 `onBrowserAmbientUrl` 交给开轮 `# In app browser:`（对标 Codex #39562）；`browserMenuCommand` 把 View / 命令面板 Focus Browser Address Bar / Reload Browser Page / Browser back / Browser forward / Reload browser page without cache / Copy browser URL / Toggle browser browse or comment mode 交给已挂载的浏览器；`goToLineCommand` 把 Go to line or focus browser address bar 交给文件预览或审查跳行框（对标 Codex #30659 / learn.chatgpt.com commands）；浏览器批注进同一条 composer 芯片（对标 Codex Annotation mode）；全屏时隐藏下层防叠字；`right-panel--compact` 抽屉 + 遮罩 enter/exit（遮罩自带 motion token，不依赖 panel 变量） |
| `InlineDemo.tsx` / `.css` | 对话内联演示：无外框、透明底、iframe 按内容真实底边撑高（只升不降）；直播未可绘先 96px 骨架、不挂空 iframe，可绘再 srcDoc；首帧用声明高度 / 块数估高并缓存实测，避免 48px 猛涨把直播贴底顶跳；直播实例（含收束后留下的那行）父页不挂全树量高 ResizeObserver，iframe 也不扫整棵、不灌 KaTeX CDN、不灌终端套壳脚本 / 终端窗与卡片 CSS，只留主题与解锁裁切，信估高、range/body 底边与 postMessage；`live` / `streaming` 可从 `LiveMarkdownLiveContext` / `LiveMarkdownStreamingContext` 继承；历史重挂再灌套壳，`resolveInlineDemoSrcDoc` 按 walkTree+主题+HTML 缓存套壳 srcDoc，稳定 demoId 复用同一 srcDoc 引用，`adoptInlineDemoFrame` 把已加载 iframe 留在池里；srcDoc 刷新用 `liveInlineDemoPaintDelay`（对标 Codex #22860 / #39120） |
| `ProviderBrandIcon.tsx` / `.css` | 模型厂商官方标识图标（DeepSeek / xAI / OpenAI / Kimi / 智谱 / OpenCode） |
| `FeedbackDialog.tsx` / `.css` | `/feedback` 对话框（对标 Codex Share feedback / Include current Codex session logs）；说明用官方 Type / into the message composer to provide feedback… leftover；关闭用官方 Close；只复制本机诊断，不上传、不发明 Safety check |
| `ShareDialog.tsx` / `.css` | `/share` 只读快照（斜杠说明 Share a read-only snapshot of a local Codex thread.，对标 learn.chatgpt.com Use ChatGPT）：打开时拍一帧，预览脱敏 Markdown；标题 Share，说明用官方 snapshot 原文，按钮 Close / Copy as Markdown，不上传、不发明 Who has access / Copy link |
| `ReviewScopeDialog.tsx` / `.css` | `/review` 空参数先选官方 Review uncommitted changes / Review against a base branch / Review a commit；标题 Review，说明 Choose Review against a base branch or Review uncommitted changes.，关闭 Close（对标 learn.chatgpt.com/docs/code-review）；选择器 strong nowrap；选定前不派发回合。sha 栏仍本机，不发明 Commit SHA |
| `MemoryChatDialog.tsx` / `.css` | `/memories` 空参数先选本对话 Use memories / Generate memories / Disabled / Inherit；说明用官方 Use /memories to choose whether a chat can use local memories or contribute to future memories.（对标 Codex chat-level memories；不改全局 Enable memories） |
| `ProjectFoldersDialog.tsx` / `.css` | Edit project：说明用官方 New chats start in the primary folder…；分区 Primary folder / Secondary folders；空附加用官方 Projects don't need a folder, but you can attach folders as needed.；Add folder / Make primary；关闭 Close（对标 Codex Edit project / learn.chatgpt.com/docs/projects）；Git / AGENTS.md / Skill 仍走主路径 |
| `ErrorBoundary.tsx` | 渲染错误捕获与降级 |
| `ARCH.md` | 本层架构说明 |
