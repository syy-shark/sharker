/**
 * 直播行过程 / 回答切片：token 只换回答；正文或思考加长、同一工具只改详情时不扫过程指纹 / 正文 ```demo 只换演示槽、不重跑过程 / 全文 buildAnswerParts。
 * 工具详情只换该步引用；工具收束无新写盘也只换该步（不必是末步；同一帧多条只读并行 complete_call 也只换这些步，不发明 Exploring 分组格，对标 Codex exec_cell complete_call）；写盘 +/- / 参数或收束带核实 diff 只换该步，回答只换该工具的 diff 槽、已画正文不重拆（对标 ~0.5s / Edited 格，不复制 #38695）；写盘收束同时新开工具时过程 remap 并追加，回答只换该工具的 diff 槽；写盘收束同时新开 status / 思考 / 散文 / ```demo / compress / 错误 / present_inline_demo 时过程 remap（status / compress 再追加该行，思考续旁白，散文/演示/错误开回答槽），写盘收束同时新开 status+思考 / 思考+散文 / status+散文 时过程 remap（有 status 再追加该行）且回答只换 diff 槽，以免藏直播 +/-（不把写盘收束算进 isLivePrefixClose）；前缀没变或只收束思考/status/散文/无新写盘的工具时新开一或多个工具（可带一条 Awaiting / Question requested 行）只追加过程步并封回答尾（同一 16ms 里 token 尾 + tool_start 可先加长再标 done、complete_call + add_call、只读并行多个 tool_start、规划下一步后同一帧或下一轮 tool_start（规划下一步可先标 done，可夹 think）、think + tool_start、tool_start + approval_needed / user_input_needed、规划下一步后同一帧 tool_start 且立刻 tool_done 也走这条，不发明 Exploring 分组格）、新思考只换旁白（无新写盘的工具收束后同一帧开思考也走这条，不复制 #24850；think 尾 + 首枚 token 可先加长再标 done）、新散文只开回答尾、新 status 只追加过程步（对标 Reconnecting... n/5 / Compacting）、无新写盘的工具收束后同一帧新开 status+思考 / 思考+散文 / status+散文 / status+思考+散文 / 思考+```demo / status+```demo / status+思考+```demo 时过程 remap（有 status 再追加该行；规划下一步后本地/快模型首枚 think / token / ```demo 也走这条，think 后首枚 token 可先把旁白标 done）、`compress` 收口 status 或无新写盘的工具后只追加已完成压缩步（对标 contextCompaction / complete_call；规划下一步后同一帧 compress 可先把 status 标 done，可夹 think）、审批挂上或收束只换工具步与 Awaiting approval 行（Deny 后同一帧 approval_resolved + tool_done error 只把该行与工具收成 error，可再追加 规划下一步或下一工具；Allow / Deny 只收口 Awaiting 后同一帧 compress / Stop / think / 首枚 token / 错误 / ```demo 只 remap（可再夹 compress / Stop；写盘收束或 Reconnecting / 规划下一步后同一帧也走 remap）；Allow / Deny 收口并 tool_done 后同一帧 compress / Stop 只追加压缩步或换 cancelled 步；不复制 #10760 / #24432 compact 卡住 / Stop 失败）、Ask User 挂上只换工具步与 Question requested 行（规划下一步后同一帧 user_input_needed / approval_needed 可改写规划下一步为第一题 header / Awaiting，已在场时 think 后推新 Question requested / Awaiting 只追加该行，可夹规划下一步；作答后同一帧 user_input_resolved + tool_done 只把该行与工具收成 done；作答后同一帧 Stop 只追加 cancelled 问句行，可夹 think；不发明 TUI Questions n/n / 60s 空答，不复制 #10952 Stop 失效）、status 收束只换该行、Stop 把多条 active 收成 cancelled 只换这些步（对标 You stopped after；规划下一步后同一帧 Stop 可先挂上 status / think 再标 cancelled）、错误收口 status 或无新写盘的工具后只开错误回答尾（不进过程）、新 present_inline_demo 或正文 ```demo 只开演示槽（过程不追加；规划下一步后同一帧 present_inline_demo 可先把 status 标 done，过程只追加该行）；演示 HTML / 说明 / 收束只换该槽；命令末行不换过程数组、不发 16ms store。对标 Codex #22860（已画过程不跟每枚 token 闪）。
 * @see shared/ARCH.md
 */
import {
  isAwaitingApprovalText,
  isInlineDemoPaintable,
  liveThinkingText,
  sameRefList
} from './live-display'
import { isLiveStableToolDetail } from './tool-output-display'
import { COMPRESS_TOOL } from './compact-activity'
import { REQUEST_USER_INPUT_TOOL } from './user-input'
import { hasLiveAssistantBody } from './session-runtime'
import type { TurnSegment } from './types'
import {
  buildAnswerParts,
  extractFinalContent,
  hasStreamingDemoFence,
  hasStreamingDemoFenceGrowth,
  processSegments,
  reuseAnswerParts,
  type AnswerPart
} from './turn-segments'
import type { LiveStreamUiSnapshot } from './live-stream-ui'

/** 直播过程区：工具/思考，不含增长中的正文 */
export interface LiveProcessView {
  processForFlow: TurnSegment[]
  thinkText: string
  contentStreaming: boolean
  generatingDemo: boolean
  answerStreaming: boolean
}

/** 时间线切片：不含 thinkText，思考 token 不抬 TurnFlow */
export interface LiveProcessTimeline {
  processForFlow: TurnSegment[]
  contentStreaming: boolean
  generatingDemo: boolean
  answerStreaming: boolean
  hasThought: boolean
}

/** 直播回答槽：闭合块与增长尾分开，已画正文不跟 token 重挂 */
export interface LiveAnswerView {
  parts: AnswerPart[]
  closed: AnswerPart[]
  tail: AnswerPart | null
  show: boolean
  copyable: string
  hasCopyable: boolean
}

/** 操作条只订布尔，避免 copyable 每枚 token 抬按钮 */
export interface LiveAnswerActions {
  show: boolean
  reserved: boolean
}

let answerCache: { snap: LiveStreamUiSnapshot; view: LiveAnswerView } | null = null
let answerGrowHold: {
  view: LiveAnswerView
  segments: readonly TurnSegment[]
  tailPlain: boolean
} | null = null
let processHold: {
  view: LiveProcessView
  identity: string
  segments: readonly TurnSegment[]
  answerTailPlain: boolean
} | null = null

function isLiveAnswerText(segment: TurnSegment): boolean {
  return segment.kind === 'text' && (segment.role === 'final' || segment.status === 'active')
}

function isLiveThinking(segment: TurnSegment): boolean {
  return segment.kind === 'thinking'
}

function isLiveStatus(segment: TurnSegment): boolean {
  return segment.kind === 'status'
}

/** 16ms flush：前缀没变时不必整表 extract / 思考预览 / 找 active tool */
export type LiveStreamDerivationSkip = 'think' | 'status' | 'text' | 'tool'

/** 同一工具只改详情 / 摘要：预览与参数引用没变，不必重拆回答 */
function isLiveToolMetaOnlyChange(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.kind !== 'tool' || next.kind !== 'tool') return false
  if (prev.id !== next.id || prev.status !== next.status) return false
  if (prev.toolName !== next.toolName) return false
  if (
    prev.toolName === 'present_inline_demo' &&
    ((prev.content ?? '') !== (next.content ?? '') || (prev.toolDetail ?? '') !== (next.toolDetail ?? ''))
  ) {
    return false
  }
  return (
    prev.toolArgs === next.toolArgs &&
    prev.fileDiff === next.fileDiff &&
    prev.fileDiffs === next.fileDiffs &&
    prev.editPreview === next.editPreview
  )
}

/** 同一工具收束且没新写盘：就地换该步，不重拆回答（对标 Codex exec_cell complete_call） */
export function isLiveToolSettleChange(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.kind !== 'tool' || next.kind !== 'tool') return false
  if (prev.id !== next.id || prev.toolName !== next.toolName) return false
  if (prev.toolName === 'present_inline_demo') return false
  if (prev.status !== 'active') return false
  if (next.status !== 'done' && next.status !== 'error' && next.status !== 'cancelled') return false
  return (
    prev.toolArgs === next.toolArgs &&
    prev.fileDiff === next.fileDiff &&
    prev.fileDiffs === next.fileDiffs &&
    prev.editPreview === next.editPreview
  )
}

/** 思考 / 桥接 status 只把 active 标成 done，正文没变（tool_start 收束） */
export function isLiveThinkOrStatusClose(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.id !== next.id || prev.kind !== next.kind) return false
  if (prev.kind !== 'thinking' && prev.kind !== 'status') return false
  if (prev.status !== 'active' || next.status !== 'done') return false
  return (prev.content ?? '') === (next.content ?? '')
}

/** 散文只把 active 标成 done，正文没变（tool_start 收束，对标 Codex flush then add_call） */
export function isLiveTextClose(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.id !== next.id || prev.kind !== 'text' || next.kind !== 'text') return false
  if (prev.status !== 'active' || next.status !== 'done') return false
  return (prev.content ?? '') === (next.content ?? '')
}

/** 散文收束：正文可在同一 16ms 里先加长再标 done（token 尾 + tool_start） */
export function isLiveTextGrowClose(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.id !== next.id || prev.kind !== 'text' || next.kind !== 'text') return false
  if (prev.status !== 'active' || next.status !== 'done') return false
  return (next.content ?? '').startsWith(prev.content ?? '')
}

/** Stop 把增长散文标成 cancelled：仍可就地封回答尾（对标 You stopped after） */
function isLiveTextCancelClose(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.id !== next.id || prev.kind !== 'text' || next.kind !== 'text') return false
  if (prev.status !== 'active' || next.status !== 'cancelled') return false
  return (next.content ?? '').startsWith(prev.content ?? '')
}

/** 思考收束：旁白可在同一 16ms 里先加长再标 done（think 尾 + tool_start） */
export function isLiveThinkGrowClose(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.id !== next.id || prev.kind !== 'thinking' || next.kind !== 'thinking') return false
  if (prev.status !== 'active' || next.status !== 'done') return false
  return (next.content ?? '').startsWith(prev.content ?? '')
}

function isLivePrefixClose(prev: TurnSegment, next: TurnSegment): boolean {
  return (
    isLiveThinkOrStatusClose(prev, next) ||
    isLiveThinkGrowClose(prev, next) ||
    isLiveTextClose(prev, next) ||
    isLiveToolSettleChange(prev, next)
  )
}

/** 思考 / 回答 / ```demo 围栏前缀：只认 think/status 收口（旁白可先加长）或无新写盘的工具收束（写盘 +/- 仍重拆回答） */
function isLiveThinkAnswerPrefixClose(prev: TurnSegment, next: TurnSegment): boolean {
  return (
    isLiveThinkOrStatusClose(prev, next) ||
    isLiveThinkGrowClose(prev, next) ||
    isLiveToolSettleChange(prev, next)
  )
}

/** 前缀里被 tool_start 收成 done 的散文；用来就地封回答尾 */
export function findLiveClosedAnswerText(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): TurnSegment | null {
  if (!prev) return null
  const n = Math.min(prev.length, next.length)
  for (let i = 0; i < n; i++) {
    const before = prev[i]
    const after = next[i]
    if (before && after && (isLiveTextGrowClose(before, after) || isLiveTextCancelClose(before, after))) {
      return after
    }
  }
  return null
}

/** 前缀没变或只收束思考/status/散文/无新写盘的工具（正文/思考可在同一 16ms 先加长再标 done）、末尾新开一或多个工具，可带一条 Awaiting / Question requested 行：只追加这些步（对标 Codex exec_cell complete_call + add_call / Awaiting approval；token 尾 + tool_start、只读并行一次 yield 多个 tool_start，不发明 Exploring 分组格） */
function isLiveAddedToolsWithOptionalStatus(
  prevLen: number,
  next: readonly TurnSegment[]
): boolean {
  if (next.length <= prevLen) return false
  let tools = 0
  for (let i = prevLen; i < next.length; i++) {
    const added = next[i]
    if (!added) return false
    if (
      added.kind === 'status' &&
      added.status === 'active' &&
      i === next.length - 1 &&
      tools >= 1
    ) {
      return true
    }
    if (
      added.kind !== 'tool' ||
      added.status !== 'active' ||
      !added.toolName ||
      added.toolName === 'present_inline_demo'
    ) {
      return false
    }
    tools += 1
  }
  return tools >= 1
}

function isLiveAddedSettledTool(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'tool' &&
      (segment.status === 'done' || segment.status === 'error') &&
      segment.toolName &&
      segment.toolName !== 'present_inline_demo'
  )
}

/** 同一帧 tool_start + tool_done：新开的工具已是 done / error，可带一条 规划下一步 */
function isLiveAddedSettledToolsWithOptionalStatus(
  prevLen: number,
  next: readonly TurnSegment[]
): boolean {
  if (next.length <= prevLen) return false
  let tools = 0
  for (let i = prevLen; i < next.length; i++) {
    const added = next[i]
    if (!added) return false
    if (
      added.kind === 'status' &&
      added.status === 'active' &&
      i === next.length - 1 &&
      tools >= 1
    ) {
      return true
    }
    if (!isLiveAddedSettledTool(added)) return false
    tools += 1
  }
  return tools >= 1
}


function isLiveAddedCancelledTool(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'tool' &&
      segment.status === 'cancelled' &&
      segment.toolName &&
      segment.toolName !== 'present_inline_demo'
  )
}

/** 同一帧 tool_start 后立刻 Stop：新开的工具已是 cancelled，可带一条 规划下一步 */
function isLiveAddedCancelledToolsWithOptionalStatus(
  prevLen: number,
  next: readonly TurnSegment[]
): boolean {
  if (next.length <= prevLen) return false
  let tools = 0
  for (let i = prevLen; i < next.length; i++) {
    const added = next[i]
    if (!added) return false
    if (
      added.kind === 'status' &&
      added.status === 'active' &&
      i === next.length - 1 &&
      tools >= 1
    ) {
      return true
    }
    if (!isLiveAddedCancelledTool(added)) return false
    tools += 1
  }
  return tools >= 1
}


function isLiveAddedActiveFollowTool(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'tool' &&
      segment.status === 'active' &&
      segment.toolName &&
      segment.toolName !== 'present_inline_demo'
  )
}

function addedSettledToolsHaveWriteStat(
  prevLen: number,
  next: readonly TurnSegment[]
): boolean {
  for (let i = prevLen; i < next.length; i++) {
    const added = next[i]
    if (!added || added.kind !== 'tool') continue
    if (added.fileDiff || added.fileDiffs || added.editPreview) return true
  }
  return false
}

/** 规划下一步后同一帧 tool_start 且立刻 tool_done：前缀只收口，追加已收束工具（对标 query-loop assertToolAllowed / 快工具 complete_call，不发明 Exploring 分组格） */
export function isLiveSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || !isLiveAddedSettledToolsWithOptionalStatus(prev.length, next)) return false
  return hasLiveToolAppendPrefixClose(prev, next)
}

/** 规划下一步后同一帧 think + tool_start + tool_done：旁白可先标 done，再追加已收束工具 */
export function isLiveThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 规划下一步 + think + 立刻收束的工具：status 可先收口，思考不进过程 */
export function isLiveStatusThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

function isLiveAddedAnswerPair(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'text' &&
      (segment.status === 'active' || segment.status === 'done') &&
      !hasStreamingDemoFence(segment.content ?? '')
  )
}

/** 规划下一步后同一帧首枚 token + tool_start + tool_done：散文可先标 done，再追加已收束工具 */
export function isLiveAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedAnswerPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 写盘收束同时下一工具已在同一帧 complete_call：过程 remap 并追加已收束工具，回答只换 diff 槽 */
export function isLiveWriteStatSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveWriteStatPrefix(prev, next) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length, next)
}

/** 规划下一步后同一帧 think + 首枚 token + tool_start + tool_done */
export function isLiveThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedAnswerPair(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** 规划下一步 + think + 首枚 token + 立刻收束的工具：status 可先收口 */
export function isLiveStatusThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  if (!isLiveAddedAnswerPair(next[prev!.length + 2])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 规划下一步后同一帧 think + ```demo + present_inline_demo：思考不进过程，回答开演示槽 */
export function isLiveThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 3) return false
  const text = next[prev!.length + 1]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 2])
}

/** 写盘收束同时 think + 下一工具已 complete_call：过程 remap，旁白续尾 */
export function isLiveWriteStatThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 写盘收束同时首枚 token + 下一工具已 complete_call：回答开散文尾并换 diff 槽 */
export function isLiveWriteStatAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedAnswerPair(next[prev!.length])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 写盘收束同时 think + 首枚 token + 下一工具已 complete_call：过程 remap，回答开散文尾并换 diff 槽 */
export function isLiveWriteStatThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedAnswerPair(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** 写盘收束同时 think + ```demo + present_inline_demo：过程 remap，旁白续尾，回答开演示槽 */
export function isLiveWriteStatThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) return false
  if (next.length !== prev!.length + 3) return false
  const text = next[prev!.length + 1]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 2])
}

/** 写盘收束同时 ```demo + present_inline_demo：过程 remap，回答开演示槽 */
export function isLiveWriteStatAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  const text = next[prev!.length]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 1])
}

/** 写盘收束同时 规划下一步 + think + 首枚 token + 下一工具已 complete_call */
export function isLiveWriteStatStatusThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  if (!isLiveAddedAnswerPair(next[prev!.length + 2])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 写盘收束同时 规划下一步 + think + 下一工具已 complete_call：过程 remap 并追加 status */
export function isLiveWriteStatStatusThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** 写盘收束同时 规划下一步 + 首枚 token + 下一工具已 complete_call */
export function isLiveWriteStatStatusAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedAnswerPair(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** 写盘收束同时 规划下一步 + think + ```demo + present_inline_demo */
export function isLiveWriteStatStatusThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  if (next.length !== prev!.length + 4) return false
  const text = next[prev!.length + 2]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 3])
}

/** 写盘收束同时 规划下一步 + ```demo + present_inline_demo */
export function isLiveWriteStatStatusAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (next.length !== prev!.length + 3) return false
  const text = next[prev!.length + 1]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 2])
}

/** 正文 ```demo 与 present_inline_demo 同一帧：过程不追加演示步，回答开演示槽（对标 query-loop token + tool_preview） */
export function isLiveAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev) return false
  if (next.length === prev.length + 2) {
    if (!hasLiveToolAppendPrefixClose(prev, next)) return false
    const text = next[prev.length]
    if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
    return isLiveAddedInlineDemo(next[prev.length + 1])
  }
  if (next.length !== prev.length + 1) return false
  if (!isLiveAddedInlineDemo(next[next.length - 1])) return false
  let fenceGrew = false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (
      i === prev.length - 1 &&
      before.kind === 'text' &&
      after.kind === 'text' &&
      before.id === after.id &&
      hasStreamingDemoFence(after.content ?? '')
    ) {
      fenceGrew = true
      continue
    }
    if (isLivePrefixClose(before, after) || isLiveTextGrowClose(before, after)) continue
    return false
  }
  return fenceGrew
}

export function isLiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || !isLiveAddedToolsWithOptionalStatus(prev.length, next)) return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return true
}

function hasLiveToolAppendPrefixClose(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev) return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return true
}

/** 规划下一步后同一帧 tool_start：status 可先被收口，再追加一或多个工具 */
export function isLiveStatusToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 规划下一步已在场时 think 后 tool_start：旁白可先标 done，已画散文也可被收口，再追加工具 */
export function isLiveThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 规划下一步 + think + tool_start 同一帧：status 可仍在，旁白可先标 done */
export function isLiveStatusThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
}

/** 前缀没变或只收束思考/status/散文/无新写盘的工具、末尾新开 status：只追加过程步（对标 Codex Reconnecting... n/5 / Compacting） */
export function isLiveStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  const added = next[next.length - 1]
  if (!added || added.kind !== 'status' || added.status !== 'active') return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLivePrefixClose(before, after)) return false
  }
  return true
}

/** 前缀没变或只收束思考/status/无新写盘的工具、末尾新开思考：只换旁白（对标 Codex Thinking cell / complete_call，不复制 #24850） */
export function isLiveThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  const added = next[next.length - 1]
  if (!added || added.kind !== 'thinking' || added.status !== 'active') return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLiveThinkAnswerPrefixClose(before, after)) return false
  }
  return true
}

/** 前缀没变或只收束思考/status/无新写盘的工具、末尾新开散文：只开回答尾（对标 Codex 工具后首枚 token / complete_call） */
export function isLiveAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  const added = next[next.length - 1]
  if (!added || !isLiveAnswerText(added) || added.status === 'done') return false
  if (hasStreamingDemoFence(added.content ?? '')) return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLiveThinkAnswerPrefixClose(before, after)) return false
  }
  return true
}

/** 前缀没变或只收束思考/status/无新写盘的工具、末尾新开带 ```demo 的散文：过程不追加、回答只开演示槽 */
export function isLiveDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  const added = next[next.length - 1]
  if (!added || !isLiveAnswerText(added) || added.status === 'done') return false
  if (!hasStreamingDemoFence(added.content ?? '')) return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLiveThinkAnswerPrefixClose(before, after)) return false
  }
  return true
}

/** 前缀没变或只收束思考/status/无新写盘的工具、末尾新开已完成 compress：只追加过程步（对标 Codex contextCompaction / complete_call，写盘 +/- 仍走 write-stat） */
export function isLiveCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  const added = next[next.length - 1]
  if (!added || added.kind !== 'tool' || added.toolName !== COMPRESS_TOOL) return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLiveThinkAnswerPrefixClose(before, after)) return false
  }
  return true
}

/** 同一段散文刚出现或加长 ```demo：只换该槽，不重拆过程 / 全文 buildAnswerParts */
function sameLiveToolCore(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.kind !== 'tool' || next.kind !== 'tool') return false
  if (prev.id !== next.id || prev.toolName !== next.toolName || prev.status !== next.status) {
    return false
  }
  return (
    prev.toolArgs === next.toolArgs &&
    prev.fileDiff === next.fileDiff &&
    prev.fileDiffs === next.fileDiffs &&
    prev.editPreview === next.editPreview
  )
}

function isLiveApprovalAttach(prev: TurnSegment, next: TurnSegment): boolean {
  return sameLiveToolCore(prev, next) && !prev.approval && Boolean(next.approval)
}

function isLiveApprovalDetach(prev: TurnSegment, next: TurnSegment): boolean {
  return sameLiveToolCore(prev, next) && Boolean(prev.approval) && !next.approval
}

function isLiveAwaitingStatusRetarget(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.kind !== 'status' || next.kind !== 'status' || prev.id !== next.id) return false
  if (prev.status !== 'active' || next.status !== 'active') return false
  return isAwaitingApprovalText(next.content ?? '')
}

function isLiveAwaitingStatusResolve(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.kind !== 'status' || next.kind !== 'status' || prev.id !== next.id) return false
  if (prev.status !== 'active' || next.status !== 'done') return false
  return isAwaitingApprovalText(prev.content ?? '')
}

/** 工具挂上 approval，并新开或改写 Awaiting approval 行：只换这两步（对标 Codex Awaiting approval） */
export function isLiveApprovalNeededChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || (next.length !== prev.length && next.length !== prev.length + 1)) return false
  let attached = 0
  let statusRetarget = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveApprovalAttach(before, after)) {
      attached += 1
      continue
    }
    if (next.length === prev.length && isLiveAwaitingStatusRetarget(before, after)) {
      statusRetarget += 1
      continue
    }
    return false
  }
  if (attached !== 1) {
    return attached === 0 && next.length === prev.length && statusRetarget === 1
  }
  if (next.length === prev.length + 1) {
    const added = next[next.length - 1]
    return Boolean(
      added &&
        added.kind === 'status' &&
        added.status === 'active' &&
        isAwaitingApprovalText(added.content ?? '')
    )
  }
  return statusRetarget === 1
}

function isLiveAddedAwaitingStatus(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'status' &&
      (segment.status === 'active' || segment.status === 'done') &&
      isAwaitingApprovalText(segment.content ?? '')
  )
}

function isLiveAddedCancelledAwaitingStatus(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'status' &&
      segment.status === 'cancelled' &&
      isAwaitingApprovalText(segment.content ?? '')
  )
}

function isLiveAddedApprovalTool(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && segment.kind === 'tool' && segment.status === 'active' && segment.approval)
}

function isLiveAddedCancelledApprovalTool(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && segment.kind === 'tool' && segment.status === 'cancelled')
}

function hasLiveApprovalNeededAppendHead(next: readonly TurnSegment[], start: number): boolean {
  return isLiveAddedApprovalTool(next[start]) && isLiveAddedAwaitingStatus(next[start + 1])
}

function hasLiveApprovalNeededCancelledHead(next: readonly TurnSegment[], start: number): boolean {
  return isLiveAddedCancelledApprovalTool(next[start]) && isLiveAddedCancelledAwaitingStatus(next[start + 1])
}

/** Stop 后审批工具标 cancelled，Awaiting 行已是 done（不跟 Ask 一起 cancelled） */
function hasLiveApprovalNeededStoppedHead(next: readonly TurnSegment[], start: number): boolean {
  return isLiveAddedCancelledApprovalTool(next[start]) && isLiveAddedAwaitingStatus(next[start + 1])
}

/** 已在场工具挂上或摘掉 approval，并新开 Awaiting 行（可先标 done） */
function hasLiveApprovalNeededPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length <= prev.length) return false
  let holds = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (sameLiveToolCore(before, after)) {
      holds += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return holds === 1 && isLiveAddedAwaitingStatus(next[prev.length])
}

/** Awaiting approval 挂上后同一帧 think：过程追加问句行，旁白续尾 */
export function isLiveApprovalNeededThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalNeededPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return isLiveAddedThinkPair(next[prev!.length + 1])
}

/** Awaiting approval 挂上后同一帧首枚 token：过程追加该行，回答开散文尾 */
export function isLiveApprovalNeededAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalNeededPrefix(prev, next) || next.length !== prev!.length + 2) return false
  const text = next[prev!.length + 1]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text))
}

/** Awaiting approval 挂上后同一帧 context_compress：过程追加该行与压缩步 */
export function isLiveApprovalNeededCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalNeededPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return isLiveAddedCompress(next[prev!.length + 1])
}

/** Awaiting approval 挂上后同一帧 think + compress */
export function isLiveApprovalNeededThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalNeededPrefix(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return isLiveAddedCompress(next[prev!.length + 2])
}

/** Awaiting approval 挂上后同一帧错误：过程追加该行，错误正文只进回答 */
export function isLiveApprovalNeededErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalNeededPrefix(prev, next) || next.length !== prev!.length + 2) return false
  const text = next[prev!.length + 1]
  return Boolean(text && isLiveErrorAnswer(text))
}

/** Awaiting approval 挂上后同一帧 ```demo + present_inline_demo */
export function isLiveApprovalNeededAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalNeededPrefix(prev, next) || next.length !== prev!.length + 3) return false
  const text = next[prev!.length + 1]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 2])
}

/** 写盘收束同时新开工具并立刻 Awaiting + compress */
export function isLiveWriteStatApprovalNeededCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedCompress(next[prev!.length + 2])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Stop */
export function isLiveWriteStatApprovalNeededCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return hasLiveApprovalNeededCancelledHead(next, prev!.length)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + compress */
export function isLiveStatusApprovalNeededCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Stop */
export function isLiveStatusApprovalNeededCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 3) return false
  return hasLiveApprovalNeededCancelledHead(next, prev!.length + 1)
}


/** Awaiting approval 挂上后同一帧 首枚 token + Ask User */
export function isLiveApprovalNeededAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User */
export function isLiveApprovalNeededThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + Stop */
export function isLiveApprovalNeededAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + Stop */
export function isLiveApprovalNeededThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + compress */
export function isLiveApprovalNeededAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + compress */
export function isLiveApprovalNeededThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一工具仍 active */
export function isLiveApprovalNeededAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 2)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User */
export function isLiveApprovalNeededErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User */
export function isLiveApprovalNeededThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User */
export function isLiveStatusApprovalNeededAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User */
export function isLiveStatusApprovalNeededThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + Stop */
export function isLiveStatusApprovalNeededAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + Stop */
export function isLiveStatusApprovalNeededThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + compress */
export function isLiveStatusApprovalNeededAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User */
export function isLiveStatusApprovalNeededErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User */
export function isLiveStatusApprovalNeededThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User */
export function isLiveWriteStatApprovalNeededAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User */
export function isLiveWriteStatApprovalNeededThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + Stop */
export function isLiveWriteStatApprovalNeededAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + compress */
export function isLiveWriteStatApprovalNeededAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 2)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User */
export function isLiveWriteStatApprovalNeededErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User */
export function isLiveWriteStatApprovalNeededThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User */
export function isLiveWriteStatStatusApprovalNeededAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalNeededErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededHead(next, prev!.length + 5)
}


/** Awaiting approval 挂上后同一帧 ```demo + Ask User */
export function isLiveApprovalNeededAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User */
export function isLiveApprovalNeededThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + Stop */
export function isLiveApprovalNeededAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + Stop */
export function isLiveApprovalNeededThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + compress */
export function isLiveApprovalNeededAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User */
export function isLiveApprovalNeededAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User */
export function isLiveApprovalNeededErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User */
export function isLiveStatusApprovalNeededAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + Stop */
export function isLiveStatusApprovalNeededAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + Stop */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && isLiveAddedError(next[prev!.length + 6]) && hasLiveAskNeededHead(next, prev!.length + 7)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && isLiveAddedDemoFencePair(next, prev!.length + 5) && hasLiveAskNeededHead(next, prev!.length + 7)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User */
export function isLiveWriteStatApprovalNeededAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalNeededAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && isLiveAddedError(next[prev!.length + 6]) && hasLiveAskNeededHead(next, prev!.length + 7)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && isLiveAddedDemoFencePair(next, prev!.length + 5) && hasLiveAskNeededHead(next, prev!.length + 7)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}


/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  const text = next[prev!.length]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  const text = next[prev!.length]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User */
export function isLiveApprovalResolvedErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededHead(next, prev!.length + 3)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededHead(next, prev!.length + 4)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededResolvedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 ```demo + 错误 + 再开 Ask User */
export function isLiveAskResolvedAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededHead(next, prev!.length + 3)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** Ask User 作答后同一帧 错误 + ```demo + 再开 Ask User */
export function isLiveAskResolvedErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededHead(next, prev!.length + 3)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + 再开 Ask User */
export function isLiveAskResolvedThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededHead(next, prev!.length + 4)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** Ask User 作答后同一帧 ```demo + 再开 Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 ```demo + 再开 Ask User + 下一工具仍 active */
export function isLiveAskResolvedAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 think + ```demo + 再开 Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Ask User 作答后同一帧 再开 Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Ask User 作答后同一帧 再开 Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    hasLiveAskNeededResolvedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 首枚 token + 再开 Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2 + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2 + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2 + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 2, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 2, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2 + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2 + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2 + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 2, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 2, next)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 1 + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 1 + 3) && isLiveAddedToolsWithOptionalStatus(prev!.length + 1 + 5, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 1 + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1 + 4, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1 + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 1 + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1 + 5, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 1 + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 1 + 4, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 1 + 2, next)
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 2, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2 + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2 + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2 + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 2, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3 + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3 + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3 + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 4, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3 + 2, next)
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具仍 active */
export function isLiveAskResolvedThinkAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 2 + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 2 + 3) && isLiveAddedToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedThinkAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2 + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2 + 5, next)
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 2 + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 4, next)
}

/** Ask User 作答后同一帧 Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 2 + 2, next)
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + compress */
export function isLiveApprovalNeededErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalNeededErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + Stop */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + Stop */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 7)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 7)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedAnswerAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + compress */
export function isLiveApprovalResolvedErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalResolvedErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedAnswerAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + Stop */
export function isLiveAskResolvedThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedCompress(next[prev!.length + 8])
}
/** Awaiting approval 挂上后同一帧 Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededResolvedHead(next, prev!.length) && isLiveAddedActiveFollowTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Ask User 作答后同一帧 Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    hasLiveAskNeededResolvedHead(next, prev!.length) && isLiveAddedActiveFollowTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}
/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]))
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7]))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8]))
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]))
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7]))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8]))
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]))
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next))
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next))
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5]))
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]))
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next))
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next))
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5]))
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]))
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]))
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next))
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next))
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next))
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next))
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5]))
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6]))
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8]))
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]))
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7]))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next))
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8]))
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveAskResolvedThinkAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]))
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]))
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedThinkAnswerAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next))
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedThinkAnswerAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next))
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next))
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedThinkAnswerAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5]))
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7]))
}
/** Awaiting approval 挂上后同一帧 错误 + Ask User + Stop */
export function isLiveApprovalNeededErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + compress */
export function isLiveApprovalNeededErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + Stop */
export function isLiveApprovalNeededThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + compress */
export function isLiveApprovalNeededThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalNeededAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + compress */
export function isLiveApprovalNeededAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + compress */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + Stop */
export function isLiveStatusApprovalNeededErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + compress */
export function isLiveStatusApprovalNeededErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + Stop */
export function isLiveStatusApprovalNeededThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + compress */
export function isLiveStatusApprovalNeededThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + Stop */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + compress */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + Stop */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 7)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalNeededErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalNeededErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalNeededThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 6)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 7)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + compress */
export function isLiveApprovalResolvedThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + compress */
export function isLiveApprovalResolvedAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + compress */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedThinkAnswerAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5]))
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6]))
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedThinkAnswerAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5]))
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7]))
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedThinkAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}
/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 3])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具仍 active */
export function isLiveAskResolvedErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedThinkErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具仍 active */
export function isLiveAskResolvedThinkErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}
/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalNeededErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalNeededErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalNeededErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalAllowedSettleErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalDeniedErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalResolvedErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalResolvedErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedThinkAnswerDemoErrorAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveAskResolvedAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active */
export function isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}
/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalNeededThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalNeededThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalDeniedThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 3, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedActiveFollowTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedThinkErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedThinkErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedThinkErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedThinkErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedActiveFollowTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}
/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalDeniedAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}
/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 6])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  return isLiveAddedActiveFollowTool(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedActiveFollowTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveAskResolvedErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedThinkErrorAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active */
export function isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedActiveFollowTool(next[prev!.length + 8])
}
/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalDeniedErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 5, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedActiveFollowTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop */
export function isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}
/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededAppendHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 7, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedActiveFollowTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  return isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 9, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedActiveFollowTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop */
export function isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 6, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedCancelledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedActiveFollowTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededResolvedHead(next, prev!.length + 6) && isLiveAddedActiveFollowTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}
/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 9, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedError(next[prev!.length + 5])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedError(next[prev!.length + 6])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 7)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5]) && hasLiveAskNeededStoppedHead(next, prev!.length + 6) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkErrorAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerAskToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 1])) return false
  return isLiveAddedCompress(next[prev!.length + 2])
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 1])) return false
  return isLiveAddedCompress(next[prev!.length + 2])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedCancelledTool(next[prev!.length + 1]) && isLiveAddedCompress(next[prev!.length + 2])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 ```demo + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 think + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 错误 + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedCancelledTool(next[prev!.length + 1]) && isLiveAddedCompress(next[prev!.length + 2])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
}
/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkErrorAskTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}
/** Awaiting approval 挂上后同一帧 think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + ```demo + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + 错误 + 下一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 ```demo + 下一工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerDemoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 5])
}
/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}
/** Awaiting approval 挂上后同一帧 think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Allow 收口并 tool_done 后同一帧 think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 ```demo + 下一两工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerDemoTwoToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    hasLiveAskNeededStoppedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 think + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}
/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 12) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 11])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 12) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 11])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 10) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 12) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 11])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 12) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)) return false
  return isLiveAddedCompress(next[prev!.length + 11])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3]) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 9 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 11) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next) && isLiveAddedCompress(next[prev!.length + 10])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一三工具已 complete_call + Stop + compress */
export function isLiveAskResolvedThinkErrorAskThreeToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}
/** Awaiting approval 挂上后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** Awaiting approval 挂上后同一帧 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** Awaiting approval 挂上后同一帧 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Awaiting approval 挂上后同一帧 think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Awaiting + think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Awaiting + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Awaiting + think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Awaiting + think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!hasLiveApprovalNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Allow 收口并 tool_done 后同一帧 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 1])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalAllowedSettleThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny 收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Deny 收口并 tool_done 后同一帧 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 1)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 2)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** Deny 收口并 tool_done 后同一帧 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedError(next[prev!.length])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 1])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny 收口并 tool_done 后同一帧 think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalDeniedThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  if (!isLiveAddedError(next[prev!.length + 1])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return Boolean(isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedAnswerPair(next[prev!.length + 2]!) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 9) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 7) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 8) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedCancelledTool(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 3)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedError(next[prev!.length + 2])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  return isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4]!) && !isLiveErrorAnswer(next[prev!.length + 4]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 11) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 6)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 8])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 9])) return false
  return isLiveAddedCompress(next[prev!.length + 10])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 4)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!hasLiveAskNeededStoppedHead(next, prev!.length + 5)) return false
  if (!isLiveAddedSettledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  return isLiveAddedCompress(next[prev!.length + 8])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5)) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 7])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 8])) return false
  return isLiveAddedCompress(next[prev!.length + 9])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedError(next[prev!.length + 3])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  return isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedError(next[prev!.length + 4])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 5])) return false
  if (!isLiveAddedCancelledTool(next[prev!.length + 6])) return false
  return isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    Boolean(isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1]!) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3]!) && !isLiveErrorAnswer(next[prev!.length + 3]!)) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 think + ```demo + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 8 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 10) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && hasLiveAskNeededStoppedHead(next, prev!.length + 5) && isLiveAddedSettledTool(next[prev!.length + 7]) && isLiveAddedCancelledTool(next[prev!.length + 8]) && isLiveAddedCompress(next[prev!.length + 9])
}

/** Ask User 作答后同一帧 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && hasLiveAskNeededStoppedHead(next, prev!.length + 1) && isLiveAddedSettledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededStoppedHead(next, prev!.length + 3) && isLiveAddedSettledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + Ask User + 下一工具已 complete_call + 再一工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededStoppedHead(next, prev!.length + 2) && isLiveAddedSettledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededStoppedHead(next, prev!.length + 4) && isLiveAddedSettledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCompress(next[prev!.length + 7])
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) && isLiveAddedCancelledTool(next[prev!.length + 6]) && isLiveAddedCancelledTool(next[prev!.length + 7]) && isLiveAddedCompress(next[prev!.length + 8])
}

/** Ask User 作答后同一帧 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedError(next[prev!.length]) && isLiveAddedCancelledTool(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 think + 错误 + 下一两工具仍 active + Stop + compress */
export function isLiveAskResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedError(next[prev!.length + 1]) && isLiveAddedCancelledTool(next[prev!.length + 2]) && isLiveAddedCancelledTool(next[prev!.length + 3]) && isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3]) && isLiveAddedCancelledTool(next[prev!.length + 4]) && isLiveAddedCancelledTool(next[prev!.length + 5]) && isLiveAddedCompress(next[prev!.length + 6])
}
/** 写盘收束同时新开工具并立刻 Allow/Deny + compress */
export function isLiveWriteStatApprovalResolvedCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedCompress(next[prev!.length + 2])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Stop */
export function isLiveWriteStatApprovalResolvedCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  return isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + compress */
export function isLiveStatusApprovalResolvedCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Stop */
export function isLiveStatusApprovalResolvedCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 3) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  return isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + compress */
export function isLiveWriteStatApprovalResolvedThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + compress */
export function isLiveStatusApprovalResolvedThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedCompress(next[prev!.length + 4])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token */
export function isLiveWriteStatApprovalResolvedAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text))
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 */
export function isLiveWriteStatApprovalResolvedErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveErrorAnswer(text))
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo */
export function isLiveWriteStatApprovalResolvedAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + Stop */
export function isLiveWriteStatApprovalResolvedThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  return isLiveAddedCancelledThink(next[prev!.length + 2])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Stop */
export function isLiveWriteStatApprovalResolvedAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  return isLiveAddedCancelledAnswer(next[prev!.length + 2])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Stop */
export function isLiveWriteStatApprovalResolvedAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  return isLiveAddedCancelledDemoFencePair(next, prev!.length + 2)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedCancelledAnswer(next[prev!.length + 3])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 3)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedCancelledThink(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  const tool = next[prev!.length]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 1])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) && isLiveAddedCancelledDemoFencePair(next, prev!.length + 4)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[prev!.length + 4])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}


/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User */
export function isLiveWriteStatApprovalResolvedErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User */
export function isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedError(next[prev!.length + 4]) &&
    hasLiveAskNeededHead(next, prev!.length + 5)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededHead(next, prev!.length + 5)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}


/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) &&
    isLiveAddedError(next[prev!.length + 4]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalResolvedErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedError(next[prev!.length + 4]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}


/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User */
export function isLiveWriteStatApprovalResolvedAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具 */
export function isLiveWriteStatApprovalResolvedAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 2)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具 */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}


/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2])) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + Stop */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3])) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + compress */
export function isLiveWriteStatApprovalResolvedAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 2)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatApprovalResolvedAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + 下一工具 */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next)) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token */
export function isLiveStatusApprovalResolvedAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + Stop */
export function isLiveStatusApprovalResolvedThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return isLiveAddedCancelledThink(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Stop */
export function isLiveStatusApprovalResolvedAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return isLiveAddedCancelledAnswer(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedCancelledAnswer(next[prev!.length + 4])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Stop */
export function isLiveStatusApprovalResolvedAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return isLiveAddedCancelledDemoFencePair(next, prev!.length + 3)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Stop */
export function isLiveStatusApprovalResolvedThinkErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedCancelledThink(next[prev!.length + 3]) && isLiveAddedError(next[prev!.length + 4])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 4)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token */
export function isLiveWriteStatApprovalResolvedThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text))
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 */
export function isLiveWriteStatApprovalResolvedThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveErrorAnswer(next[prev!.length + 3]!)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 首枚 token + compress */
export function isLiveWriteStatApprovalResolvedAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  const text = next[prev!.length + 2]
  return (
    Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) &&
    isLiveAddedCompress(next[prev!.length + 3])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 错误 + compress */
export function isLiveWriteStatApprovalResolvedErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveErrorAnswer(next[prev!.length + 2]!) && isLiveAddedCompress(next[prev!.length + 3])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + ```demo + compress */
export function isLiveWriteStatApprovalResolvedAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedCompress(next[prev!.length + 4])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 首枚 token + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return (
    Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 错误 + compress */
export function isLiveWriteStatApprovalResolvedThinkErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveErrorAnswer(next[prev!.length + 3]!) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + ```demo + compress */
export function isLiveWriteStatApprovalResolvedThinkAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + 下一工具 */
export function isLiveWriteStatApprovalResolvedToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedResolvedApprovalHead(next, prev!.length)) {
    return false
  }
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 下一工具 */
export function isLiveWriteStatApprovalResolvedThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedResolvedApprovalHead(next, prev!.length)) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + think + 下一工具已 complete_call */
export function isLiveWriteStatApprovalResolvedThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedResolvedApprovalHead(next, prev!.length)) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 写盘收束同时新开工具并立刻 Allow/Deny + Ask User */
export function isLiveWriteStatApprovalResolvedAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length)) return false
  return hasLiveAskNeededHead(next, prev!.length + 2)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token */
export function isLiveStatusApprovalResolvedThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text))
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo */
export function isLiveStatusApprovalResolvedAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 */
export function isLiveStatusApprovalResolvedErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveErrorAnswer(next[prev!.length + 3]!)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + compress */
export function isLiveStatusApprovalResolvedAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return (
    Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + compress */
export function isLiveStatusApprovalResolvedThinkAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return (
    Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 */
export function isLiveStatusApprovalResolvedThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveErrorAnswer(next[prev!.length + 4]!)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + compress */
export function isLiveStatusApprovalResolvedThinkErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) &&
    isLiveErrorAnswer(next[prev!.length + 4]!) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + compress */
export function isLiveStatusApprovalResolvedErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveErrorAnswer(next[prev!.length + 3]!) && isLiveAddedCompress(next[prev!.length + 4])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + compress */
export function isLiveStatusApprovalResolvedAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 下一工具 */
export function isLiveStatusApprovalResolvedToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 下一工具 */
export function isLiveStatusApprovalResolvedThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User */
export function isLiveStatusApprovalResolvedAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return hasLiveAskNeededHead(next, prev!.length + 3)
}


/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User */
export function isLiveStatusApprovalResolvedAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User */
export function isLiveStatusApprovalResolvedErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User */
export function isLiveStatusApprovalResolvedThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    hasLiveAskNeededHead(next, prev!.length + 7)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 5) &&
    hasLiveAskNeededHead(next, prev!.length + 7)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededHead(next, prev!.length + 6)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededHead(next, prev!.length + 6)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + Stop */
export function isLiveStatusApprovalResolvedAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + Stop */
export function isLiveStatusApprovalResolvedErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + Stop */
export function isLiveStatusApprovalResolvedThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 7)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + Stop */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + Stop */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + Stop */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 7)
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + compress */
export function isLiveStatusApprovalResolvedAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + Ask User + compress */
export function isLiveStatusApprovalResolvedErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + Ask User + compress */
export function isLiveStatusApprovalResolvedThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + compress */
export function isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 7) &&
    isLiveAddedCompress(next[prev!.length + 9])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + 错误 + Ask User + compress */
export function isLiveStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 错误 + ```demo + Ask User + compress */
export function isLiveStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + compress */
export function isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 5) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 7) &&
    isLiveAddedCompress(next[prev!.length + 9])
  )
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User */
export function isLiveStatusApprovalResolvedAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User */
export function isLiveStatusApprovalResolvedThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + ```demo + Ask User + 下一工具 */
export function isLiveStatusApprovalResolvedAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}


/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + Stop */
export function isLiveStatusApprovalResolvedAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3])) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + Stop */
export function isLiveStatusApprovalResolvedThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 4])) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + compress */
export function isLiveStatusApprovalResolvedAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + compress */
export function isLiveStatusApprovalResolvedThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveStatusApprovalResolvedAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveStatusApprovalResolvedThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 规划下一步 / Reconnecting 后同一帧新开工具并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具 */
export function isLiveStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 下一工具 */
export function isLiveWriteStatStatusApprovalResolvedToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User */
export function isLiveWriteStatStatusApprovalResolvedAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return hasLiveAskNeededHead(next, prev!.length + 3)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedCancelledAnswer(next[prev!.length + 4])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return isLiveAddedCancelledDemoFencePair(next, prev!.length + 3)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return isLiveAddedError(next[prev!.length + 3])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedCancelledThink(next[prev!.length + 3]) && isLiveAddedError(next[prev!.length + 4])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 4)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) &&
    isLiveErrorAnswer(next[prev!.length + 4]!) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveErrorAnswer(next[prev!.length + 3]!) && isLiveAddedCompress(next[prev!.length + 4])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + Ask User */
export function isLiveWriteStatStatusApprovalResolvedThinkAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 下一工具 */
export function isLiveWriteStatStatusApprovalResolvedThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return isLiveAddedCancelledAnswer(next[prev!.length + 3])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return (
    Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && isLiveAddedError(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 4) && isLiveAddedError(next[prev!.length + 6])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && isLiveAddedDemoFencePair(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) && isLiveAddedCancelledDemoFencePair(next, prev!.length + 5)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && isLiveAddedDemoFencePair(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 4) && hasLiveAskNeededHead(next, prev!.length + 6)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 下一工具 */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 3) && isLiveAddedError(next[prev!.length + 5])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) && isLiveAddedCancelledDemoFencePair(next, prev!.length + 4)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 3) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededHead(next, prev!.length + 5)
}


/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    hasLiveAskNeededHead(next, prev!.length + 7)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 5) &&
    hasLiveAskNeededHead(next, prev!.length + 7)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}


/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return isLiveAddedError(next[prev!.length + 3]) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return isLiveAddedError(next[prev!.length + 4]) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 7)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 5) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 7)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 4) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 7) &&
    isLiveAddedCompress(next[prev!.length + 9])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + 错误 + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}


/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 9) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return (
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 错误 + ```demo + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 10) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 5) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 7) &&
    isLiveAddedCompress(next[prev!.length + 9])
  )
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + ```demo + Ask User + 下一工具 */
export function isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 3)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + Ask User + 下一工具 */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  if (!isLiveAddedDemoFencePair(next, prev!.length + 4)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 6)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 8, next)
}


/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3])) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + Stop */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 4])) && hasLiveAskNeededCancelledHead(next, prev!.length + 5)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 7) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 5) && isLiveAddedCompress(next[prev!.length + 7])
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + 首枚 token + Ask User + 下一工具仍 active */
export function isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 4)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 6, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedResolvedApprovalHead(next, prev!.length + 1)) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  const text = next[prev!.length + 4]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 5)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 7, next)
}

/** 写盘收束同时新开 Reconnecting / 规划下一步并立刻 Allow/Deny + think + ```demo + 错误 + Stop + compress */
export function isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 8) return false
  const tool = next[prev!.length + 1]
  if (!tool || tool.kind !== 'tool' || tool.status !== 'cancelled') return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length + 2])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 3])) return false
  return (
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedError(next[prev!.length + 6]) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** 审批收束：Awaiting 行标 done，可选摘掉工具 approval，不重拆回答 */
export function isLiveApprovalResolvedChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let detached = 0
  let resolved = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveApprovalDetach(before, after)) {
      detached += 1
      continue
    }
    if (isLiveAwaitingStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    return false
  }
  return resolved === 1 && detached <= 1
}

/** Allow / Deny 只收口 Awaiting 行（工具仍在跑）后同一帧 think：过程 remap，旁白续尾 */
export function isLiveApprovalResolvedThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 1) {
    return isLiveAddedThinkPair(next[prev!.length])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 2) {
    return isLiveAddedThinkPair(next[prev!.length + 1])
  }
  return false
}

function isLiveAddedResolvedApprovalStatus(segment: TurnSegment | undefined): boolean {
  const text = segment?.content ?? ''
  return Boolean(
    segment &&
      segment.kind === 'status' &&
      segment.status === 'done' &&
      (text === '已确认，继续执行' || text === '已拒绝该操作')
  )
}

function hasLiveApprovalResolvedPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let detached = 0
  let resolved = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveApprovalDetach(before, after)) {
      detached += 1
      continue
    }
    if (isLiveAwaitingStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    return false
  }
  return resolved === 1 && detached <= 1
}

/** 已在场 Allow/Deny 收口，且同一帧 tool_start 可先把散文 / 旁白标 done */
function hasLiveApprovalResolvedPrefixClose(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let detached = 0
  let resolved = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveApprovalDetach(before, after)) {
      detached += 1
      continue
    }
    if (isLiveAwaitingStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return resolved === 1 && detached <= 1
}

function hasLiveApprovalResolvedAppendPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length <= prev.length) return false
  let holds = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (sameLiveToolCore(before, after)) {
      holds += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return holds === 1 && isLiveAddedResolvedApprovalStatus(next[prev.length])
}

function isLiveAddedResolvedApprovalHead(next: readonly TurnSegment[], start: number): boolean {
  const tool = next[start]
  return Boolean(tool && tool.kind === 'tool') && isLiveAddedResolvedApprovalStatus(next[start + 1])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 context_compress：过程 remap 并追加压缩步（对标 query-loop 放行后立即 compact；不复制 #24432 compact 卡住） */
export function isLiveApprovalResolvedCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 1) {
    return isLiveAddedCompress(next[prev!.length])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 2) {
    return isLiveAddedCompress(next[prev!.length + 1])
  }
  return false
}

/** Allow / Deny 挂上并立刻收口后同一帧 Stop：过程追加已确认/已拒绝行，已在场 active 标 cancelled（对标 You stopped after；不复制 Stop 失败卡住） */
export function isLiveApprovalResolvedCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedResolvedApprovalStatus(next[prev!.length])
}

/** Allow / Deny 已在场收口后同一帧 Stop：Awaiting 行标 done，active 标 cancelled */
export function isLiveApprovalResolvedCancelChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let cancelled = 0
  let resolved = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) {
      cancelled += 1
      continue
    }
    return false
  }
  return resolved === 1 && cancelled >= 1
}

/** Allow / Deny 只收口 Awaiting 行后同一帧首枚 token：过程 remap，回答开散文尾 */
export function isLiveApprovalResolvedAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 1) {
    return isLiveAddedAnswerPair(next[prev!.length])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 2) {
    return isLiveAddedAnswerPair(next[prev!.length + 1])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧错误：过程 remap，错误正文只进回答 */
export function isLiveApprovalResolvedErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 1) {
    const text = next[prev!.length]
    return Boolean(text && isLiveErrorAnswer(text))
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 2) {
    const text = next[prev!.length + 1]
    return Boolean(text && isLiveErrorAnswer(text))
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + present_inline_demo */
export function isLiveApprovalResolvedAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 2) {
    const text = next[prev!.length]
    if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
    return isLiveAddedInlineDemo(next[prev!.length + 1])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 3) {
    const text = next[prev!.length + 1]
    if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
    return isLiveAddedInlineDemo(next[prev!.length + 2])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + compress */
export function isLiveApprovalResolvedThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 2) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedCompress(next[prev!.length + 1])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedCompress(next[prev!.length + 2])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧首枚 token + compress */
export function isLiveApprovalResolvedAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 2) {
    return isLiveAddedAnswerPair(next[prev!.length]) && isLiveAddedCompress(next[prev!.length + 1])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedAnswerPair(next[prev!.length + 1]) && isLiveAddedCompress(next[prev!.length + 2])
  }
  return false
}

function hasLiveApprovalResolvedCancelPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let cancelled = 0
  let resolved = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) {
      cancelled += 1
      continue
    }
    return false
  }
  return resolved === 1 && cancelled >= 1
}

/** Allow / Deny 收口后同一帧 think + Stop：旁白可先挂上再标 cancelled，思考不进过程 */
export function isLiveApprovalResolvedThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 1 &&
    isLiveAddedCancelledThink(next[prev!.length])
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 2) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return isLiveAddedCancelledThink(next[prev!.length + 1])
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token */
export function isLiveApprovalResolvedThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 2) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!))
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 3) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!))
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 */
export function isLiveApprovalResolvedThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 2) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveErrorAnswer(next[prev!.length + 1]!)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveErrorAnswer(next[prev!.length + 2]!)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo */
export function isLiveApprovalResolvedThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + compress */
export function isLiveApprovalResolvedThinkAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 3) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) &&
      isLiveAddedCompress(next[prev!.length + 2])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) &&
      isLiveAddedCompress(next[prev!.length + 3])
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + compress */
export function isLiveApprovalResolvedAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedCompress(next[prev!.length + 2])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedCompress(next[prev!.length + 3])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User */
export function isLiveApprovalResolvedAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 2) {
    return hasLiveAskNeededHead(next, prev!.length)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 3) {
    return hasLiveAskNeededHead(next, prev!.length + 1)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧下一工具 */
export function isLiveApprovalResolvedToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next)) {
    return isLiveAddedToolsWithOptionalStatus(prev!.length, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next)) {
    return isLiveAddedToolsWithOptionalStatus(prev!.length + 1, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧下一工具已 complete_call */
export function isLiveApprovalResolvedSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next)) {
    return isLiveAddedSettledToolsWithOptionalStatus(prev!.length, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next)) {
    return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 下一工具已 complete_call */
export function isLiveApprovalResolvedThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && isLiveAddedThinkPair(next[prev!.length])) {
    return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && isLiveAddedThinkPair(next[prev!.length + 1])) {
    return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 下一工具 */
export function isLiveApprovalResolvedThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && isLiveAddedThinkPair(next[prev!.length])) {
    return isLiveAddedToolsWithOptionalStatus(prev!.length + 1, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && isLiveAddedThinkPair(next[prev!.length + 1])) {
    return isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + compress */
export function isLiveApprovalResolvedThinkErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 3) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveErrorAnswer(next[prev!.length + 1]!) &&
      isLiveAddedCompress(next[prev!.length + 2])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveErrorAnswer(next[prev!.length + 2]!) &&
      isLiveAddedCompress(next[prev!.length + 3])
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + compress */
export function isLiveApprovalResolvedThinkAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      isLiveAddedCompress(next[prev!.length + 3])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      isLiveAddedCompress(next[prev!.length + 4])
    )
  }
  return false
}

/** Allow / Deny 收口后同一帧首枚 token + Stop：散文可先挂上再标 cancelled */
export function isLiveApprovalResolvedAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 1 &&
    isLiveAddedCancelledAnswer(next[prev!.length])
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 2) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return isLiveAddedCancelledAnswer(next[prev!.length + 1])
}

/** Allow / Deny 收口后同一帧 think + 首枚 token + Stop */
export function isLiveApprovalResolvedThinkAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 2 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedCancelledAnswer(next[prev!.length + 1])
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 1]) && isLiveAddedCancelledAnswer(next[prev!.length + 2])
  )
}

/** Allow / Deny 收口后同一帧错误 + Stop */
export function isLiveApprovalResolvedErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 1 &&
    isLiveErrorAnswer(next[prev!.length]!)
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 2) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return isLiveErrorAnswer(next[prev!.length + 1]!)
}

/** Allow / Deny 收口后同一帧 think + ```demo + Stop：工具未 tool_done，演示标 cancelled */
export function isLiveApprovalResolvedThinkAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 2)
  )
}

/** Allow / Deny 收口后同一帧 think + 错误 + Stop：工具未 tool_done，旁白标 cancelled */
export function isLiveApprovalResolvedThinkErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 2 &&
    isLiveAddedCancelledThink(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1])
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return (
    isLiveAddedCancelledThink(next[prev!.length + 1]) && isLiveAddedError(next[prev!.length + 2])
  )
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedError(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedError(next[prev!.length + 2]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 3)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      isLiveAddedError(next[prev!.length + 3])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      isLiveAddedError(next[prev!.length + 4])
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 */
export function isLiveApprovalResolvedAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && isLiveAddedError(next[prev!.length + 3])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + ```demo */
export function isLiveApprovalResolvedErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length + 1]) && isLiveAddedDemoFencePair(next, prev!.length + 2)
  }
  return false
}

/** Allow / Deny 收口后同一帧 think + ```demo + 错误 + Stop：工具未 tool_done，演示标 cancelled */
export function isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedError(next[next.length - 1])
  )
}

/** Allow / Deny 收口后同一帧 think + 错误 + ```demo + Stop：工具未 tool_done，演示标 cancelled */
export function isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveApprovalResolvedCancelPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedResolvedApprovalStatus(next[prev!.length])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 3)
  )
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + compress */
export function isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      isLiveAddedError(next[prev!.length + 3]) &&
      isLiveAddedCompress(next[prev!.length + 4])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      isLiveAddedError(next[prev!.length + 4]) &&
      isLiveAddedCompress(next[prev!.length + 5])
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + compress */
export function isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedError(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      isLiveAddedCompress(next[prev!.length + 4])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedError(next[prev!.length + 2]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 3) &&
      isLiveAddedCompress(next[prev!.length + 5])
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User */
export function isLiveApprovalResolvedThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedError(next[prev!.length + 1]) &&
      hasLiveAskNeededHead(next, prev!.length + 2)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedError(next[prev!.length + 2]) &&
      hasLiveAskNeededHead(next, prev!.length + 3)
    )
  }
  return false
}


/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User */
export function isLiveApprovalResolvedAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededHead(next, prev!.length + 2)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedDemoFencePair(next, prev!.length + 1) && hasLiveAskNeededHead(next, prev!.length + 3)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User */
export function isLiveApprovalResolvedErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededHead(next, prev!.length + 1)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededHead(next, prev!.length + 2)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User */
export function isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      hasLiveAskNeededHead(next, prev!.length + 3)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      hasLiveAskNeededHead(next, prev!.length + 4)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      isLiveAddedError(next[prev!.length + 3]) &&
      hasLiveAskNeededHead(next, prev!.length + 4)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      isLiveAddedError(next[prev!.length + 4]) &&
      hasLiveAskNeededHead(next, prev!.length + 5)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + ```demo + Ask User */
export function isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedError(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      hasLiveAskNeededHead(next, prev!.length + 4)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedError(next[prev!.length + 2]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 3) &&
      hasLiveAskNeededHead(next, prev!.length + 5)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User */
export function isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedDemoFencePair(next, prev!.length) &&
      isLiveAddedError(next[prev!.length + 2]) &&
      hasLiveAskNeededHead(next, prev!.length + 3)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      isLiveAddedError(next[prev!.length + 3]) &&
      hasLiveAskNeededHead(next, prev!.length + 4)
    )
  }
  return false
}


/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + Stop */
export function isLiveApprovalResolvedAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 2)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 3)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + Stop */
export function isLiveApprovalResolvedErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededCancelledHead(next, prev!.length + 1)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedError(next[prev!.length + 1]) && hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + Stop */
export function isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 3)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 4)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 错误 + Ask User + Stop */
export function isLiveApprovalResolvedThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedError(next[prev!.length + 1]) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 2)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedError(next[prev!.length + 2]) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 3)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalResolvedAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
      isLiveAddedError(next[prev!.length + 2]) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 3)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
      isLiveAddedError(next[prev!.length + 3]) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 4)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
      isLiveAddedError(next[prev!.length + 3]) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 4)
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) &&
      isLiveAddedError(next[prev!.length + 4]) &&
      hasLiveAskNeededCancelledHead(next, prev!.length + 5)
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 ```demo + Ask User + compress */
export function isLiveApprovalResolvedAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedDemoFencePair(next, prev!.length) &&
      hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
      isLiveAddedCompress(next[prev!.length + 4])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
      isLiveAddedCompress(next[prev!.length + 5])
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 错误 + Ask User + compress */
export function isLiveApprovalResolvedErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return (
      isLiveAddedError(next[prev!.length]) &&
      hasLiveAskNeededResolvedHead(next, prev!.length + 1) &&
      isLiveAddedCompress(next[prev!.length + 3])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return (
      isLiveAddedError(next[prev!.length + 1]) &&
      hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
      isLiveAddedCompress(next[prev!.length + 4])
    )
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + ```demo + Ask User + compress */
export function isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 6) {
    return (
      isLiveAddedThinkPair(next[prev!.length]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 1) &&
      hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
      isLiveAddedCompress(next[prev!.length + 5])
    )
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 7) {
    return (
      isLiveAddedThinkPair(next[prev!.length + 1]) &&
      isLiveAddedDemoFencePair(next, prev!.length + 2) &&
      hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
      isLiveAddedCompress(next[prev!.length + 6])
    )
  }
  return false
}


/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User */
export function isLiveApprovalResolvedAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    const text = next[prev!.length]
    return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 1)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    const text = next[prev!.length + 1]
    return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 2)
  }
  return false
}


/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User */
export function isLiveApprovalResolvedThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedThinkPair(next[prev!.length]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededHead(next, prev!.length + 2)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededHead(next, prev!.length + 3)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + Stop */
export function isLiveApprovalResolvedAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length])) && hasLiveAskNeededCancelledHead(next, prev!.length + 1)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1])) && hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + compress */
export function isLiveApprovalResolvedAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedCompress(next[prev!.length + 3])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedCompress(next[prev!.length + 4])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + Stop */
export function isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return isLiveAddedThinkPair(next[prev!.length]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 1])) && hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 2])) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + compress */
export function isLiveApprovalResolvedThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedCompress(next[prev!.length + 4])
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return hasLiveAskNeededResolvedHead(next, prev!.length) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 3) {
    return hasLiveAskNeededHead(next, prev!.length) && isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 4) {
    return hasLiveAskNeededHead(next, prev!.length + 1) && isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalResolvedAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 4) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length]) && !isLiveErrorAnswer(next[prev!.length]!)) && hasLiveAskNeededHead(next, prev!.length + 1) && isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 5) {
    return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededHead(next, prev!.length + 2) && isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
  }
  return false
}

/** Allow / Deny 只收口 Awaiting 行后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalResolvedThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalResolvedPrefixClose(prev, next) && next.length === prev!.length + 5) {
    return isLiveAddedThinkPair(next[prev!.length]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  }
  if (hasLiveApprovalResolvedAppendPrefix(prev, next) && next.length === prev!.length + 6) {
    return isLiveAddedThinkPair(next[prev!.length + 1]) && Boolean(isLiveAddedAnswerPair(next[prev!.length + 2]) && !isLiveErrorAnswer(next[prev!.length + 2]!)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
  }
  return false
}

/** Deny 收口并 tool_done error 后同一帧 Stop：Awaiting 行与工具一起收口，其余 active 标 cancelled */
export function isLiveApprovalDeniedSettleCancelChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let statusResolved = 0
  let toolSettled = 0
  let cancelled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) {
      cancelled += 1
      continue
    }
    return false
  }
  return statusResolved === 1 && toolSettled === 1 && cancelled >= 1
}

/** Allow 收口并 tool_done 后同一帧 think + Stop */
export function isLiveApprovalAllowedSettleThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  if (!isLiveAddedCancelledThink(next[prev.length])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 ```demo + compress */
export function isLiveApprovalAllowedSettleAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedCompress(next[prev!.length + 2])
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  )
}

/** Allow 收口并 tool_done 后同一帧 think + 首枚 token + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 2) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledAnswer(next[prev.length + 1])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧首枚 token + Stop */
export function isLiveApprovalAllowedSettleAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  if (!isLiveAddedCancelledAnswer(next[prev.length])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

function hasLiveApprovalAllowedSettlePrefixClose(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Stop */
export function isLiveApprovalAllowedSettleThinkErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 2) return false
  if (!isLiveAddedCancelledThink(next[prev.length])) return false
  if (!isLiveAddedError(next[prev.length + 1])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 3) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2)
  )
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3])
  )
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Stop */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedError(next[prev.length + 1])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 2)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2])
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 3) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length)) return false
  if (!isLiveAddedError(next[prev.length + 2])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Stop */
export function isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 3) return false
  if (!isLiveAddedError(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + compress */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  if (!isLiveAddedError(next[prev.length + 3])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Stop + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  if (!isLiveAddedError(next[prev.length + 3])) return false
  if (!isLiveAddedCompress(next[prev.length + 4])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededHead(next, prev!.length + 3)
  )
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User */
export function isLiveApprovalAllowedSettleThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededHead(next, prev!.length + 2)
  )
}


/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User */
export function isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededHead(next, prev!.length + 2)
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User */
export function isLiveApprovalAllowedSettleErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededHead(next, prev!.length + 1)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededHead(next, prev!.length + 4)
  )
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + ```demo + Ask User */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    hasLiveAskNeededHead(next, prev!.length + 4)
  )
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededHead(next, prev!.length + 3)
  )
}

/** Allow 收口并 tool_done 后同一帧 错误 + ```demo + Ask User */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededHead(next, prev!.length + 3)
  )
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  )
}

/** Allow 收口并 tool_done 后同一帧 think + 错误 + Ask User + Stop */
export function isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  )
}


/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  )
}

/** Allow 收口并 tool_done 后同一帧 错误 + Ask User + Stop */
export function isLiveApprovalAllowedSettleErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededCancelledHead(next, prev!.length + 1)
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  )
}

/** Allow 收口并 tool_done 后同一帧 ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  )
}

/** Allow 收口并 tool_done 后同一帧 think + ```demo + Ask User + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Ask User + compress */
export function isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}


/** Allow 收口并 tool_done 后同一帧 + think + 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  )
}

/** Allow 收口并 tool_done 后同一帧 + 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  )
}

/** Allow 收口并 tool_done 后同一帧 + 错误 + Ask User + compress */
export function isLiveApprovalAllowedSettleErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  )
}

/** Allow 收口并 tool_done 后同一帧 + think + 错误 + Ask User + compress */
export function isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** Allow 收口并 tool_done 后同一帧 + think + ```demo + 错误 + Ask User + compress */
export function isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** Allow 收口并 tool_done 后同一帧 + ```demo + 错误 + Ask User + compress */
export function isLiveApprovalAllowedSettleAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** Allow 收口并 tool_done 后同一帧 + 错误 + ```demo + Ask User + compress */
export function isLiveApprovalAllowedSettleErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}


/** Allow收口并 tool_done 后同一帧 think + 首枚 token + Ask User */
export function isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  const text = next[prev!.length + 1]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 2)
}

/** Allow收口并 tool_done 后同一帧 首枚 token + Ask User + Stop */
export function isLiveApprovalAllowedSettleAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length])) && hasLiveAskNeededCancelledHead(next, prev!.length + 1)
}

/** Allow收口并 tool_done 后同一帧 首枚 token + Ask User + compress */
export function isLiveApprovalAllowedSettleAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  const text = next[prev!.length]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedCompress(next[prev!.length + 3])
}

/** Allow收口并 tool_done 后同一帧 think + 首枚 token + Ask User + Stop */
export function isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1])) && hasLiveAskNeededCancelledHead(next, prev!.length + 2)
}

/** Allow收口并 tool_done 后同一帧 think + 首枚 token + Ask User + compress */
export function isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  const text = next[prev!.length + 1]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Allow收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Allow收口并 tool_done 后同一帧 Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (!hasLiveAskNeededHead(next, prev!.length)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Allow收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  const text = next[prev!.length]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Allow收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalAllowedSettleAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  const text = next[prev!.length]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 1)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Allow收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefixClose(prev, next)) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  const text = next[prev!.length + 1]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Allow 收口并 tool_done 后同一帧 ```demo + Stop */
export function isLiveApprovalAllowedSettleAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 2) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧错误 + Stop */
export function isLiveApprovalAllowedSettleErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  if (!isLiveErrorAnswer(next[prev.length]!)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow 收口并 tool_done 后同一帧下一工具 */
export function isLiveApprovalAllowedSettleToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return (
    hasLiveApprovalAllowedSettlePrefixClose(prev, next) &&
    isLiveAddedToolsWithOptionalStatus(prev!.length, next)
  )
}

function hasLiveApprovalDeniedPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 后同一帧 approval_resolved + tool_done error：Awaiting 行与工具一起收口（对标 query-loop 拒绝后立即 yield，不复制 #10760 卡住审批） */
export function isLiveApprovalDeniedSettleChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveApprovalDeniedPrefix(prev, next) && next.length === prev!.length
}

/** Deny 收口并 tool_done error 后同一帧 think + 首枚 token */
export function isLiveApprovalDeniedThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!))
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 */
export function isLiveApprovalDeniedThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return isLiveAddedThinkPair(next[prev!.length]) && isLiveErrorAnswer(next[prev!.length + 1]!)
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo */
export function isLiveApprovalDeniedThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1)
}

/** Deny 收口并 tool_done error 后同一帧 think + 首枚 token + compress */
export function isLiveApprovalDeniedThinkAnswerCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    Boolean(isLiveAddedAnswerPair(next[prev!.length + 1]) && !isLiveErrorAnswer(next[prev!.length + 1]!)) &&
    isLiveAddedCompress(next[prev!.length + 2])
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + compress */
export function isLiveApprovalDeniedThinkAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + compress */
export function isLiveApprovalDeniedThinkErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveErrorAnswer(next[prev!.length + 1]!) &&
    isLiveAddedCompress(next[prev!.length + 2])
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 首枚 token + Stop */
export function isLiveApprovalDeniedThinkAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 2) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledAnswer(next[prev.length + 1])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 3) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + Stop */
export function isLiveApprovalDeniedThinkErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 2) return false
  if (!isLiveAddedCancelledThink(next[prev.length])) return false
  if (!isLiveAddedError(next[prev.length + 1])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + ```demo */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2)
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + 错误 */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3])
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + ```demo + Stop */
export function isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedError(next[prev.length + 1])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 2)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 ```demo + 错误 */
export function isLiveApprovalDeniedAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedError(next[prev!.length + 2])
}

/** Deny 收口并 tool_done error 后同一帧 错误 + ```demo */
export function isLiveApprovalDeniedErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedError(next[prev!.length]) && isLiveAddedDemoFencePair(next, prev!.length + 1)
}

/** Deny 收口并 tool_done error 后同一帧 ```demo + 错误 + Stop */
export function isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 3) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length)) return false
  if (!isLiveAddedError(next[prev.length + 2])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 错误 + ```demo + Stop */
export function isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 3) return false
  if (!isLiveAddedError(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + 错误 + compress */
export function isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + ```demo + compress */
export function isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + 错误 + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  if (!isLiveAddedError(next[prev.length + 3])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + 错误 + Stop + compress */
export function isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev.length])) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length + 1)) return false
  if (!isLiveAddedError(next[prev.length + 3])) return false
  if (!isLiveAddedCompress(next[prev.length + 4])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + Ask User */
export function isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededHead(next, prev!.length + 3)
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + Ask User */
export function isLiveApprovalDeniedThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededHead(next, prev!.length + 2)
  )
}


/** Deny 收口并 tool_done error 后同一帧 ```demo + Ask User */
export function isLiveApprovalDeniedAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return isLiveAddedDemoFencePair(next, prev!.length) && hasLiveAskNeededHead(next, prev!.length + 2)
}

/** Deny 收口并 tool_done error 后同一帧 错误 + Ask User */
export function isLiveApprovalDeniedErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededHead(next, prev!.length + 1)
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + 错误 + Ask User */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededHead(next, prev!.length + 4)
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + ```demo + Ask User */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    hasLiveAskNeededHead(next, prev!.length + 4)
  )
}

/** Deny 收口并 tool_done error 后同一帧 ```demo + 错误 + Ask User */
export function isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededHead(next, prev!.length + 3)
  )
}

/** Deny 收口并 tool_done error 后同一帧 错误 + ```demo + Ask User */
export function isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededHead(next, prev!.length + 3)
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + Ask User + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + 错误 + Ask User + Stop */
export function isLiveApprovalDeniedThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  )
}


/** Deny 收口并 tool_done error 后同一帧 ```demo + Ask User + Stop */
export function isLiveApprovalDeniedAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  )
}

/** Deny 收口并 tool_done error 后同一帧 错误 + Ask User + Stop */
export function isLiveApprovalDeniedErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedError(next[prev!.length]) && hasLiveAskNeededCancelledHead(next, prev!.length + 1)
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  )
}

/** Deny 收口并 tool_done error 后同一帧 ```demo + 错误 + Ask User + Stop */
export function isLiveApprovalDeniedAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  )
}

/** Deny 收口并 tool_done error 后同一帧 think + ```demo + Ask User + compress */
export function isLiveApprovalDeniedThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** Deny 收口并 tool_done error 后同一帧 ```demo + Ask User + compress */
export function isLiveApprovalDeniedAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}


/** Deny 收口并 tool_done error 后同一帧 + think + 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  )
}

/** Deny 收口并 tool_done error 后同一帧 + 错误 + ```demo + Ask User + Stop */
export function isLiveApprovalDeniedErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  )
}

/** Deny 收口并 tool_done error 后同一帧 + 错误 + Ask User + compress */
export function isLiveApprovalDeniedErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  )
}

/** Deny 收口并 tool_done error 后同一帧 + think + 错误 + Ask User + compress */
export function isLiveApprovalDeniedThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  )
}

/** Deny 收口并 tool_done error 后同一帧 + think + ```demo + 错误 + Ask User + compress */
export function isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** Deny 收口并 tool_done error 后同一帧 + ```demo + 错误 + Ask User + compress */
export function isLiveApprovalDeniedAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** Deny 收口并 tool_done error 后同一帧 + 错误 + ```demo + Ask User + compress */
export function isLiveApprovalDeniedErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  return (
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}


/** Deny收口并 tool_done 后同一帧 think + 首枚 token + Ask User */
export function isLiveApprovalDeniedThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  const text = next[prev!.length + 1]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 2)
}

/** Deny收口并 tool_done 后同一帧 首枚 token + Ask User + Stop */
export function isLiveApprovalDeniedAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 3) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length])) && hasLiveAskNeededCancelledHead(next, prev!.length + 1)
}

/** Deny收口并 tool_done 后同一帧 首枚 token + Ask User + compress */
export function isLiveApprovalDeniedAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  const text = next[prev!.length]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 1) && isLiveAddedCompress(next[prev!.length + 3])
}

/** Deny收口并 tool_done 后同一帧 think + 首枚 token + Ask User + Stop */
export function isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 4) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 1])) && hasLiveAskNeededCancelledHead(next, prev!.length + 2)
}

/** Deny收口并 tool_done 后同一帧 think + 首枚 token + Ask User + compress */
export function isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (next.length !== prev!.length + 5) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  const text = next[prev!.length + 1]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 2) && isLiveAddedCompress(next[prev!.length + 4])
}

/** Deny收口并 tool_done 后同一帧 Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Deny收口并 tool_done 后同一帧 Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (!hasLiveAskNeededHead(next, prev!.length)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Deny收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  const text = next[prev!.length]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 1)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Deny收口并 tool_done 后同一帧 首枚 token + Ask User + 下一工具仍 active */
export function isLiveApprovalDeniedAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  const text = next[prev!.length]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 1)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Deny收口并 tool_done 后同一帧 think + 首枚 token + Ask User + 下一工具已 complete_call */
export function isLiveApprovalDeniedThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefixClose(prev, next)) return false
  if (!isLiveAddedThinkPair(next[prev!.length])) return false
  const text = next[prev!.length + 1]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Deny 收口并 tool_done error 后同一帧 ```demo + Stop */
export function isLiveApprovalDeniedAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 2) return false
  if (!isLiveAddedCancelledDemoFencePair(next, prev.length)) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧错误 + Stop */
export function isLiveApprovalDeniedErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  if (!isLiveAddedError(next[prev.length])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧首枚 token + Stop */
export function isLiveApprovalDeniedAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  if (!isLiveAddedCancelledAnswer(next[prev.length])) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) continue
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口并 tool_done error 后同一帧 ```demo */
export function isLiveApprovalDeniedAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return isLiveAddedDemoFencePair(next, prev!.length)
}

/** Deny 收口并 tool_done error 后同一帧 ```demo + compress */
export function isLiveApprovalDeniedAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedDemoFencePair(next, prev!.length) && isLiveAddedCompress(next[prev!.length + 2])
}

/** Deny 收口后同一帧新开 规划下一步：过程 remap 并追加 status */
export function isLiveApprovalDeniedStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedStatusPair(next[prev!.length])
}

/** Deny 收口后同一帧 context_compress：过程 remap 并追加压缩步（不复制 #24432 compact 卡住） */
export function isLiveApprovalDeniedCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedCompress(next[prev!.length])
}

function hasLiveApprovalDeniedPrefixClose(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'error') {
      toolSettled += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Deny 收口后同一帧下一工具（可带一条 规划下一步）：过程 remap 并追加这些步 */
export function isLiveApprovalDeniedToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveApprovalDeniedPrefix(prev, next) && isLiveAddedToolsWithOptionalStatus(prev!.length, next)) {
    return true
  }
  return (
    hasLiveApprovalDeniedPrefixClose(prev, next) && isLiveAddedToolsWithOptionalStatus(prev!.length, next)
  )
}

function hasLiveApprovalAllowedWriteStatPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let resolved = 0
  let writeStat = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    if (isLiveToolWriteStatChange(before, after)) {
      writeStat += 1
      continue
    }
    return false
  }
  return resolved === 1 && writeStat === 1
}

/** Allow once 后同一帧 approval_resolved + 首枚 tool_preview：Awaiting 行收口并换该工具写盘 +/-（对标 query-loop 放行后立即 runToolWithLiveStatus；不复制 #10760 / #38695） */
export function isLiveApprovalAllowedWriteStatChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveApprovalAllowedWriteStatPrefix(prev, next) && Boolean(prev && next.length === prev.length)
}

/** Allow 写盘收口后同一帧新开 规划下一步：过程 remap 并追加 status，回答只换 diff 槽 */
export function isLiveApprovalAllowedWriteStatStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) {
    return false
  }
  return isLiveAddedStatusPair(next[prev!.length])
}

/** Allow 写盘收口后同一帧下一工具（可带一条 规划下一步）：过程 remap 并追加这些步，回答只换 diff 槽 */
export function isLiveApprovalAllowedWriteStatToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return (
    hasLiveApprovalAllowedWriteStatPrefix(prev, next) &&
    isLiveAddedToolsWithOptionalStatus(prev!.length, next)
  )
}

function hasLiveApprovalAllowedSettlePrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let statusResolved = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      statusResolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    return false
  }
  return statusResolved === 1 && toolSettled === 1
}

/** Allow once 后同一帧 approval_resolved + tool_done：Awaiting 行与工具一起收口（对标 query-loop 放行后立即执行；不复制 #10760 / #36115） */
export function isLiveApprovalAllowedSettleChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveApprovalAllowedSettlePrefix(prev, next) && next.length === prev!.length
}

/** Allow 收口后同一帧新开 规划下一步：过程 remap 并追加 status */
export function isLiveApprovalAllowedStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedStatusPair(next[prev!.length])
}

/** Allow 收口后同一帧下一工具（可带一条 规划下一步）：过程 remap 并追加这些步 */
export function isLiveApprovalAllowedToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveApprovalAllowedSettlePrefix(prev, next) && isLiveAddedToolsWithOptionalStatus(prev!.length, next)
}

/** Allow 收口后同一帧 context_compress：过程 remap 并追加压缩步（不复制 #24432 compact 卡住） */
export function isLiveApprovalAllowedCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedCompress(next[prev!.length])
}

/** Allow 收口后同一帧 Stop：工具已 complete_call，其余 active 标 cancelled */
export function isLiveApprovalAllowedCancelChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let resolved = 0
  let toolSettled = 0
  let cancelled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAwaitingStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.status === 'done') {
      toolSettled += 1
      continue
    }
    if (isLiveCancelRetarget(before, after)) {
      cancelled += 1
      continue
    }
    return false
  }
  return resolved === 1 && toolSettled === 1 && cancelled >= 1
}

/** Allow 收口后同一帧 think：过程 remap，旁白续尾 */
export function isLiveApprovalAllowedThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedThinkPair(next[prev!.length])
}

/** Allow 收口后同一帧首枚 token：过程 remap，回答开散文尾 */
export function isLiveApprovalAllowedAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedAnswerPair(next[prev!.length])
}

/** Allow 收口后同一帧 think + 首枚 token */
export function isLiveApprovalAllowedThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 2) return false
  return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1])
}

/** Allow 收口后同一帧 think + 下一工具已 complete_call */
export function isLiveApprovalAllowedThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** Allow 收口后同一帧首枚 token + 下一工具已 complete_call */
export function isLiveApprovalAllowedAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || !isLiveAddedAnswerPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** Allow 收口后同一帧 think + 首枚 token + 下一工具已 complete_call */
export function isLiveApprovalAllowedThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedAnswerPair(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Allow 收口后同一帧 ```demo + present_inline_demo */
export function isLiveApprovalAllowedAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || next.length !== prev!.length + 2) return false
  const text = next[prev!.length]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 1])
}

/** Allow 收口后同一帧 think + ```demo + present_inline_demo */
export function isLiveApprovalAllowedThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedSettlePrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 3) return false
  const text = next[prev!.length + 1]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 2])
}

/** Allow 写盘收口后同一帧 think：过程 remap，旁白续尾，回答只换 diff 槽 */
export function isLiveApprovalAllowedWriteStatThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) {
    return false
  }
  return isLiveAddedThinkPair(next[prev!.length])
}

/** Allow 写盘收口后同一帧首枚 token：过程 remap，回答开散文尾并换 diff 槽 */
export function isLiveApprovalAllowedWriteStatAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) {
    return false
  }
  return isLiveAddedAnswerPair(next[prev!.length])
}

/** Allow 写盘收口后同一帧 think + 首枚 token */
export function isLiveApprovalAllowedWriteStatThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) {
    return false
  }
  return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAnswerPair(next[prev!.length + 1])
}

/** Allow 写盘收口后同一帧 think + 下一工具已 complete_call */
export function isLiveApprovalAllowedWriteStatThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** Allow 写盘收口后同一帧首枚 token + 下一工具已 complete_call */
export function isLiveApprovalAllowedWriteStatAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || !isLiveAddedAnswerPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** Allow 写盘收口后同一帧 think + 首枚 token + 下一工具已 complete_call */
export function isLiveApprovalAllowedWriteStatThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedAnswerPair(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Allow 写盘收口后同一帧 ```demo + present_inline_demo */
export function isLiveApprovalAllowedWriteStatAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) {
    return false
  }
  const text = next[prev!.length]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 1])
}

/** Allow 写盘收口后同一帧 think + ```demo + present_inline_demo */
export function isLiveApprovalAllowedWriteStatThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalAllowedWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 3) return false
  const text = next[prev!.length + 1]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 2])
}

/** Deny 收口后同一帧 think：过程 remap，旁白续尾 */
export function isLiveApprovalDeniedThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedThinkPair(next[prev!.length])
}

/** Deny 收口后同一帧首枚 token：过程 remap，回答开散文尾 */
export function isLiveApprovalDeniedAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedAnswerPair(next[prev!.length])
}

/** Deny 收口后同一帧 think + 下一工具已 complete_call */
export function isLiveApprovalDeniedThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

/** Deny 收口后同一帧首枚 token + 下一工具已 complete_call */
export function isLiveApprovalDeniedAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveApprovalDeniedPrefix(prev, next) || !isLiveAddedAnswerPair(next[prev!.length])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 1, next)
}

function isLiveUserInputToolRetarget(prev: TurnSegment, next: TurnSegment): boolean {
  if (!sameLiveToolCore(prev, next) || prev.toolName !== REQUEST_USER_INPUT_TOOL) return false
  return (prev.toolTitle ?? '') !== (next.toolTitle ?? '') || (prev.toolDetail ?? '') !== (next.toolDetail ?? '')
}

function isLiveStatusContentHold(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.kind !== 'status' || next.kind !== 'status' || prev.id !== next.id) return false
  return prev.status === 'active' && next.status === 'active'
}

function isLiveAskStatusRetarget(prev: TurnSegment, next: TurnSegment): boolean {
  return isLiveStatusContentHold(prev, next) && next.toolName === REQUEST_USER_INPUT_TOOL
}

function isLiveAddedAskOrAwaitingStatus(segment: TurnSegment | undefined): boolean {
  if (!segment || segment.kind !== 'status' || segment.status !== 'active') return false
  return (
    segment.toolName === REQUEST_USER_INPUT_TOOL || isAwaitingApprovalText(segment.content ?? '')
  )
}

/** Ask User 挂上：工具标题换成 Question requested / header，并新开或改写 status 行 */
export function isLiveUserInputNeededChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || (next.length !== prev.length && next.length !== prev.length + 1)) return false
  let toolRetarget = 0
  let statusRetarget = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveUserInputToolRetarget(before, after)) {
      toolRetarget += 1
      continue
    }
    if (next.length === prev.length && isLiveAskStatusRetarget(before, after)) {
      statusRetarget += 1
      continue
    }
    if (next.length === prev.length && isLiveStatusContentHold(before, after)) {
      statusRetarget += 1
      continue
    }
    return false
  }
  if (next.length === prev.length + 1) {
    if (toolRetarget !== 1) return false
    const added = next[next.length - 1]
    return Boolean(added && added.kind === 'status' && added.status === 'active')
  }
  if (toolRetarget === 1 && statusRetarget === 1) return true
  return toolRetarget === 0 && statusRetarget === 1 && next.some((segment, index) => {
    const before = prev[index]
    return Boolean(before && before !== segment && isLiveAskStatusRetarget(before, segment))
  })
}

/** 同一条 status 从 active 收成 done：只换该行（Ask User / compact / reconnect 收束） */
export function isLiveStatusSettleChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let settled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (
      before.kind === 'status' &&
      after.kind === 'status' &&
      before.id === after.id &&
      before.status === 'active' &&
      after.status === 'done'
    ) {
      settled += 1
      continue
    }
    return false
  }
  return settled === 1
}

/** Ask User 作答后同一帧 user_input_resolved + tool_done：Question requested 行与工具一起收成 done（对标 query-loop 连续 yield，不发明 60s 空答 #28969） */
export function isLiveAskResolvedSettleChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let statusSettled = 0
  let toolSettled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (
      before.kind === 'status' &&
      after.kind === 'status' &&
      before.id === after.id &&
      before.status === 'active' &&
      after.status === 'done' &&
      after.toolName === REQUEST_USER_INPUT_TOOL
    ) {
      statusSettled += 1
      continue
    }
    if (isLiveToolSettleChange(before, after) && after.toolName === REQUEST_USER_INPUT_TOOL) {
      toolSettled += 1
      continue
    }
    return false
  }
  return statusSettled === 1 && toolSettled === 1
}

function isLiveAddedResolvedAskStatus(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'status' &&
      segment.status === 'done' &&
      segment.toolName === REQUEST_USER_INPUT_TOOL
  )
}

/** Ask User 作答后同一帧 Stop：问句行标 done，工具标 cancelled（对标 interrupt after request_user_input；不发明 60s 空答，不复制 #10952 Stop 失效） */
export function isLiveAskResolvedCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 2) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  return isLiveAddedResolvedAskStatus(next[prev!.length + 1])
}

/** Ask User 作答后同一帧 think + Stop：问句行标 done，工具与旁白标 cancelled */
export function isLiveAskResolvedThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedCancelledThink(next[prev!.length + 2])
}

function isLiveAskStatusResolve(prev: TurnSegment, next: TurnSegment): boolean {
  return (
    prev.kind === 'status' &&
    next.kind === 'status' &&
    prev.id === next.id &&
    prev.status === 'active' &&
    next.status === 'done' &&
    next.toolName === REQUEST_USER_INPUT_TOOL
  )
}

function hasLiveAskResolvedCancelHangPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let cancelledAsk = 0
  let resolved = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAskStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    if (
      isLiveCancelRetarget(before, after) &&
      after.kind === 'tool' &&
      after.toolName === REQUEST_USER_INPUT_TOOL
    ) {
      cancelledAsk += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveCancelRetarget(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return cancelledAsk === 1 && resolved === 1
}

function isLiveAddedCancelledAnswer(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'text' &&
      segment.status === 'cancelled' &&
      !hasStreamingDemoFence(segment.content ?? '')
  )
}

/** Ask User 作答后同一帧首枚 token + Stop：问句行标 done，工具与散文标 cancelled */
export function isLiveAskResolvedAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 1 &&
    isLiveAddedCancelledAnswer(next[prev!.length])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedCancelledAnswer(next[prev!.length + 2])
}

/** Ask User 作答后同一帧 ```demo + compress：问句行标 done，演示与压缩步追加 */
export function isLiveAskResolvedAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedCompress(next[prev!.length + 2])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedCompress(next[prev!.length + 4])
}

function hasLiveAskResolvedHangPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  let resolved = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveAskStatusResolve(before, after)) {
      resolved += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after) ||
      sameLiveToolCore(before, after)
    ) {
      continue
    }
    return false
  }
  return resolved === 1
}

/** Ask User 作答后同一帧 think + 首枚 token + Stop */
export function isLiveAskResolvedThinkAnswerCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 2 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedCancelledAnswer(next[prev!.length + 1])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedCancelledAnswer(next[prev!.length + 3])
  )
}

/** Ask User 作答后同一帧 think + 错误 + Stop */
export function isLiveAskResolvedThinkErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 2 &&
    isLiveAddedCancelledThink(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedCancelledThink(next[prev!.length + 2]) && isLiveAddedError(next[prev!.length + 3])
  )
}

/** Ask User 作答后同一帧 错误 + Stop */
export function isLiveAskResolvedErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 1 &&
    isLiveAddedError(next[prev!.length])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2])
}

/** Ask User 作答后同一帧 ```demo + Stop */
export function isLiveAskResolvedAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 2 &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedCancelledDemoFencePair(next, prev!.length + 2)
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Stop */
export function isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 4)
  )
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Stop */
export function isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[next.length - 1])
  )
}

/** Ask User 作答后同一帧 错误 + ```demo + Stop */
export function isLiveAskResolvedErrorAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) && isLiveAddedCancelledDemoFencePair(next, prev!.length + 3)
  )
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + compress */
export function isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + Stop + compress */
export function isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** Ask User 作答后同一帧 ```demo + 错误 + Stop */
export function isLiveAskResolvedAnswerDemoErrorCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[next.length - 1])
  )
}

/** Ask User 作答后同一帧 错误 + ```demo + compress */
export function isLiveAskResolvedErrorAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}


/** Ask User 作答后同一帧 think + ```demo + 再开 Ask User */
export function isLiveAskResolvedThinkAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededHead(next, prev!.length + 3)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededHead(next, prev!.length + 5)
  )
}


/** Ask User 作答后同一帧 ```demo + 再开 Ask User */
export function isLiveAskResolvedAnswerDemoAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedDemoFencePair(next, prev!.length) &&
    hasLiveAskNeededHead(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && hasLiveAskNeededHead(next, prev!.length + 4)
}

/** Ask User 作答后同一帧 错误 + 再开 Ask User */
export function isLiveAskResolvedErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedError(next[prev!.length]) &&
    hasLiveAskNeededHead(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** Ask User 作答后同一帧 think + 错误 + 再开 Ask User */
export function isLiveAskResolvedThinkErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededHead(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededHead(next, prev!.length + 4)
  )
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + 再开 Ask User */
export function isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededHead(next, prev!.length + 4)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededHead(next, prev!.length + 6)
  )
}


/** Ask User 作答后同一帧 ```demo + 再开 Ask User + Stop */
export function isLiveAskResolvedAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** Ask User 作答后同一帧 错误 + 再开 Ask User + Stop */
export function isLiveAskResolvedErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedError(next[prev!.length]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** Ask User 作答后同一帧 think + 错误 + 再开 Ask User + Stop */
export function isLiveAskResolvedThinkErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  )
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + 再开 Ask User + Stop */
export function isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 4)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 6)
  )
}

/** Ask User 作答后同一帧 ```demo + 再开 Ask User + compress */
export function isLiveAskResolvedAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDemoFencePair(next, prev!.length) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** Ask User 作答后同一帧 错误 + 再开 Ask User + compress */
export function isLiveAskResolvedErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedError(next[prev!.length]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** Ask User 作答后同一帧 think + 错误 + 再开 Ask User + compress */
export function isLiveAskResolvedThinkErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}


/** Ask User 作答后同一帧 think + ```demo + 再开 Ask User + Stop */
export function isLiveAskResolvedThinkAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}

/** Ask User 作答后同一帧 think + ```demo + 再开 Ask User + compress */
export function isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** Ask User 作答后同一帧 think + ```demo + 错误 + 再开 Ask User + compress */
export function isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 7 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 9) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[prev!.length + 5]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 6) &&
    isLiveAddedCompress(next[prev!.length + 8])
  )
}

/** Ask User 作答后同一帧 错误 + ```demo + 再开 Ask User + Stop */
export function isLiveAskResolvedErrorAnswerDemoAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 1) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 3) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}

/** Ask User 作答后同一帧 错误 + ```demo + 再开 Ask User + compress */
export function isLiveAskResolvedErrorAnswerDemoAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** Ask User 作答后同一帧 ```demo + 错误 + 再开 Ask User + Stop */
export function isLiveAskResolvedAnswerDemoErrorAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 3)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedDoneDemoCancelledToolPair(next, prev!.length + 2) &&
    isLiveAddedError(next[prev!.length + 4]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 5)
  )
}

/** Ask User 作答后同一帧 ```demo + 错误 + 再开 Ask User + compress */
export function isLiveAskResolvedAnswerDemoErrorAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 6 &&
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 8) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedError(next[prev!.length + 4]) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 5) &&
    isLiveAddedCompress(next[prev!.length + 7])
  )
}

/** Ask User 作答后同一帧 首枚 token + 再开 Ask User */
export function isLiveAskResolvedAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedAnswerPair(next[prev!.length]) &&
    !isLiveErrorAnswer(next[prev!.length]!) &&
    hasLiveAskNeededHead(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 3)
}

/** Ask User 作答后同一帧 think + 首枚 token + 再开 Ask User */
export function isLiveAskResolvedThinkAnswerAskAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedAnswerPair(next[prev!.length + 1]) &&
    !isLiveErrorAnswer(next[prev!.length + 1]!) &&
    hasLiveAskNeededHead(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededHead(next, prev!.length + 4)
}


/** Ask User 作答后同一帧 首枚 token + 再开 Ask User + Stop */
export function isLiveAskResolvedAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedAnswerPair(next[prev!.length]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 2])) && hasLiveAskNeededCancelledHead(next, prev!.length + 3)
}

/** Ask User 作答后同一帧 think + 首枚 token + 再开 Ask User + Stop */
export function isLiveAskResolvedThinkAnswerAskCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedAnswerPair(next[prev!.length + 1]) &&
    hasLiveAskNeededCancelledHead(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return Boolean(isLiveAddedAnswerPair(next[prev!.length + 3])) && hasLiveAskNeededCancelledHead(next, prev!.length + 4)
}

/** Ask User 作答后同一帧 首枚 token + 再开 Ask User + compress */
export function isLiveAskResolvedAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedAnswerPair(next[prev!.length]) &&
    !isLiveErrorAnswer(next[prev!.length]!) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  const text = next[prev!.length + 2]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 3) && isLiveAddedCompress(next[prev!.length + 5])
}

/** Ask User 作答后同一帧 think + 首枚 token + 再开 Ask User + compress */
export function isLiveAskResolvedThinkAnswerAskCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedAnswerPair(next[prev!.length + 1]) &&
    !isLiveErrorAnswer(next[prev!.length + 1]!) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  return Boolean(text && isLiveAddedAnswerPair(text) && !isLiveErrorAnswer(text)) && hasLiveAskNeededResolvedHead(next, prev!.length + 4) && isLiveAddedCompress(next[prev!.length + 6])
}

/** Ask User 作答后同一帧 再开 Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    hasLiveAskNeededResolvedHead(next, prev!.length) &&
    isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 2)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Ask User 作答后同一帧 再开 Ask User + 下一工具仍 active */
export function isLiveAskResolvedAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    hasLiveAskNeededHead(next, prev!.length) &&
    isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 2)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 4, next)
}

/** Ask User 作答后同一帧 首枚 token + 再开 Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedAnswerPair(next[prev!.length]) &&
    !isLiveErrorAnswer(next[prev!.length]!) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 1) &&
    isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 3)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Ask User 作答后同一帧 首枚 token + 再开 Ask User + 下一工具仍 active */
export function isLiveAskResolvedAnswerAskActiveToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedAnswerPair(next[prev!.length]) &&
    !isLiveErrorAnswer(next[prev!.length]!) &&
    hasLiveAskNeededHead(next, prev!.length + 1) &&
    isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  const text = next[prev!.length + 2]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededHead(next, prev!.length + 3)) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 5, next)
}

/** Ask User 作答后同一帧 think + 首枚 token + 再开 Ask User + 下一工具已 complete_call */
export function isLiveAskResolvedThinkAnswerAskToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedAnswerPair(next[prev!.length + 1]) &&
    !isLiveErrorAnswer(next[prev!.length + 1]!) &&
    hasLiveAskNeededResolvedHead(next, prev!.length + 2) &&
    isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  const text = next[prev!.length + 3]
  if (!text || !isLiveAddedAnswerPair(text) || isLiveErrorAnswer(text)) return false
  if (!hasLiveAskNeededResolvedHead(next, prev!.length + 4)) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 6, next)
}

/** Ask User 作答后同一帧 ```demo + 错误 + compress */
export function isLiveAskResolvedAnswerDemoErrorCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2]) &&
    isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedError(next[prev!.length + 4]) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** Ask User 作答后同一帧 think + ```demo + 错误 */
export function isLiveAskResolvedThinkAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedError(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedError(next[next.length - 1])
  )
}

/** Ask User 作答后同一帧 think + 错误 + ```demo */
export function isLiveAskResolvedThinkErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 2)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 4)
  )
}

/** Ask User 作答后同一帧 ```demo + 错误 */
export function isLiveAskResolvedAnswerDemoErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedDemoFencePair(next, prev!.length) &&
    isLiveAddedError(next[prev!.length + 2])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedDemoFencePair(next, prev!.length + 2) && isLiveAddedError(next[next.length - 1])
}

/** Ask User 作答后同一帧 错误 + ```demo */
export function isLiveAskResolvedErrorAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedError(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedError(next[prev!.length + 2]) && isLiveAddedDemoFencePair(next, prev!.length + 3)
}

/** Ask User 作答后同一帧 think + 错误 + ```demo + Stop + compress */
export function isLiveAskResolvedThinkErrorAnswerDemoCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 5 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedError(next[prev!.length + 1]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 2) &&
    isLiveAddedCompress(next[prev!.length + 4])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 7) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedError(next[prev!.length + 3]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 4) &&
    isLiveAddedCompress(next[prev!.length + 6])
  )
}

/** Ask User 作答后同一帧 think + ```demo + Stop */
export function isLiveAskResolvedThinkAnswerDemoCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 3 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 1)
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 5) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedCancelledDemoFencePair(next, prev!.length + 3)
  )
}

/** Ask User 作答后同一帧 think + ```demo + compress */
export function isLiveAskResolvedThinkAnswerDemoCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedHangPrefix(prev, next) &&
    next.length === prev!.length + 4 &&
    isLiveAddedThinkPair(next[prev!.length]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 1) &&
    isLiveAddedCompress(next[prev!.length + 3])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 6) return false
  if (!isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) &&
    isLiveAddedDemoFencePair(next, prev!.length + 3) &&
    isLiveAddedCompress(next[prev!.length + 5])
  )
}

/** Ask User 作答后同一帧首枚 token + Stop + compress */
export function isLiveAskResolvedAnswerCancelCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    hasLiveAskResolvedCancelHangPrefix(prev, next) &&
    next.length === prev!.length + 2 &&
    isLiveAddedCancelledAnswer(next[prev!.length]) &&
    isLiveAddedCompress(next[prev!.length + 1])
  ) {
    return true
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  if (!isLiveAddedCancelledAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return (
    isLiveAddedCancelledAnswer(next[prev!.length + 2]) && isLiveAddedCompress(next[prev!.length + 3])
  )
}

/** Ask User 作答后同一帧 think + 下一工具 */
export function isLiveAskResolvedThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveAskResolvedHangPrefix(prev, next) && isLiveAddedThinkPair(next[prev!.length])) {
    return isLiveAddedToolsWithOptionalStatus(prev!.length + 1, next)
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 2])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Ask User 作答后同一帧下一工具 */
export function isLiveAskResolvedToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveAskResolvedHangPrefix(prev, next)) {
    return isLiveAddedToolsWithOptionalStatus(prev!.length, next)
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
}

/** Ask User 作答后同一帧下一工具已 complete_call */
export function isLiveAskResolvedSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (hasLiveAskResolvedHangPrefix(prev, next)) {
    return isLiveAddedSettledToolsWithOptionalStatus(prev!.length, next)
  }
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedAskTool(next[prev!.length])) return false
  if (!isLiveAddedResolvedAskStatus(next[prev!.length + 1])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 2, next)
}

function isLiveAddedAskTool(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'tool' &&
      segment.status === 'active' &&
      segment.toolName === REQUEST_USER_INPUT_TOOL
  )
}

function isLiveAddedAskStatusPair(segment: TurnSegment | undefined): boolean {
  return isLiveAddedStatusPair(segment) && segment!.toolName === REQUEST_USER_INPUT_TOOL
}

function hasLiveAskNeededHead(
  next: readonly TurnSegment[],
  start: number
): boolean {
  return isLiveAddedAskTool(next[start]) && isLiveAddedAskStatusPair(next[start + 1])
}

function hasLiveAskNeededResolvedHead(
  next: readonly TurnSegment[],
  start: number
): boolean {
  return isLiveAddedAskTool(next[start]) && isLiveAddedResolvedAskStatus(next[start + 1])
}

function hasLiveAskNeededPrefixClose(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveToolAppendPrefixClose(prev, next) && hasLiveAskNeededHead(next, prev!.length)
}

function hasLiveAskNeededWriteStatPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveWriteStatPrefix(prev, next) && hasLiveAskNeededHead(next, prev!.length)
}

function hasLiveAskNeededWriteStatStatusPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  return hasLiveAskNeededHead(next, prev!.length + 1)
}

/** 规划下一步 / 正文后同一帧 Ask User 挂上并立刻 think：过程追加问句与 Question requested，旁白续尾（不发明 TUI Questions n/n） */
export function isLiveAskNeededThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedThinkPair(next[prev!.length + 2])
}

/** Ask User 挂上后同一帧首枚 token：过程追加问句行，回答开散文尾 */
export function isLiveAskNeededAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedAnswerPair(next[prev!.length + 2])
}

/** Ask User 挂上后同一帧 think + 首枚 token */
export function isLiveAskNeededThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  return isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3])
}

/** Ask User 挂上后同一帧 think + 下一工具已 complete_call */
export function isLiveAskNeededThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 2])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Ask User 挂上后同一帧首枚 token + 下一工具已 complete_call */
export function isLiveAskNeededAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || !isLiveAddedAnswerPair(next[prev!.length + 2])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** Ask User 挂上后同一帧 ```demo + present_inline_demo */
export function isLiveAskNeededAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || next.length !== prev!.length + 4) return false
  const text = next[prev!.length + 2]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 3])
}

/** Ask User 挂上后同一帧 think + ```demo + present_inline_demo */
export function isLiveAskNeededThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 2])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const text = next[prev!.length + 3]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 4])
}

/** 写盘收束同时 Ask User 挂上并立刻 think：过程 remap 并追加问句行，旁白续尾，回答只换 diff 槽 */
export function isLiveWriteStatAskNeededThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedThinkPair(next[prev!.length + 2])
}

/** 写盘收束同时 Ask User 挂上并立刻首枚 token */
export function isLiveWriteStatAskNeededAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedAnswerPair(next[prev!.length + 2])
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 think */
export function isLiveWriteStatStatusAskNeededThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatStatusPrefix(prev, next) || next.length !== prev!.length + 4) {
    return false
  }
  return isLiveAddedThinkPair(next[prev!.length + 3])
}

/** Ask User 挂上后同一帧 think + 首枚 token + 下一工具已 complete_call */
export function isLiveAskNeededThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 2])) {
    return false
  }
  if (!isLiveAddedAnswerPair(next[prev!.length + 3])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

function hasLiveAskNeededStatusPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  return hasLiveAskNeededHead(next, prev!.length + 1)
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 think */
export function isLiveStatusAskNeededThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || next.length !== prev!.length + 4) return false
  return isLiveAddedThinkPair(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻首枚 token */
export function isLiveStatusAskNeededAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || next.length !== prev!.length + 4) return false
  return isLiveAddedAnswerPair(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 think + 首枚 token */
export function isLiveStatusAskNeededThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || next.length !== prev!.length + 5) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4])
  )
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 ```demo + present_inline_demo */
export function isLiveStatusAskNeededAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || next.length !== prev!.length + 5) return false
  const text = next[prev!.length + 3]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 4])
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 think + ```demo + present_inline_demo */
export function isLiveStatusAskNeededThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 3])) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  const text = next[prev!.length + 4]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 5])
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 think + 下一工具已 complete_call */
export function isLiveStatusAskNeededThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 3])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻首枚 token + 下一工具已 complete_call */
export function isLiveStatusAskNeededAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || !isLiveAddedAnswerPair(next[prev!.length + 3])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时 Ask User 挂上并立刻 think + 首枚 token */
export function isLiveWriteStatAskNeededThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  return (
    isLiveAddedThinkPair(next[prev!.length + 2]) && isLiveAddedAnswerPair(next[prev!.length + 3])
  )
}

/** 写盘收束同时 Ask User 挂上并立刻 think + 下一工具已 complete_call */
export function isLiveWriteStatAskNeededThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 2])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 写盘收束同时 Ask User 挂上并立刻首枚 token + 下一工具已 complete_call */
export function isLiveWriteStatAskNeededAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || !isLiveAddedAnswerPair(next[prev!.length + 2])) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 3, next)
}

/** 写盘收束同时 Ask User 挂上并立刻 ```demo + present_inline_demo */
export function isLiveWriteStatAskNeededAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || next.length !== prev!.length + 4) return false
  const text = next[prev!.length + 2]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 3])
}

/** 写盘收束同时 Ask User 挂上并立刻 think + ```demo + present_inline_demo */
export function isLiveWriteStatAskNeededThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 2])) {
    return false
  }
  if (next.length !== prev!.length + 5) return false
  const text = next[prev!.length + 3]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 4])
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻首枚 token */
export function isLiveWriteStatStatusAskNeededAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatStatusPrefix(prev, next) || next.length !== prev!.length + 4) {
    return false
  }
  return isLiveAddedAnswerPair(next[prev!.length + 3])
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 think + 首枚 token */
export function isLiveWriteStatStatusAskNeededThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatStatusPrefix(prev, next) || next.length !== prev!.length + 5) {
    return false
  }
  return (
    isLiveAddedThinkPair(next[prev!.length + 3]) && isLiveAddedAnswerPair(next[prev!.length + 4])
  )
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 ```demo + present_inline_demo */
export function isLiveWriteStatStatusAskNeededAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatStatusPrefix(prev, next) || next.length !== prev!.length + 5) {
    return false
  }
  const text = next[prev!.length + 3]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 4])
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 think + 下一工具已 complete_call */
export function isLiveWriteStatStatusAskNeededThinkSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    !hasLiveAskNeededWriteStatStatusPrefix(prev, next) ||
    !isLiveAddedThinkPair(next[prev!.length + 3])
  ) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时 Ask User 挂上并立刻 think + 首枚 token + 下一工具已 complete_call */
export function isLiveWriteStatAskNeededThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length + 2])) {
    return false
  }
  if (!isLiveAddedAnswerPair(next[prev!.length + 3])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 think + ```demo + present_inline_demo */
export function isLiveWriteStatStatusAskNeededThinkAnswerDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    !hasLiveAskNeededWriteStatStatusPrefix(prev, next) ||
    !isLiveAddedThinkPair(next[prev!.length + 3])
  ) {
    return false
  }
  if (next.length !== prev!.length + 6) return false
  const text = next[prev!.length + 4]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[prev!.length + 5])
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻首枚 token + 下一工具已 complete_call */
export function isLiveWriteStatStatusAskNeededAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    !hasLiveAskNeededWriteStatStatusPrefix(prev, next) ||
    !isLiveAddedAnswerPair(next[prev!.length + 3])
  ) {
    return false
  }
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 4, next)
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 think + 首枚 token + 下一工具已 complete_call */
export function isLiveWriteStatStatusAskNeededThinkAnswerSettledToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    !hasLiveAskNeededWriteStatStatusPrefix(prev, next) ||
    !isLiveAddedThinkPair(next[prev!.length + 3])
  ) {
    return false
  }
  if (!isLiveAddedAnswerPair(next[prev!.length + 4])) return false
  return isLiveAddedSettledToolsWithOptionalStatus(prev!.length + 5, next)
}

function isLiveAddedCancelledAskTool(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'tool' &&
      segment.status === 'cancelled' &&
      segment.toolName === REQUEST_USER_INPUT_TOOL
  )
}

function isLiveAddedCancelledAskStatus(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'status' &&
      segment.status === 'cancelled' &&
      segment.toolName === REQUEST_USER_INPUT_TOOL
  )
}

function hasLiveAskNeededCancelledHead(
  next: readonly TurnSegment[],
  start: number
): boolean {
  return isLiveAddedCancelledAskTool(next[start]) && isLiveAddedCancelledAskStatus(next[start + 1])
}

/** Stop 后 Ask 工具标 cancelled，Question requested 行已是 done（下一工具已 complete_call 时不跟 Ask 一起 cancelled） */
function hasLiveAskNeededStoppedHead(next: readonly TurnSegment[], start: number): boolean {
  return isLiveAddedCancelledAskTool(next[start]) && isLiveAddedResolvedAskStatus(next[start + 1])
}

/** Ask User 挂上后同一帧 context_compress：过程追加问句行与压缩步 */
export function isLiveAskNeededCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedCompress(next[prev!.length + 2])
}

/** 写盘收束同时 Ask User 挂上并立刻 compress */
export function isLiveWriteStatAskNeededCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return isLiveAddedCompress(next[prev!.length + 2])
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 compress */
export function isLiveWriteStatStatusAskNeededCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededWriteStatStatusPrefix(prev, next) || next.length !== prev!.length + 4) {
    return false
  }
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 compress */
export function isLiveStatusAskNeededCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveAskNeededStatusPrefix(prev, next) || next.length !== prev!.length + 4) return false
  return isLiveAddedCompress(next[prev!.length + 3])
}

/** Ask User 挂上后同一帧 Stop：问句行与工具标 cancelled */
export function isLiveAskNeededCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 2) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length)
}

/** Ask User 挂上后同一帧 think + Stop：问句行 cancelled，思考不进过程 */
export function isLiveAskNeededThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  if (!hasLiveAskNeededCancelledHead(next, prev!.length)) return false
  return isLiveAddedCancelledThink(next[prev!.length + 2])
}

/** 写盘收束同时 Ask User 挂上并立刻 Stop */
export function isLiveWriteStatAskNeededCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length)
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 Stop */
export function isLiveWriteStatStatusAskNeededCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (next.length !== prev!.length + 3) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 1)
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 Stop */
export function isLiveStatusAskNeededCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 3) return false
  return hasLiveAskNeededCancelledHead(next, prev!.length + 1)
}

/** 写盘收束同时 Ask User 挂上并立刻 think + Stop */
export function isLiveWriteStatAskNeededThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  if (!hasLiveAskNeededCancelledHead(next, prev!.length)) return false
  return isLiveAddedCancelledThink(next[prev!.length + 2])
}

/** 写盘收束同时 规划下一步 + Ask User 挂上并立刻 think + Stop */
export function isLiveWriteStatStatusAskNeededThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededCancelledHead(next, prev!.length + 1)) return false
  return isLiveAddedCancelledThink(next[prev!.length + 3])
}

/** 规划下一步 / Reconnecting 后同一帧 Ask User 挂上并立刻 think + Stop */
export function isLiveStatusAskNeededThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (next.length !== prev!.length + 4) return false
  if (!hasLiveAskNeededCancelledHead(next, prev!.length + 1)) return false
  return isLiveAddedCancelledThink(next[prev!.length + 3])
}

function isLiveCancelRetarget(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.id !== next.id || prev.kind !== next.kind) return false
  if (next.status !== 'cancelled') return false
  if (prev.status === 'active') {
    if (prev.kind !== 'tool') return (prev.content ?? '') === (next.content ?? '')
    return (
      prev.toolName === next.toolName &&
      prev.toolArgs === next.toolArgs &&
      prev.fileDiff === next.fileDiff &&
      prev.fileDiffs === next.fileDiffs &&
      prev.editPreview === next.editPreview
    )
  }
  return prev.kind === 'tool' && prev.status === 'error' && prev.toolName === next.toolName
}

/** Stop：多条 active 收成 cancelled，只换这些过程步（对标 Codex You stopped after / preserved streamed activity，不复制 Stop 失败卡住） */
export function isLiveCancelChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let cancelled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLiveCancelRetarget(before, after)) return false
    cancelled += 1
  }
  return cancelled >= 1
}

function isLiveAddedCancelledStatus(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && segment.kind === 'status' && segment.status === 'cancelled')
}

function isLiveAddedCancelledThink(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && segment.kind === 'thinking' && segment.status === 'cancelled')
}

function hasLiveCancelPrefixClose(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length < prev.length) return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (
      isLivePrefixClose(before, after) ||
      isLiveCancelRetarget(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return true
}

function hasLiveWriteStatCancelPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length <= prev.length) return false
  let writeStats = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveToolWriteStatChange(before, after)) {
      writeStats += 1
      continue
    }
    if (
      isLivePrefixClose(before, after) ||
      isLiveCancelRetarget(before, after) ||
      isLiveTextGrowClose(before, after) ||
      isLiveThinkGrowClose(before, after)
    ) {
      continue
    }
    return false
  }
  return writeStats === 1
}

/** 规划下一步后同一帧 Stop：status 可先挂上再标 cancelled，只追加该行 */
export function isLiveStatusCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedCancelledStatus(next[next.length - 1])
}

/** 规划下一步已在场时 think 后 Stop：旁白可先挂上再标 cancelled，思考不进过程 */
export function isLiveThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedCancelledThink(next[next.length - 1])
}

/** 规划下一步 + think + Stop 同一帧：过程追加 cancelled status，思考不进过程 */
export function isLiveStatusThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveCancelPrefixClose(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedCancelledStatus(next[prev!.length]) &&
    isLiveAddedCancelledThink(next[prev!.length + 1])
  )
}

/** 写盘收束同时新开 cancelled status：过程 remap 并追加该行，回答只换 diff 槽 */
export function isLiveWriteStatStatusCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatCancelPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedCancelledStatus(next[next.length - 1])
}

/** 写盘收束同时新开 cancelled 思考：过程 remap，旁白续尾 */
export function isLiveWriteStatThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatCancelPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedCancelledThink(next[next.length - 1])
}

/** 写盘收束同时新开 cancelled status + 思考：过程 remap 并追加 status */
export function isLiveWriteStatStatusThinkCancelAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatCancelPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedCancelledStatus(next[prev!.length]) &&
    isLiveAddedCancelledThink(next[prev!.length + 1])
  )
}

function isLiveErrorAnswer(segment: TurnSegment): boolean {
  return segment.kind === 'text' && (segment.content ?? '').includes('**错误**:')
}

/** 错误：收口 status/think/无新写盘的工具后追加错误正文，或就地封回答尾（对标 Codex 直播错误仍留已画过程；写盘 +/- 仍走 write-stat） */
export function isLiveErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev) return false
  if (next.length === prev.length + 1) {
    const added = next[next.length - 1]
    if (!added || !isLiveErrorAnswer(added) || added.status !== 'done') return false
    for (let i = 0; i < prev.length; i++) {
      const before = prev[i]
      const after = next[i]
      if (!before || !after) return false
      if (before === after) continue
      if (!isLiveThinkAnswerPrefixClose(before, after)) return false
    }
    return true
  }
  if (next.length !== prev.length) return false
  const last = next.length - 1
  for (let i = 0; i < last; i++) {
    if (prev[i] !== next[i]) return false
  }
  const from = prev[last]
  const to = next[last]
  if (!from || !to || from === to || from.id !== to.id) return false
  if (from.kind !== 'text' || to.kind !== 'text') return false
  if (from.status !== 'active' || to.status !== 'done') return false
  return isLiveErrorAnswer(to) && (to.content ?? '').startsWith(from.content ?? '')
}

export function findLiveDemoFenceChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): { from: TurnSegment; to: TurnSegment } | null {
  if (!prev || prev.length !== next.length) return null
  const last = next.length - 1
  for (let i = 0; i < last; i++) {
    if (prev[i] !== next[i]) return null
  }
  const from = prev[last]
  const to = next[last]
  if (!from || !to || from === to) return null
  if (!isLiveAnswerText(from) || !isLiveAnswerText(to) || from.id !== to.id) return null
  if (!hasStreamingDemoFence(to.content ?? '')) return null
  if ((from.content ?? '') === (to.content ?? '') && from.status === to.status) return null
  return { from, to }
}

/** 前缀没变或只收束思考/status/散文/无新写盘的工具、末尾新开演示：过程不追加、回答只开演示槽 */
export function isLiveDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length !== prev.length + 1) return false
  const added = next[next.length - 1]
  if (
    !added ||
    added.kind !== 'tool' ||
    added.status !== 'active' ||
    added.toolName !== 'present_inline_demo'
  ) {
    return false
  }
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLivePrefixClose(before, after)) return false
  }
  return true
}

function isLiveDemoSegment(segment: TurnSegment): boolean {
  return segment.kind === 'tool' && segment.toolName === 'present_inline_demo'
}

/** 同一演示只改 HTML / 说明 / 收束：只换该槽，不重拆过程 / buildAnswerParts */
export function isLiveDemoHtmlChange(prev: TurnSegment, next: TurnSegment): boolean {
  if (!isLiveDemoSegment(prev) || !isLiveDemoSegment(next)) return false
  if (prev.id !== next.id) return false
  if (!isLiveToolStatusHoldOrSettle(prev, next)) return false
  return (
    prev.content !== next.content ||
    prev.toolDetail !== next.toolDetail ||
    prev.toolArgs !== next.toolArgs ||
    prev.status !== next.status
  )
}

export function findLiveDemoHtmlChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): { from: TurnSegment; to: TurnSegment } | null {
  if (!prev || prev.length !== next.length) return null
  let found: { from: TurnSegment; to: TurnSegment } | null = null
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (before === after) continue
    if (!before || !after || !isLiveDemoHtmlChange(before, after)) return null
    if (found) return null
    found = { from: before, to: after }
  }
  return found
}

/** 同一列表里只有一个工具就地改详情或收束：找出该对，供非末步 complete_call */
export function findLiveToolInPlaceChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): { from: TurnSegment; to: TurnSegment } | null {
  if (!prev || prev.length !== next.length) return null
  let found: { from: TurnSegment; to: TurnSegment } | null = null
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (before === after) continue
    if (!before || !after) return null
    if (!isLiveToolMetaOnlyChange(before, after) && !isLiveToolSettleChange(before, after)) {
      return null
    }
    if (found) return null
    found = { from: before, to: after }
  }
  return found
}

/** 同一帧里多条只读工具收束且没新写盘：只换这些步（对标 Codex 并行 complete_call / Promise.all tool_done，不发明 Exploring 分组格） */
export function isLiveMultiToolSettleChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || prev.length !== next.length) return false
  let settled = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!isLiveToolSettleChange(before, after)) return false
    settled += 1
  }
  return settled >= 2
}

function isLiveToolStatusHoldOrSettle(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.status === next.status) return true
  if (prev.status !== 'active') return false
  return next.status === 'done' || next.status === 'error' || next.status === 'cancelled'
}

/** 同一工具只改写盘 +/- / 参数，或收束时带上核实 diff：就地换该步；回答只换该工具的 diff 槽（对标 Codex Edited 格 / ~0.5s，不复制 #38695） */
export function isLiveToolWriteStatChange(prev: TurnSegment, next: TurnSegment): boolean {
  if (prev.kind !== 'tool' || next.kind !== 'tool') return false
  if (prev.id !== next.id || prev.toolName !== next.toolName) return false
  if (!isLiveToolStatusHoldOrSettle(prev, next)) return false
  return (
    prev.toolArgs !== next.toolArgs ||
    prev.fileDiff !== next.fileDiff ||
    prev.fileDiffs !== next.fileDiffs ||
    prev.editPreview !== next.editPreview
  )
}

export function findLiveToolWriteStatChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): { from: TurnSegment; to: TurnSegment } | null {
  if (!prev || prev.length !== next.length) return null
  let found: { from: TurnSegment; to: TurnSegment } | null = null
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (before === after) continue
    if (!before || !after || !isLiveToolWriteStatChange(before, after)) return null
    if (found) return null
    found = { from: before, to: after }
  }
  return found
}

function hasLiveWriteStatPrefix(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!prev || next.length <= prev.length) return false
  let writeStats = 0
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (isLiveToolWriteStatChange(before, after)) {
      writeStats += 1
      continue
    }
    if (!isLivePrefixClose(before, after)) return false
  }
  return writeStats === 1
}

/** 一条写盘 +/- 收束，同时末尾新开一或多个工具，可带一条 Awaiting / Question requested 行：过程 remap + 追加，回答只换 diff 槽（对标 Codex ~0.5s / add_call / Awaiting approval，不复制 #38695） */
export function isLiveToolWriteStatAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  return hasLiveWriteStatPrefix(prev, next) && isLiveAddedToolsWithOptionalStatus(prev!.length, next)
}

/** 写盘收束同时新开 status + 工具：过程 remap 并追加这些步，回答只换 diff 槽 */
export function isLiveWriteStatStatusToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 写盘收束同时新开思考 + 工具：过程 remap 并追加工具，旁白续尾，回答只换 diff 槽 */
export function isLiveWriteStatThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 1, next)
}

/** 写盘收束同时新开 status + 思考 + 工具：过程 remap 并追加 status 与工具，旁白续尾 */
export function isLiveWriteStatStatusThinkToolAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return isLiveAddedToolsWithOptionalStatus(prev!.length + 2, next)
}

/** 写盘收束同时新开 status：过程 remap + 追加，回答只换 diff 槽（对标 规划下一步 / Reconnecting... n/5） */
export function isLiveWriteStatStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) return false
  const added = next[next.length - 1]
  return Boolean(added && added.kind === 'status' && added.status === 'active')
}

/** 写盘收束同时新开思考：过程 remap，旁白续尾，回答只换 diff 槽（不复制 #24850） */
export function isLiveWriteStatThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) return false
  const added = next[next.length - 1]
  return Boolean(added && added.kind === 'thinking' && added.status === 'active')
}

/** 写盘收束同时新开散文：过程 remap，回答开尾并重拆（对标 ~0.5s / 工具后首枚 token） */
export function isLiveWriteStatAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) return false
  const added = next[next.length - 1]
  if (!added || !isLiveAnswerText(added) || added.status === 'done') return false
  return !hasStreamingDemoFence(added.content ?? '')
}

/** 写盘收束同时新开 ```demo：过程 remap，回答开演示槽并重拆 */
export function isLiveWriteStatDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) return false
  const added = next[next.length - 1]
  if (!added || !isLiveAnswerText(added) || added.status === 'done') return false
  return hasStreamingDemoFence(added.content ?? '')
}

function isLiveAddedCompress(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && segment.kind === 'tool' && segment.toolName === COMPRESS_TOOL)
}

/** 写盘收束同时新开已完成 compress：过程 remap + 追加，回答只换 diff 槽（对标 contextCompaction 紧跟 Edited） */
export function isLiveWriteStatCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedCompress(next[next.length - 1])
}

/** 规划下一步后同一帧 compress：status 可先标 done，过程追加该行与压缩步 */
export function isLiveStatusCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  return next.length === prev!.length + 2 && isLiveAddedCompress(next[next.length - 1])
}

/** 规划下一步已在场时 think 后 compress：旁白可仍在，过程追加工具、思考不进过程 */
export function isLiveThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return next.length === prev!.length + 2 && isLiveAddedCompress(next[next.length - 1])
}

/** 规划下一步 + think + compress 同一帧：过程追加 status 与压缩步 */
export function isLiveStatusThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return next.length === prev!.length + 3 && isLiveAddedCompress(next[next.length - 1])
}

/** 写盘收束同时新开 status + compress：过程 remap 并追加这两步，回答只换 diff 槽 */
export function isLiveWriteStatStatusCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  return next.length === prev!.length + 2 && isLiveAddedCompress(next[next.length - 1])
}

/** 写盘收束同时新开思考 + compress：过程 remap 并追加压缩步，旁白续尾 */
export function isLiveWriteStatThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) return false
  return next.length === prev!.length + 2 && isLiveAddedCompress(next[next.length - 1])
}

/** 写盘收束同时新开 status + 思考 + compress：过程 remap 并追加 status 与压缩步 */
export function isLiveWriteStatStatusThinkCompressAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return next.length === prev!.length + 3 && isLiveAddedCompress(next[next.length - 1])
}

function isLiveAddedError(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && isLiveErrorAnswer(segment) && segment.status === 'done')
}

/** 写盘收束同时新开错误正文：过程 remap，错误只进回答（不复制 Stop / 错误卡住） */
export function isLiveWriteStatErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedError(next[next.length - 1])
}

/** 规划下一步后同一帧错误：status 可先标 done，过程追加该行，错误只进回答 */
export function isLiveStatusErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  return next.length === prev!.length + 2 && isLiveAddedError(next[next.length - 1])
}

/** 规划下一步已在场时 think 后错误：旁白可仍在，错误只进回答 */
export function isLiveThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return next.length === prev!.length + 2 && isLiveAddedError(next[next.length - 1])
}

/** 规划下一步 + think + 错误同一帧：过程追加 status，思考不进过程，错误只进回答 */
export function isLiveStatusThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return next.length === prev!.length + 3 && isLiveAddedError(next[next.length - 1])
}

/** 写盘收束同时新开 status + 错误：过程 remap 并追加 status，错误只进回答 */
export function isLiveWriteStatStatusErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  return next.length === prev!.length + 2 && isLiveAddedError(next[next.length - 1])
}

/** 写盘收束同时新开思考 + 错误：过程 remap，旁白续尾，错误只进回答 */
export function isLiveWriteStatThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) return false
  return next.length === prev!.length + 2 && isLiveAddedError(next[next.length - 1])
}

/** 写盘收束同时新开 status + 思考 + 错误：过程 remap 并追加 status */
export function isLiveWriteStatStatusThinkErrorAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return next.length === prev!.length + 3 && isLiveAddedError(next[next.length - 1])
}

function isLiveAddedDemoFencePair(next: readonly TurnSegment[], start: number): boolean {
  const text = next[start]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  return isLiveAddedInlineDemo(next[start + 1])
}


function isLiveAddedDoneDemoCancelledToolPair(next: readonly TurnSegment[], start: number): boolean {
  const text = next[start]
  if (!text || text.kind !== 'text' || !hasStreamingDemoFence(text.content ?? '')) return false
  const demo = next[start + 1]
  return Boolean(
    demo &&
      demo.kind === 'tool' &&
      demo.status === 'cancelled' &&
      demo.toolName === 'present_inline_demo'
  )
}

function isLiveAddedCancelledDemoFencePair(next: readonly TurnSegment[], start: number): boolean {
  const text = next[start]
  if (
    !text ||
    text.kind !== 'text' ||
    text.status !== 'cancelled' ||
    !hasStreamingDemoFence(text.content ?? '')
  ) {
    return false
  }
  const demo = next[start + 1]
  return Boolean(
    demo &&
      demo.kind === 'tool' &&
      demo.status === 'cancelled' &&
      demo.toolName === 'present_inline_demo'
  )
}

function isLiveAddedInlineDemo(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'tool' &&
      segment.status === 'active' &&
      segment.toolName === 'present_inline_demo'
  )
}

/** 写盘收束同时新开 present_inline_demo：过程 remap 不开演示步，回答开槽并重拆 */
export function isLiveWriteStatDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 1) return false
  return isLiveAddedInlineDemo(next[next.length - 1])
}

/** 规划下一步后同一帧 present_inline_demo：status 可先标 done，过程追加该行、回答开演示槽 */
export function isLiveStatusDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  return next.length === prev!.length + 2 && isLiveAddedInlineDemo(next[next.length - 1])
}

/** 规划下一步已在场时 think 后 present_inline_demo：旁白可先标 done，已画散文也可被收口 */
export function isLiveThinkDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) {
    return false
  }
  return next.length === prev!.length + 2 && isLiveAddedInlineDemo(next[next.length - 1])
}

/** 规划下一步 + think + present_inline_demo 同一帧：过程追加 status，思考不进过程 */
export function isLiveStatusThinkDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) {
    return false
  }
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return next.length === prev!.length + 3 && isLiveAddedInlineDemo(next[next.length - 1])
}

/** 写盘收束同时新开 status + present_inline_demo：过程 remap 并追加 status，回答开演示槽 */
export function isLiveWriteStatStatusDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  return next.length === prev!.length + 2 && isLiveAddedInlineDemo(next[next.length - 1])
}

/** 写盘收束同时新开思考 + present_inline_demo：过程 remap 不追加演示步，旁白续尾 */
export function isLiveWriteStatThinkDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedThinkPair(next[prev!.length])) return false
  return next.length === prev!.length + 2 && isLiveAddedInlineDemo(next[next.length - 1])
}

/** 写盘收束同时新开 status + 思考 + present_inline_demo：过程 remap 并追加 status */
export function isLiveWriteStatStatusThinkDemoAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || !isLiveAddedStatusPair(next[prev!.length])) return false
  if (!isLiveAddedThinkPair(next[prev!.length + 1])) return false
  return next.length === prev!.length + 3 && isLiveAddedInlineDemo(next[next.length - 1])
}

function isLiveAddedActiveStatus(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && segment.kind === 'status' && segment.status === 'active')
}

/** 无思考时首枚 token 会先把规划下一步标 done 再开散文 / ```demo */
function isLiveAddedStatusPair(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'status' &&
      (segment.status === 'active' || segment.status === 'done')
  )
}

function isLiveAddedActiveThink(segment: TurnSegment | undefined): boolean {
  return Boolean(segment && segment.kind === 'thinking' && segment.status === 'active')
}

/** 同一 16ms 里 think 后首枚 token 会先把旁白标 done 再开散文 */
function isLiveAddedThinkPair(segment: TurnSegment | undefined): boolean {
  return Boolean(
    segment &&
      segment.kind === 'thinking' &&
      (segment.status === 'active' || segment.status === 'done')
  )
}

function isLiveAddedActiveAnswer(segment: TurnSegment | undefined): boolean {
  if (!segment || !isLiveAnswerText(segment) || segment.status === 'done') return false
  return !hasStreamingDemoFence(segment.content ?? '')
}

function isLiveAddedDemoFence(segment: TurnSegment | undefined): boolean {
  if (!segment || !isLiveAnswerText(segment) || segment.status === 'done') return false
  return hasStreamingDemoFence(segment.content ?? '')
}

function hasLivePrefixClose(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[],
  close: (before: TurnSegment, after: TurnSegment) => boolean
): boolean {
  if (!prev || next.length < prev.length) return false
  for (let i = 0; i < prev.length; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after) return false
    if (before === after) continue
    if (!close(before, after)) return false
  }
  return true
}

/** 无新写盘收束同时新开 status + 思考：过程 remap 并追加 status，旁白续尾（对标 规划下一步后首枚 think） */
export function isLiveStatusThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLivePrefixClose(prev, next, isLivePrefixClose) || next.length !== prev!.length + 2) {
    return false
  }
  return (
    isLiveAddedActiveStatus(next[prev!.length]) && isLiveAddedActiveThink(next[prev!.length + 1])
  )
}

/** 规划下一步已在场时 think 后 Question requested / Awaiting：旁白续尾，追加 status（对标 Ask User / Awaiting approval，不发明 TUI Questions n/n） */
export function isLiveThinkStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAskOrAwaitingStatus(next[prev!.length + 1])
  )
}

/** 规划下一步 + think + Ask User / Awaiting 同一帧：过程 remap 并追加两条 status，思考不进过程 */
export function isLiveStatusThinkStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveToolAppendPrefixClose(prev, next) || next.length !== prev!.length + 3) return false
  return (
    isLiveAddedStatusPair(next[prev!.length]) &&
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedAskOrAwaitingStatus(next[prev!.length + 2])
  )
}

/** 写盘收束同时新开思考 + Question requested / Awaiting：过程 remap 并追加 status，旁白续尾，回答只换 diff 槽 */
export function isLiveWriteStatThinkStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedAskOrAwaitingStatus(next[prev!.length + 1])
  )
}

/** 写盘收束同时新开 status + 思考 + Ask User / Awaiting：过程 remap 并追加两条 status，旁白续尾 */
export function isLiveWriteStatStatusThinkStatusAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return (
    isLiveAddedStatusPair(next[prev!.length]) &&
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedAskOrAwaitingStatus(next[prev!.length + 2])
  )
}

/** 无新写盘收束同时新开思考 + 散文：过程 remap，旁白续尾，回答开尾 */
export function isLiveThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    !hasLivePrefixClose(prev, next, isLiveThinkAnswerPrefixClose) ||
    next.length !== prev!.length + 2
  ) {
    return false
  }
  return (
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedActiveAnswer(next[prev!.length + 1])
  )
}

/** 无新写盘收束同时新开 status + 思考 + 散文：过程 remap 并追加 status，旁白续尾，回答开尾 */
export function isLiveStatusThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLivePrefixClose(prev, next, isLivePrefixClose) || next.length !== prev!.length + 3) {
    return false
  }
  return (
    isLiveAddedActiveStatus(next[prev!.length]) &&
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedActiveAnswer(next[prev!.length + 2])
  )
}

/** 无新写盘收束同时新开思考 + ```demo：过程 remap，旁白续尾，回答开演示槽 */
export function isLiveThinkDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (
    !hasLivePrefixClose(prev, next, isLiveThinkAnswerPrefixClose) ||
    next.length !== prev!.length + 2
  ) {
    return false
  }
  return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFence(next[prev!.length + 1])
}

/** 无新写盘收束同时新开 status + ```demo：过程 remap 并追加 status，回答开演示槽 */
export function isLiveStatusDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLivePrefixClose(prev, next, isLivePrefixClose) || next.length !== prev!.length + 2) {
    return false
  }
  return (
    isLiveAddedStatusPair(next[prev!.length]) && isLiveAddedDemoFence(next[prev!.length + 1])
  )
}

/** 无新写盘收束同时新开 status + 思考 + ```demo：过程 remap 并追加 status，旁白续尾，回答开演示槽 */
export function isLiveStatusThinkDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLivePrefixClose(prev, next, isLivePrefixClose) || next.length !== prev!.length + 3) {
    return false
  }
  return (
    isLiveAddedActiveStatus(next[prev!.length]) &&
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedDemoFence(next[prev!.length + 2])
  )
}

/** 无新写盘收束同时新开 status + 散文：过程 remap 并追加 status，回答开尾 */
export function isLiveStatusAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLivePrefixClose(prev, next, isLivePrefixClose) || next.length !== prev!.length + 2) {
    return false
  }
  return (
    isLiveAddedStatusPair(next[prev!.length]) && isLiveAddedActiveAnswer(next[prev!.length + 1])
  )
}

/** 写盘收束同时新开 status + 思考：过程 remap 并追加 status，旁白续尾，回答只换 diff 槽（对标 规划下一步后首枚 think） */
export function isLiveWriteStatStatusThinkAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedActiveStatus(next[prev!.length]) && isLiveAddedActiveThink(next[prev!.length + 1])
  )
}

/** 写盘收束同时新开思考 + 散文：过程 remap，旁白续尾，回答开尾并只换 diff 槽 */
export function isLiveWriteStatThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedActiveAnswer(next[prev!.length + 1])
  )
}

/** 写盘收束同时新开 status + 思考 + 散文：过程 remap 并追加 status，旁白续尾，回答开尾并只换 diff 槽 */
export function isLiveWriteStatStatusThinkAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return (
    isLiveAddedActiveStatus(next[prev!.length]) &&
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedActiveAnswer(next[prev!.length + 2])
  )
}

/** 写盘收束同时新开思考 + ```demo：过程 remap，旁白续尾，回答开演示槽并只换 diff 槽 */
export function isLiveWriteStatThinkDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return isLiveAddedThinkPair(next[prev!.length]) && isLiveAddedDemoFence(next[prev!.length + 1])
}

/** 写盘收束同时新开 status + ```demo：过程 remap 并追加 status，回答开演示槽并只换 diff 槽 */
export function isLiveWriteStatStatusDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedStatusPair(next[prev!.length]) && isLiveAddedDemoFence(next[prev!.length + 1])
  )
}

/** 写盘收束同时新开 status + 思考 + ```demo：过程 remap 并追加 status，旁白续尾，回答开演示槽并只换 diff 槽 */
export function isLiveWriteStatStatusThinkDemoFenceAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 3) return false
  return (
    isLiveAddedActiveStatus(next[prev!.length]) &&
    isLiveAddedThinkPair(next[prev!.length + 1]) &&
    isLiveAddedDemoFence(next[prev!.length + 2])
  )
}

/** 写盘收束同时新开 status + 散文：过程 remap 并追加 status，回答开尾并只换 diff 槽 */
export function isLiveWriteStatStatusAnswerAppendChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): boolean {
  if (!hasLiveWriteStatPrefix(prev, next) || next.length !== prev!.length + 2) return false
  return (
    isLiveAddedStatusPair(next[prev!.length]) && isLiveAddedActiveAnswer(next[prev!.length + 1])
  )
}

export function findLiveToolRetargetChange(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): { from: TurnSegment; to: TurnSegment } | null {
  return findLiveToolInPlaceChange(prev, next) ?? findLiveToolWriteStatChange(prev, next)
}

/** 同一工具只把详情换成命令末行：过程切片保持原数组，不抬 TurnFlow */
export function isLiveLastLineOnlyToolChange(prev: TurnSegment, next: TurnSegment): boolean {
  if (!isLiveToolMetaOnlyChange(prev, next)) return false
  if ((prev.toolDetail ?? '') === (next.toolDetail ?? '')) return false
  return !isLiveStableToolDetail(next.toolDetail)
}

/** 心跳同一数组或只换命令末行：16ms flush 不发 store（对标 Codex #19260 / #22860） */
export function shouldSkipLiveStreamPublish(
  prevSegments: readonly TurnSegment[] | null | undefined,
  segments: readonly TurnSegment[]
): boolean {
  if (prevSegments === segments) return true
  if (!prevSegments || prevSegments.length !== segments.length) return false
  if (sameRefList(prevSegments, segments)) return true
  if (shouldSkipLiveStreamDerivation(prevSegments, segments) !== 'tool') return false
  const change = findLiveToolInPlaceChange(prevSegments, segments)
  return Boolean(change && isLiveLastLineOnlyToolChange(change.from, change.to))
}

export function shouldSkipLiveStreamDerivation(
  prevSegments: readonly TurnSegment[] | null | undefined,
  segments: readonly TurnSegment[]
): LiveStreamDerivationSkip | null {
  if (!prevSegments) return null
  if (isLiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveThinkAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusThinkAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatThinkAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusThinkAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveToolWriteStatAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveThinkStatusAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusThinkStatusAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatThinkStatusAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusThinkStatusAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAppendChange(prevSegments, segments)) return 'status'
  if (isLiveWriteStatStatusThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveWriteStatStatusAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveWriteStatThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveThinkDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusThinkDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatThinkDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusThinkDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveThinkDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusThinkDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatThinkDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusThinkDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveCancelChange(prevSegments, segments)) {
    return segments.some((segment, index) => {
      const before = prevSegments[index]
      return Boolean(before && before !== segment && segment.kind === 'text')
    })
      ? 'text'
      : 'tool'
  }
  if (
    isLiveStatusCancelAppendChange(prevSegments, segments) ||
    isLiveThinkCancelAppendChange(prevSegments, segments) ||
    isLiveStatusThinkCancelAppendChange(prevSegments, segments)
  ) {
    return segments.some((segment, index) => {
      const before = prevSegments[index]
      return Boolean(before && before !== segment && segment.kind === 'text')
    })
      ? 'text'
      : 'tool'
  }
  if (isLiveWriteStatStatusCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusAppendChange(prevSegments, segments)) return 'status'
  if (isLiveThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveDemoFenceAppendChange(prevSegments, segments)) return 'text'
  if (findLiveDemoFenceChange(prevSegments, segments)) return 'text'
  if (isLiveDemoAppendChange(prevSegments, segments)) return 'tool'
  if (findLiveDemoHtmlChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveApprovalNeededAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalNeededCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalNeededAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedCancelChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedSettleChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedStatusAppendChange(prevSegments, segments)) return 'status'
  if (isLiveApprovalDeniedToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveApprovalDeniedThinkAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedThinkAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedThinkAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedThinkAnswerCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalDeniedThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedWriteStatChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedWriteStatStatusAppendChange(prevSegments, segments)) return 'status'
  if (isLiveApprovalAllowedWriteStatToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedWriteStatThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveApprovalAllowedWriteStatAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedWriteStatThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedWriteStatThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedWriteStatAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedWriteStatThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveApprovalAllowedWriteStatAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedWriteStatThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedAnswerCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalResolvedThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalDeniedSettleCancelChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedSettleAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedSettleErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedSettleThinkErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedSettleAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedCancelChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorCancelAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorCompressAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerCancelAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoCancelAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments)) {
    return 'tool'
  }
  if (isLiveWriteStatStatusApprovalResolvedToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusApprovalResolvedAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedErrorCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatApprovalResolvedAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatApprovalResolvedThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedThinkErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedErrorCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusApprovalResolvedAnswerCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedErrorAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusApprovalResolvedThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedSettleChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedStatusAppendChange(prevSegments, segments)) return 'status'
  if (isLiveApprovalAllowedToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveApprovalAllowedAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedThinkAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveApprovalAllowedAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalAllowedThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveApprovalResolvedChange(prevSegments, segments)) return 'tool'
  if (isLiveUserInputNeededChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedSettleChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerCancelCompressAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskResolvedAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskResolvedThinkAnswerCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskResolvedErrorCancelAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskResolvedAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerAskActiveToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerAskToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoErrorAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedErrorAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkErrorAnswerDemoCancelCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedThinkToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedAnswerDemoCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskResolvedToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskNeededThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveAskNeededAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskNeededThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskNeededThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskNeededAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveAskNeededAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskNeededThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAskNeededThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveWriteStatAskNeededAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatAskNeededThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatAskNeededThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAskNeededAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatAskNeededAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAskNeededThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAskNeededThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveWriteStatStatusAskNeededAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusAskNeededThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusAskNeededAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAskNeededThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusAskNeededThinkAppendChange(prevSegments, segments)) return 'think'
  if (isLiveStatusAskNeededAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusAskNeededThinkAnswerAppendChange(prevSegments, segments)) return 'text'
  if (isLiveStatusAskNeededAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusAskNeededThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusAskNeededThinkSettledToolAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusAskNeededAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusAskNeededThinkAnswerDemoAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAskNeededAnswerSettledToolAppendChange(prevSegments, segments)) return 'text'
  if (isLiveWriteStatStatusAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
    return 'text'
  }
  if (isLiveAskNeededCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAskNeededCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAskNeededCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusAskNeededCompressAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskNeededCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveAskNeededThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAskNeededCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAskNeededCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusAskNeededCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatAskNeededThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveWriteStatStatusAskNeededThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusAskNeededThinkCancelAppendChange(prevSegments, segments)) return 'tool'
  if (isLiveStatusSettleChange(prevSegments, segments)) return 'status'
  if (findLiveToolInPlaceChange(prevSegments, segments)) return 'tool'
  if (isLiveMultiToolSettleChange(prevSegments, segments)) return 'tool'
  if (findLiveToolWriteStatChange(prevSegments, segments)) return 'tool'
  if (prevSegments.length !== segments.length) return null
  const last = segments.length - 1
  for (let i = 0; i < last; i++) {
    if (prevSegments[i] !== segments[i]) return null
  }
  const prevTail = prevSegments[last]
  const nextTail = segments[last]
  if (!prevTail || !nextTail) return null
  if (prevTail.id !== nextTail.id || prevTail.kind !== nextTail.kind) return null
  if (prevTail !== nextTail && prevTail.status !== nextTail.status) {
    return isLiveToolSettleChange(prevTail, nextTail) ? 'tool' : null
  }
  if (isLiveThinking(nextTail)) return 'think'
  if (isLiveStatus(nextTail)) return 'status'
  if (isLiveToolMetaOnlyChange(prevTail, nextTail)) return 'tool'
  if (
    isLiveAnswerText(nextTail) &&
    !hasStreamingDemoFenceGrowth(prevTail.content ?? '', nextTail.content ?? '')
  ) {
    return 'text'
  }
  return null
}

/** 前缀引用没变时只续思考尾，不 `filter` 全段 */
export function nextLiveThinkText(
  prev: string,
  prevSegments: readonly TurnSegment[] | null,
  segments: readonly TurnSegment[]
): string {
  if (
    isLiveThinkAppendChange(prevSegments, segments) ||
    isLiveWriteStatThinkAppendChange(prevSegments, segments) ||
    isLiveStatusThinkAppendChange(prevSegments, segments) ||
    isLiveWriteStatStatusThinkAppendChange(prevSegments, segments)
  ) {
    return prev + (segments[segments.length - 1]?.content ?? '')
  }
  if (
    prevSegments &&
    (isLiveApprovalResolvedThinkAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorCancelAppendChange(prevSegments, segments))
  ) {
    const think = segments
      .slice(prevSegments.length)
      .find((segment) => segment.kind === 'thinking')
    return prev + (think?.content ?? '')
  }
  if (
    prevSegments &&
    (isLiveThinkToolAppendChange(prevSegments, segments) ||
      isLiveThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedThinkAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedWriteStatThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkToolAppendChange(prevSegments, segments) ||
      isLiveThinkStatusAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkStatusAppendChange(prevSegments, segments))
  ) {
    return prev + (segments[prevSegments.length]?.content ?? '')
  }
  if (
    prevSegments &&
    (isLiveWriteStatThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkDemoFenceAppendChange(prevSegments, segments) ||
      isLiveThinkDemoFenceAppendChange(prevSegments, segments) ||
      isLiveThinkDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkDemoAppendChange(prevSegments, segments) ||
      isLiveThinkErrorAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkErrorAppendChange(prevSegments, segments) ||
      isLiveThinkCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkCompressAppendChange(prevSegments, segments) ||
      isLiveThinkCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatThinkCancelAppendChange(prevSegments, segments))
  ) {
    return prev + (segments[prevSegments.length]?.content ?? '')
  }
  if (
    prevSegments &&
    (isLiveStatusThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusThinkDemoFenceAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkDemoFenceAppendChange(prevSegments, segments) ||
      isLiveStatusThinkToolAppendChange(prevSegments, segments) ||
      isLiveStatusThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveStatusThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkToolAppendChange(prevSegments, segments) ||
      isLiveStatusThinkDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkDemoAppendChange(prevSegments, segments) ||
      isLiveStatusThinkErrorAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkErrorAppendChange(prevSegments, segments) ||
      isLiveStatusThinkCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkCompressAppendChange(prevSegments, segments) ||
      isLiveStatusThinkCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkCancelAppendChange(prevSegments, segments) ||
      isLiveStatusThinkStatusAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusThinkStatusAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkCompressAppendChange(prevSegments, segments))
  ) {
    return prev + (segments[prevSegments.length + 1]?.content ?? '')
  }
  if (
    prevSegments &&
    (isLiveAskNeededThinkAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatAskNeededThinkAppendChange(prevSegments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveWriteStatAskNeededThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatAskNeededThinkCancelAppendChange(prevSegments, segments))
  ) {
    return prev + (segments[prevSegments.length + 2]?.content ?? '')
  }
  if (
    prevSegments &&
    (isLiveWriteStatStatusAskNeededThinkAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusAskNeededThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusAskNeededThinkCancelAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkCancelAppendChange(prevSegments, segments))
  ) {
    return prev + (segments[prevSegments.length + 3]?.content ?? '')
  }
  if (prevSegments && shouldSkipLiveStreamDerivation(prevSegments, segments)) {
    return nextLiveThinkTextOnPrefixChange(prev, prevSegments, segments)
  }
  if (!prevSegments || prevSegments.length !== segments.length) return liveThinkingText(segments)
  const last = segments.length - 1
  for (let i = 0; i < last; i++) {
    if (prevSegments[i] !== segments[i]) return liveThinkingText(segments)
  }
  const prevTail = prevSegments[last]
  const nextTail = segments[last]
  if (!prevTail || !nextTail) return liveThinkingText(segments)
  if (prevTail === nextTail) return prev
  if (isLiveAnswerText(prevTail) && isLiveAnswerText(nextTail) && prevTail.id === nextTail.id) {
    return prev
  }
  if (isLiveThinking(prevTail) && isLiveThinking(nextTail) && prevTail.id === nextTail.id) {
    const prevContent = prevTail.content ?? ''
    const nextContent = nextTail.content ?? ''
    if (nextContent === prevContent) return prev
    if (nextContent.startsWith(prevContent) && (prev === prevContent || prev.endsWith(prevContent))) {
      return prev + nextContent.slice(prevContent.length)
    }
  }
  return liveThinkingText(segments)
}

function nextLiveThinkTextOnPrefixChange(
  prev: string,
  prevSegments: readonly TurnSegment[],
  segments: readonly TurnSegment[]
): string {
  let next = prev
  const n = Math.min(prevSegments.length, segments.length)
  for (let i = 0; i < n; i++) {
    const before = prevSegments[i]
    const after = segments[i]
    if (!before || !after || before === after) continue
    if (!isLiveThinking(before) || !isLiveThinking(after) || before.id !== after.id) continue
    const prevContent = before.content ?? ''
    const nextContent = after.content ?? ''
    if (nextContent === prevContent) continue
    if (nextContent.startsWith(prevContent) && (next === prevContent || next.endsWith(prevContent))) {
      next += nextContent.slice(prevContent.length)
      continue
    }
    return liveThinkingText(segments)
  }
  return next
}

function withUpdatedThinkText(prev: LiveProcessView, thinkText: string): LiveProcessView {
  return thinkText === prev.thinkText ? prev : { ...prev, thinkText }
}

function liveAnswerTailIsPlain(segments: readonly TurnSegment[]): boolean {
  const tail = segments[segments.length - 1]
  return Boolean(tail && isLiveAnswerText(tail) && !hasStreamingDemoFence(tail.content ?? ''))
}

/**
 * 过程区指纹：增长中的回答正文 / 思考只记 id，不拼全文。
 * 工具 / 演示围栏变了才变，避免每枚 token 重跑 buildAnswerParts。
 */
export function liveProcessIdentity(segments: readonly TurnSegment[]): string {
  let out = ''
  for (const segment of segments) {
    if (isLiveThinking(segment)) {
      out += `th:${segment.id}:${segment.status};`
      continue
    }
    if (isLiveAnswerText(segment) && !hasStreamingDemoFence(segment.content ?? '')) {
      out += `a:${segment.id};`
      continue
    }
    out += `${segment.kind}:${segment.id}:${segment.status}:${segment.role ?? ''}:${segment.toolName ?? ''}:${segment.toolTitle ?? ''}:${segment.toolDetail ?? ''}:${segment.content ?? ''};`
  }
  return out
}

/**
 * 已在回答且不是演示生成中：指纹没变就复用过程视图。
 * 对标 Codex #22860：工具时间线不跟正文 token。
 */
export function shouldReuseLiveProcessView(input: {
  prev: LiveProcessView | null
  identity: string
  prevIdentity: string
}): boolean {
  if (!input.prev || !input.identity || input.identity !== input.prevIdentity) return false
  return input.prev.contentStreaming && input.prev.answerStreaming && !input.prev.generatingDemo
}

/**
 * 前缀引用没变、末段仍是同一段增长正文或思考：不必拼过程指纹。
 * 对标 Codex #22860：回答 / 思考 token 不扫整条工具时间线。
 */
export function shouldSkipLiveProcessIdentity(input: {
  prev: LiveProcessView | null
  prevSegments: readonly TurnSegment[] | null
  segments: readonly TurnSegment[]
  prevAnswerTailPlain?: boolean
}): boolean {
  if (!input.prev || !input.prevSegments) return false
  if (input.prev.generatingDemo) return false
  if (input.prevSegments.length !== input.segments.length) return false
  const last = input.segments.length - 1
  for (let i = 0; i < last; i++) {
    if (input.prevSegments[i] !== input.segments[i]) return false
  }
  const prevTail = input.prevSegments[last]
  const nextTail = input.segments[last]
  if (!prevTail || !nextTail) return false
  if (prevTail === nextTail) return true
  if (
    isLiveThinking(prevTail) &&
    isLiveThinking(nextTail) &&
    prevTail.id === nextTail.id &&
    prevTail.status === nextTail.status
  ) {
    return true
  }
  if (!input.prev.contentStreaming || !input.prev.answerStreaming) return false
  if (input.prevAnswerTailPlain === false) return false
  const nextHasFence =
    input.prevAnswerTailPlain === true
      ? hasStreamingDemoFenceGrowth(prevTail.content ?? '', nextTail.content ?? '')
      : hasStreamingDemoFence(nextTail.content ?? '')
  return (
    isLiveAnswerText(prevTail) &&
    isLiveAnswerText(nextTail) &&
    prevTail.id === nextTail.id &&
    !nextHasFence
  )
}

/** 同一工具只改详情：换时间线该步引用，不重跑 buildAnswerParts / extractFinalContent */
export function shouldRetargetLiveProcessOnToolMeta(input: {
  prev: LiveProcessView | null
  prevSegments: readonly TurnSegment[] | null
  segments: readonly TurnSegment[]
}): boolean {
  if (!input.prev || !input.prevSegments) return false
  if (input.prev.generatingDemo) return false
  return (
    findLiveToolRetargetChange(input.prevSegments, input.segments) !== null ||
    isLiveMultiToolSettleChange(input.prevSegments, input.segments)
  )
}

function remapProcessFlowRefs(
  prevFlow: TurnSegment[],
  prevSegments: readonly TurnSegment[],
  segments: readonly TurnSegment[]
): TurnSegment[] {
  const remapped = prevFlow.map((segment) => {
    const index = prevSegments.indexOf(segment)
    if (index < 0) return segment
    return segments[index] ?? segment
  })
  if (remapped.length !== prevFlow.length) return remapped
  for (let i = 0; i < remapped.length; i++) {
    if (remapped[i] !== prevFlow[i]) return remapped
  }
  return prevFlow
}

function retargetProcessFlow(
  prevFlow: TurnSegment[],
  prevTail: TurnSegment,
  nextTail: TurnSegment
): TurnSegment[] {
  let found = false
  const next = prevFlow.map((segment) => {
    if (segment !== prevTail) return segment
    found = true
    return nextTail
  })
  return found ? next : prevFlow
}

function splitClosedTail(parts: AnswerPart[]): { closed: AnswerPart[]; tail: AnswerPart | null } {
  if (!parts.length) return { closed: [], tail: null }
  if (parts.length === 1) return { closed: [], tail: parts[0]! }
  return { closed: parts.slice(0, -1), tail: parts[parts.length - 1]! }
}

function processForAnswer(segments: TurnSegment[], answerParts: AnswerPart[]): TurnSegment[] {
  const answerTextIds = new Set(
    answerParts.filter((part) => part.type === 'text').map((part) => part.id)
  )
  return processSegments(segments, { isStreaming: true }).filter((segment) => {
    if (segment.toolName === 'present_inline_demo') return false
    if (segment.kind === 'text' && answerTextIds.has(segment.id)) return false
    return true
  })
}

function liveDemoProcessFlags(
  prev: LiveProcessView,
  demo: TurnSegment
): Pick<LiveProcessView, 'generatingDemo' | 'contentStreaming' | 'answerStreaming'> {
  const paintable = isInlineDemoPaintable(demo.content ?? '')
  return {
    generatingDemo: demo.status === 'active' && !paintable,
    contentStreaming: prev.contentStreaming || paintable,
    answerStreaming: prev.answerStreaming
  }
}

function liveDemoFenceProcessFlags(
  prev: LiveProcessView,
  text: TurnSegment
): Pick<LiveProcessView, 'generatingDemo' | 'contentStreaming' | 'answerStreaming'> {
  const built = buildAnswerParts([text], { isStreaming: true })
  const demo = built.find(
    (part): part is Extract<AnswerPart, { type: 'demo' }> => part.type === 'demo'
  )
  const paintable = Boolean(demo && isInlineDemoPaintable(demo.html))
  const hasProse = built.some((part) => part.type === 'text' && part.content.trim())
  return {
    generatingDemo: Boolean(demo?.streaming && !paintable),
    contentStreaming: prev.contentStreaming || paintable || hasProse,
    answerStreaming: true
  }
}

function sameProcessView(prev: LiveProcessView, next: LiveProcessView): boolean {
  return (
    prev.processForFlow === next.processForFlow &&
    prev.thinkText === next.thinkText &&
    prev.contentStreaming === next.contentStreaming &&
    prev.generatingDemo === next.generatingDemo &&
    prev.answerStreaming === next.answerStreaming
  )
}

function sameProcessTimeline(prev: LiveProcessTimeline, next: LiveProcessTimeline): boolean {
  return (
    prev.processForFlow === next.processForFlow &&
    prev.contentStreaming === next.contentStreaming &&
    prev.generatingDemo === next.generatingDemo &&
    prev.answerStreaming === next.answerStreaming &&
    prev.hasThought === next.hasThought
  )
}

function timelineFromProcessView(view: LiveProcessView): LiveProcessTimeline {
  return {
    processForFlow: view.processForFlow,
    contentStreaming: view.contentStreaming,
    generatingDemo: view.generatingDemo,
    answerStreaming: view.answerStreaming,
    hasThought: Boolean(view.thinkText.trim())
  }
}

/** 过程切片：正文增长且工具引用没变时退回 prev；工具详情只换末步，不重跑 buildAnswerParts */
export function nextLiveProcessView(
  prev: LiveProcessView | null,
  snap: LiveStreamUiSnapshot
): LiveProcessView {
  const segments = snap.liveSegments
  if (prev && processHold?.view === prev && processHold.segments === segments) return prev
  if (
    prev &&
    processHold?.view === prev &&
    shouldSkipLiveProcessIdentity({
      prev,
      prevSegments: processHold.segments,
      segments,
      prevAnswerTailPlain: processHold.answerTailPlain
    })
  ) {
    const view = withUpdatedThinkText(
      prev,
      nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    )
    processHold = {
      view,
      identity: processHold.identity,
      segments,
      answerTailPlain: liveAnswerTailIsPlain(segments)
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveAskNeededThinkAppendChange(processHold.segments, segments) ||
      isLiveAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveAskNeededAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededThinkAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveAskNeededCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededCompressAppendChange(processHold.segments, segments) ||
      isLiveAskNeededCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedSettledToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedToolAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededThinkCancelAppendChange(processHold.segments, segments))
  ) {
    const added = segments.slice(processHold.segments.length).filter((segment) => {
      if (segment.kind === 'thinking' || segment.kind === 'text') return false
      return segment.toolName !== 'present_inline_demo'
    })
    const remapped = remapProcessFlowRefs(prev.processForFlow, processHold.segments, segments)
    const thinkText = nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    const inlineDemo =
      isLiveAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededThinkAnswerDemoAppendChange(processHold.segments, segments)
    const answer =
      isLiveAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskNeededAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveAskNeededThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAskNeededThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAskNeededThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusAskNeededAnswerSettledToolAppendChange(processHold.segments, segments)
    const hasProse =
      answer &&
      segments.some(
        (segment, index) =>
          index >= processHold.segments.length &&
          segment.kind === 'text' &&
          Boolean((segment.content ?? '').trim())
      )
    const flags = inlineDemo
      ? liveDemoProcessFlags(prev, segments[segments.length - 1]!)
      : answer
        ? {
            contentStreaming: prev.contentStreaming || Boolean(hasProse),
            answerStreaming: prev.answerStreaming || Boolean(hasProse)
          }
        : {}
    const view = {
      ...prev,
      processForFlow: [...remapped, ...added],
      thinkText,
      ...flags
    }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: inlineDemo ? processHold.answerTailPlain : Boolean(hasProse) || processHold.answerTailPlain
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveToolAppendChange(processHold.segments, segments) ||
      isLiveSettledToolAppendChange(processHold.segments, segments) ||
      isLiveThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatSettledToolAppendChange(processHold.segments, segments) ||
      isLiveThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAnswerSettledToolAppendChange(processHold.segments, segments) ||
      isLiveToolWriteStatAppendChange(processHold.segments, segments) ||
      isLiveStatusToolAppendChange(processHold.segments, segments) ||
      isLiveThinkToolAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatToolAppendChange(processHold.segments, segments) ||
      isLiveThinkStatusAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkStatusAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkStatusAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkStatusAppendChange(processHold.segments, segments) ||
      isLiveWriteStatCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusCompressAppendChange(processHold.segments, segments) ||
      isLiveThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusCancelAppendChange(processHold.segments, segments) ||
      isLiveThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveCompressAppendChange(processHold.segments, segments))
  ) {
    const added = segments.slice(processHold.segments.length).filter((segment) => {
      if (segment.kind === 'thinking' || segment.kind === 'text') return false
      return segment.toolName !== 'present_inline_demo'
    })
    const remapped = remapProcessFlowRefs(prev.processForFlow, processHold.segments, segments)
    const thinkText = nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    const view = { ...prev, processForFlow: [...remapped, ...added], thinkText }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: processHold.answerTailPlain
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveWriteStatStatusThinkAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkAppendChange(processHold.segments, segments) ||
      isLiveStatusAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveStatusDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusErrorAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkErrorAppendChange(processHold.segments, segments))
  ) {
    const status = segments[processHold.segments.length]!
    const remapped = remapProcessFlowRefs(prev.processForFlow, processHold.segments, segments)
    const thinkText = nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    const inlineDemo =
      isLiveStatusDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkDemoAppendChange(processHold.segments, segments)
    const demoFence =
      isLiveWriteStatStatusDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveStatusDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkDemoFenceAppendChange(processHold.segments, segments)
    const answer =
      isLiveWriteStatStatusAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusThinkAnswerAppendChange(processHold.segments, segments)
        ? segments[segments.length - 1]
        : null
    const hasProse = Boolean(answer && (answer.content ?? '').trim())
    const flags = inlineDemo
      ? liveDemoProcessFlags(prev, segments[segments.length - 1]!)
      : demoFence
        ? liveDemoFenceProcessFlags(prev, segments[segments.length - 1]!)
        : {
            contentStreaming: prev.contentStreaming || hasProse,
            answerStreaming: prev.answerStreaming || hasProse
          }
    const view = {
      ...prev,
      processForFlow: [...remapped, status],
      thinkText,
      ...flags
    }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: inlineDemo || demoFence ? false : hasProse || processHold.answerTailPlain
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveStatusAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedStatusAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedStatusAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatStatusAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAppendChange(processHold.segments, segments))
  ) {
    const added = segments[segments.length - 1]!
    const remapped = remapProcessFlowRefs(prev.processForFlow, processHold.segments, segments)
    const view = { ...prev, processForFlow: [...remapped, added] }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: processHold.answerTailPlain
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveThinkAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedThinkAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAppendChange(processHold.segments, segments))
  ) {
    const processForFlow = remapProcessFlowRefs(
      prev.processForFlow,
      processHold.segments,
      segments
    )
    const thinkText = nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    const view =
      processForFlow === prev.processForFlow && thinkText === prev.thinkText
        ? prev
        : { ...prev, processForFlow, thinkText }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: processHold.answerTailPlain
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAppendChange(processHold.segments, segments) ||
      isLiveThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkErrorAppendChange(processHold.segments, segments))
  ) {
    const added = segments[segments.length - 1]!
    const hasProse = Boolean((added.content ?? '').trim())
    const processForFlow = remapProcessFlowRefs(
      prev.processForFlow,
      processHold.segments,
      segments
    )
    const thinkText = nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    const view = {
      ...prev,
      processForFlow,
      thinkText,
      contentStreaming: prev.contentStreaming || hasProse,
      answerStreaming: prev.answerStreaming || hasProse
    }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: true
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveDemoAppendChange(processHold.segments, segments) ||
      isLiveAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveThinkDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkDemoAppendChange(processHold.segments, segments))
  ) {
    const added = segments[segments.length - 1]!
    const processForFlow = remapProcessFlowRefs(
      prev.processForFlow,
      processHold.segments,
      segments
    )
    const thinkText = nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    const flags = liveDemoProcessFlags(prev, added)
    const view = {
      ...prev,
      processForFlow,
      thinkText,
      ...flags
    }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: processHold.answerTailPlain
    }
    return view
  }
  if (prev && processHold?.view === prev) {
    const demoChange = findLiveDemoHtmlChange(processHold.segments, segments)
    if (demoChange) {
      const processForFlow = remapProcessFlowRefs(
        prev.processForFlow,
        processHold.segments,
        segments
      )
      const flags = liveDemoProcessFlags(prev, demoChange.to)
      const view =
        processForFlow === prev.processForFlow &&
        flags.generatingDemo === prev.generatingDemo &&
        flags.contentStreaming === prev.contentStreaming &&
        flags.answerStreaming === prev.answerStreaming
          ? prev
          : { ...prev, processForFlow, ...flags }
      processHold = {
        view,
        identity: liveProcessIdentity(segments),
        segments,
        answerTailPlain: processHold.answerTailPlain
      }
      return view
    }
  }
  if (prev && processHold?.view === prev) {
    const fenceText =
      findLiveDemoFenceChange(processHold.segments, segments)?.to ??
      (isLiveDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveWriteStatDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveThinkDemoFenceAppendChange(processHold.segments, segments) ||
      isLiveWriteStatThinkDemoFenceAppendChange(processHold.segments, segments)
        ? segments[segments.length - 1]
        : null)
    if (fenceText) {
      const processForFlow = remapProcessFlowRefs(
        prev.processForFlow,
        processHold.segments,
        segments
      )
      const flags = liveDemoFenceProcessFlags(prev, fenceText)
      const view = {
        ...prev,
        processForFlow,
        thinkText: nextLiveThinkText(prev.thinkText, processHold.segments, segments),
        ...flags
      }
      processHold = {
        view,
        identity: liveProcessIdentity(segments),
        segments,
        answerTailPlain: false
      }
      return view
    }
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveApprovalNeededThinkAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoTwoToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskThreeToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveAskResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedSettledToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkSettledToolAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkCancelAppendChange(processHold.segments, segments))
  ) {
    const added = segments.slice(processHold.segments.length).filter((segment) => {
      if (segment.kind === 'thinking' || segment.kind === 'text') return false
      return segment.toolName !== 'present_inline_demo'
    })
    const remapped = remapProcessFlowRefs(prev.processForFlow, processHold.segments, segments)
    const thinkText = nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    const inlineDemo =
      isLiveApprovalNeededAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments)
    const answer =
      isLiveApprovalNeededAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalNeededErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalDeniedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedErrorAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCompressAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolAppendChange(processHold.segments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveWriteStatApprovalResolvedErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedErrorCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerCompressAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCancelAppendChange(processHold.segments, segments) ||
      isLiveStatusApprovalResolvedAnswerCancelAppendChange(processHold.segments, segments)
    const hasProse =
      answer &&
      segments.some(
        (segment, index) =>
          index >= processHold.segments.length &&
          segment.kind === 'text' &&
          Boolean((segment.content ?? '').trim())
      )
    const flags = inlineDemo
      ? liveDemoProcessFlags(prev, segments[segments.length - 1]!)
      : answer
        ? {
            contentStreaming: prev.contentStreaming || Boolean(hasProse),
            answerStreaming: prev.answerStreaming || Boolean(hasProse)
          }
        : {}
    const view = {
      ...prev,
      processForFlow: [...remapped, ...added],
      thinkText,
      ...flags
    }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: inlineDemo ? processHold.answerTailPlain : Boolean(hasProse) || processHold.answerTailPlain
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    (isLiveApprovalNeededChange(processHold.segments, segments) ||
      isLiveApprovalDeniedSettleChange(processHold.segments, segments) ||
      isLiveApprovalDeniedSettleCancelChange(processHold.segments, segments) ||
      isLiveApprovalAllowedWriteStatChange(processHold.segments, segments) ||
      isLiveApprovalAllowedSettleChange(processHold.segments, segments) ||
      isLiveApprovalResolvedChange(processHold.segments, segments) ||
      isLiveApprovalResolvedCancelChange(processHold.segments, segments) ||
      isLiveApprovalAllowedCancelChange(processHold.segments, segments) ||
      isLiveUserInputNeededChange(processHold.segments, segments) ||
      isLiveAskResolvedSettleChange(processHold.segments, segments) ||
      isLiveStatusSettleChange(processHold.segments, segments) ||
      isLiveCancelChange(processHold.segments, segments) ||
      isLiveErrorAppendChange(processHold.segments, segments) ||
      isLiveWriteStatErrorAppendChange(processHold.segments, segments))
  ) {
    const remapped = remapProcessFlowRefs(prev.processForFlow, processHold.segments, segments)
    const grew =
      segments.length === processHold.segments.length + 1 &&
      !isLiveErrorAppendChange(processHold.segments, segments) &&
      !isLiveWriteStatErrorAppendChange(processHold.segments, segments)
    const processForFlow = grew ? [...remapped, segments[segments.length - 1]!] : remapped
    const view =
      processForFlow === prev.processForFlow ? prev : { ...prev, processForFlow }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: processHold.answerTailPlain
    }
    return view
  }
  if (
    prev &&
    processHold?.view === prev &&
    shouldRetargetLiveProcessOnToolMeta({
      prev,
      prevSegments: processHold.segments,
      segments
    })
  ) {
    if (isLiveMultiToolSettleChange(processHold.segments, segments)) {
      const processForFlow = remapProcessFlowRefs(
        prev.processForFlow,
        processHold.segments,
        segments
      )
      const view =
        processForFlow === prev.processForFlow ? prev : { ...prev, processForFlow }
      processHold = {
        view,
        identity: liveProcessIdentity(segments),
        segments,
        answerTailPlain: processHold.answerTailPlain
      }
      return view
    }
    const change = findLiveToolRetargetChange(processHold.segments, segments)
    if (!change) return prev
    if (isLiveLastLineOnlyToolChange(change.from, change.to)) {
      return prev
    }
    const processForFlow = retargetProcessFlow(prev.processForFlow, change.from, change.to)
    const view =
      processForFlow === prev.processForFlow ? prev : { ...prev, processForFlow }
    processHold = {
      view,
      identity: liveProcessIdentity(segments),
      segments,
      answerTailPlain: processHold.answerTailPlain
    }
    return view
  }
  const identity = liveProcessIdentity(segments)
  const prevIdentity = processHold?.view === prev ? processHold.identity : ''
  if (prev && shouldReuseLiveProcessView({ prev, identity, prevIdentity })) {
    const view = withUpdatedThinkText(
      prev,
      nextLiveThinkText(prev.thinkText, processHold.segments, segments)
    )
    processHold = {
      view,
      identity,
      segments,
      answerTailPlain: liveAnswerTailIsPlain(segments)
    }
    return view
  }
  const answerParts = buildAnswerParts(segments, { isStreaming: true })
  const flow = processForAnswer(segments, answerParts)
  const processForFlow =
    prev && sameRefList(prev.processForFlow, flow) ? prev.processForFlow : flow
  const hasLiveProse = answerParts.some((part) => part.type === 'text' && part.content.trim())
  const hasLiveDemo = answerParts.some((part) => part.type === 'demo')
  const hasPaintableDemo = answerParts.some(
    (part) => part.type === 'demo' && isInlineDemoPaintable(part.html)
  )
  const finalRaw = extractFinalContent(segments, { isStreaming: true })
  const next: LiveProcessView = {
    processForFlow,
    thinkText: liveThinkingText(segments),
    contentStreaming: hasLiveProse || hasPaintableDemo,
    generatingDemo: hasLiveDemo && !hasPaintableDemo,
    answerStreaming: Boolean(finalRaw.trim() || hasLiveProse)
  }
  const view = prev && sameProcessView(prev, next) ? prev : next
  processHold = {
    view,
    identity,
    segments,
    answerTailPlain: liveAnswerTailIsPlain(segments)
  }
  return view
}

/** 同一帧快照只派生一次过程视图 */
export function liveProcessViewFromSnap(snap: LiveStreamUiSnapshot): LiveProcessView {
  return nextLiveProcessView(processHold?.view ?? null, snap)
}

/**
 * 时间线切片：思考原文加长时退回 prev，不抬 TurnFlow。
 * 对标 Codex #22860：默认折叠的 Thinking 不跟 token 重挂步骤。
 */
export function nextLiveProcessTimeline(
  prev: LiveProcessTimeline | null,
  snap: LiveStreamUiSnapshot
): LiveProcessTimeline {
  const next = timelineFromProcessView(liveProcessViewFromSnap(snap))
  return prev && sameProcessTimeline(prev, next) ? prev : next
}

/** 末段是增长中的回答正文时可以只换 tail，不必切出前缀数组 */
export function liveAnswerGrowState(
  segments: readonly TurnSegment[],
  prevTail?: { content: string; plain: boolean }
): { tail: TurnSegment | null } {
  const tail = segments[segments.length - 1]
  if (!tail || tail.kind !== 'text' || !tail.content?.trim()) {
    return { tail: null }
  }
  const hasFence =
    prevTail?.plain === true
      ? hasStreamingDemoFenceGrowth(prevTail.content, tail.content)
      : hasStreamingDemoFence(tail.content)
  if (hasFence) return { tail: null }
  return { tail }
}

/**
 * 前缀引用没变且尾仍是同一段正文：只换 tail，不重跑 buildAnswerParts。
 * 就地比 all-but-last，不 `slice`（对标 Codex #22860）。
 */
/**
 * 末段仍是同一段增长中的思考 / 状态，或同一工具只改详情：回答槽没变，不必 `buildAnswerParts`。
 * 工具预览 / diff / 参数变了才重拆。对标 Codex #22860。
 */
export function shouldSkipLiveAnswerIdentity(input: {
  prev: LiveAnswerView | null
  prevSegments: readonly TurnSegment[] | null
  segments: readonly TurnSegment[]
}): boolean {
  if (!input.prev || !input.prevSegments) return false
  if (
    (isLiveSettledToolAppendChange(input.prevSegments, input.segments) ||
      isLiveThinkSettledToolAppendChange(input.prevSegments, input.segments) ||
      isLiveStatusThinkSettledToolAppendChange(input.prevSegments, input.segments) ||
      isLiveWriteStatSettledToolAppendChange(input.prevSegments, input.segments)) &&
    addedSettledToolsHaveWriteStat(input.prevSegments.length, input.segments)
  ) {
    return false
  }
  if (isLiveWriteStatSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatThinkSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusThinkSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (
    isLiveToolAppendChange(input.prevSegments, input.segments) ||
    isLiveSettledToolAppendChange(input.prevSegments, input.segments) ||
    isLiveThinkSettledToolAppendChange(input.prevSegments, input.segments) ||
    isLiveStatusThinkSettledToolAppendChange(input.prevSegments, input.segments) ||
    isLiveStatusToolAppendChange(input.prevSegments, input.segments) ||
    isLiveThinkToolAppendChange(input.prevSegments, input.segments) ||
    isLiveStatusThinkToolAppendChange(input.prevSegments, input.segments) ||
    isLiveThinkStatusAppendChange(input.prevSegments, input.segments) ||
    isLiveStatusThinkStatusAppendChange(input.prevSegments, input.segments)
  ) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveThinkAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveStatusThinkAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveCompressAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveStatusCompressAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveThinkCompressAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveStatusThinkCompressAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveCancelChange(input.prevSegments, input.segments)) {
    return !input.segments.some((segment, index) => {
      const before = input.prevSegments![index]
      return Boolean(before && before !== segment && segment.kind === 'text')
    })
  }
  if (
    isLiveStatusCancelAppendChange(input.prevSegments, input.segments) ||
    isLiveThinkCancelAppendChange(input.prevSegments, input.segments) ||
    isLiveStatusThinkCancelAppendChange(input.prevSegments, input.segments)
  ) {
    return !input.segments.some((segment, index) => {
      const before = input.prevSegments![index]
      return Boolean(before && before !== segment && segment.kind === 'text')
    })
  }
  if (isLiveApprovalNeededChange(input.prevSegments, input.segments)) return true
  if (isLiveApprovalNeededThinkAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalNeededThinkCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalNeededCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalNeededAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalNeededErrorAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalNeededAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatApprovalNeededCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusApprovalNeededCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalNeededAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoTwoToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskThreeToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusApprovalResolvedCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedCancelChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalDeniedSettleChange(input.prevSegments, input.segments)) return true
  if (isLiveApprovalDeniedCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalDeniedThinkAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveApprovalDeniedThinkAnswerCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkErrorAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalDeniedThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalDeniedThinkAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalDeniedAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalDeniedAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalDeniedThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalDeniedAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalDeniedAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalDeniedStatusAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalDeniedToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedSettleChange(input.prevSegments, input.segments)) return true
  if (isLiveApprovalAllowedThinkAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveApprovalAllowedWriteStatThinkAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedWriteStatAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedWriteStatThinkAnswerAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedWriteStatThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedWriteStatAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedWriteStatThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedWriteStatAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedWriteStatThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAppendChange(input.prevSegments, input.segments)) return true
  if (isLiveApprovalResolvedThinkCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedThinkAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedThinkErrorAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedThinkAnswerCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerCancelAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedErrorCancelAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedThinkAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkErrorCancelAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedThinkToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedAskAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalDeniedSettleCancelChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedSettleThinkAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleThinkAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedSettleToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedSettleThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedSettleAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalResolvedAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedErrorAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalResolvedAnswerCompressAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedCancelChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusApprovalResolvedAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatApprovalResolvedErrorAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatApprovalResolvedAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatApprovalResolvedThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusApprovalResolvedAskAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusApprovalResolvedThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusApprovalResolvedThinkAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusApprovalResolvedThinkAnswerCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedThinkAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusApprovalResolvedThinkErrorAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusApprovalResolvedThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusApprovalResolvedAnswerCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusApprovalResolvedErrorAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusApprovalResolvedAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusApprovalResolvedThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedThinkAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveApprovalAllowedAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveApprovalAllowedStatusAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalAllowedToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveApprovalResolvedChange(input.prevSegments, input.segments)) return true
  if (isLiveUserInputNeededChange(input.prevSegments, input.segments)) return true
  if (isLiveAskResolvedSettleChange(input.prevSegments, input.segments)) return true
  if (isLiveAskResolvedCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskResolvedThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskResolvedAnswerCancelCompressAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskResolvedAnswerCancelAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskResolvedThinkAnswerCancelAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskResolvedThinkAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerAskActiveToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerAskToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedAnswerDemoErrorAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedErrorAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkErrorAnswerDemoCancelCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskResolvedThinkToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskResolvedAnswerDemoCompressAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskResolvedSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskResolvedToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskNeededThinkAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskNeededThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskNeededAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskNeededThinkAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskNeededAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskNeededAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveAskNeededThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatAskNeededThinkAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatAskNeededAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatAskNeededThinkAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatAskNeededThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatAskNeededAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatAskNeededAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatAskNeededThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusAskNeededThinkAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusAskNeededAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusAskNeededThinkAnswerAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusAskNeededAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusAskNeededThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskNeededThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusAskNeededThinkAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusAskNeededThinkSettledToolAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusAskNeededAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusAskNeededThinkAnswerAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusAskNeededAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusAskNeededThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveStatusAskNeededAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatAskNeededThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusAskNeededThinkAnswerDemoAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusAskNeededAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusAskNeededThinkAnswerSettledToolAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskNeededCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusAskNeededCompressAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveWriteStatAskNeededCompressAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusAskNeededCompressAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveAskNeededCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveAskNeededThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusAskNeededCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveWriteStatAskNeededCancelAppendChange(input.prevSegments, input.segments)) return false
  if (isLiveWriteStatStatusAskNeededCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatAskNeededThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveWriteStatStatusAskNeededThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return false
  }
  if (isLiveStatusAskNeededThinkCancelAppendChange(input.prevSegments, input.segments)) {
    return findLiveClosedAnswerText(input.prevSegments, input.segments) === null
  }
  if (isLiveStatusSettleChange(input.prevSegments, input.segments)) return true
  if (findLiveToolInPlaceChange(input.prevSegments, input.segments)) return true
  if (isLiveMultiToolSettleChange(input.prevSegments, input.segments)) return true
  if (input.prevSegments.length !== input.segments.length) return false
  const last = input.segments.length - 1
  for (let i = 0; i < last; i++) {
    if (input.prevSegments[i] !== input.segments[i]) return false
  }
  const prevTail = input.prevSegments[last]
  const nextTail = input.segments[last]
  if (!prevTail || !nextTail) return false
  if (prevTail === nextTail) return true
  if (prevTail.id !== nextTail.id || prevTail.kind !== nextTail.kind) return false
  if (nextTail.kind === 'thinking' || nextTail.kind === 'status') {
    return prevTail.status === nextTail.status
  }
  if (nextTail.kind === 'tool') {
    return isLiveToolMetaOnlyChange(prevTail, nextTail) || isLiveToolSettleChange(prevTail, nextTail)
  }
  return false
}

export function shouldGrowLiveAnswerTail(input: {
  prev: LiveAnswerView | null
  prevSegments: readonly TurnSegment[] | null
  segments: readonly TurnSegment[]
  tail: TurnSegment | null
}): boolean {
  if (!input.prev?.tail || input.prev.tail.type !== 'text' || !input.tail) return false
  if (input.tail.kind !== 'text' || input.tail.id !== input.prev.tail.id) return false
  if (hasStreamingDemoFenceGrowth(input.prev.tail.content, input.tail.content ?? '')) return false
  if (!input.prevSegments || input.prevSegments.length !== input.segments.length) return false
  const last = input.segments.length - 1
  for (let i = 0; i < last; i++) {
    if (input.prevSegments[i] !== input.segments[i]) return false
  }
  return true
}

function copyableFromAnswerParts(parts: readonly AnswerPart[]): string {
  return parts
    .filter((part): part is Extract<AnswerPart, { type: 'text' }> => part.type === 'text')
    .map((part) => part.content)
    .join('\n\n')
    .trim()
}

function growLiveAnswerView(prev: LiveAnswerView, tail: TurnSegment): LiveAnswerView {
  const content = tail.content ?? ''
  if (prev.tail?.type === 'text' && prev.tail.content === content) return prev
  const tailPart: AnswerPart = { type: 'text', id: tail.id, content }
  const parts = prev.closed.length ? [...prev.closed, tailPart] : [tailPart]
  const copyable = prev.closed.length ? copyableFromAnswerParts(parts) : content.trim()
  return {
    parts,
    closed: prev.closed,
    tail: tailPart,
    show: true,
    copyable,
    hasCopyable: Boolean(copyable)
  }
}

function applyLiveDemoFenceView(prev: LiveAnswerView, text: TurnSegment): LiveAnswerView {
  const built = buildAnswerParts([text], { isStreaming: true })
  const owned = new Set([text.id, `${text.id}-demo-stream`, `${text.id}-post`])
  const prefix = [
    ...prev.closed.filter((part) => !owned.has(part.id)),
    ...(prev.tail && !owned.has(prev.tail.id) ? [prev.tail] : [])
  ]
  const parts = reuseAnswerParts(prev.parts, [...prefix, ...built])
  const split = splitClosedTail(parts)
  const copyable = copyableFromAnswerParts(parts)
  return {
    parts,
    closed: sameRefList(prev.closed, split.closed) ? prev.closed : split.closed,
    tail: split.tail,
    show: parts.length > 0,
    copyable,
    hasCopyable: Boolean(copyable)
  }
}

function liveDemoAnswerPart(demo: TurnSegment): Extract<AnswerPart, { type: 'demo' }> {
  const html = demo.content ?? ''
  return {
    type: 'demo',
    id: demo.id,
    html: html || '<!-- streaming -->',
    caption: demo.toolDetail,
    streaming: demo.status === 'active'
  }
}

/** 新开 present_inline_demo：先收起上一尾，再开演示槽，不重跑 buildAnswerParts */
function appendLiveDemoView(prev: LiveAnswerView, demo: TurnSegment): LiveAnswerView {
  const demoPart = liveDemoAnswerPart(demo)
  const closed = prev.tail ? [...prev.closed, prev.tail] : prev.closed
  const parts = [...closed, demoPart]
  const copyable = copyableFromAnswerParts(parts)
  return {
    parts,
    closed,
    tail: demoPart,
    show: true,
    copyable,
    hasCopyable: Boolean(copyable)
  }
}

/** 同一演示 HTML / 说明 / 收束：只换该槽 */
function growLiveDemoView(prev: LiveAnswerView, demo: TurnSegment): LiveAnswerView {
  const demoPart = liveDemoAnswerPart(demo)
  if (prev.tail?.type === 'demo' && prev.tail.id === demo.id) {
    if (
      prev.tail.html === demoPart.html &&
      prev.tail.caption === demoPart.caption &&
      prev.tail.streaming === demoPart.streaming
    ) {
      return prev
    }
    const parts = prev.closed.length ? [...prev.closed, demoPart] : [demoPart]
    return {
      ...prev,
      parts,
      tail: demoPart,
      show: true
    }
  }
  const index = prev.parts.findIndex((part) => part.type === 'demo' && part.id === demo.id)
  if (index < 0) {
    return {
      parts: reuseAnswerParts(prev.parts, [...prev.parts, demoPart]),
      closed: prev.closed,
      tail: demoPart,
      show: true,
      copyable: prev.copyable,
      hasCopyable: prev.hasCopyable
    }
  }
  const current = prev.parts[index]!
  if (
    current.type === 'demo' &&
    current.html === demoPart.html &&
    current.caption === demoPart.caption &&
    current.streaming === demoPart.streaming
  ) {
    return prev
  }
  const parts = prev.parts.slice()
  parts[index] = demoPart
  const tail = prev.tail?.id === demo.id ? demoPart : prev.tail
  const closed =
    prev.tail?.id === demo.id
      ? prev.closed
      : prev.closed.map((part) => (part.id === demo.id && part.type === 'demo' ? demoPart : part))
  return {
    ...prev,
    parts,
    closed,
    tail,
    show: true
  }
}

function liveWriteStatDiffParts(tool: TurnSegment): Extract<AnswerPart, { type: 'diff' }>[] {
  return buildAnswerParts([tool], { isStreaming: true }).filter(
    (part): part is Extract<AnswerPart, { type: 'diff' }> => part.type === 'diff'
  )
}

function findLiveWriteStatTool(
  prev: readonly TurnSegment[] | null | undefined,
  next: readonly TurnSegment[]
): TurnSegment | null {
  if (!prev) return null
  const n = Math.min(prev.length, next.length)
  let found: TurnSegment | null = null
  for (let i = 0; i < n; i++) {
    const before = prev[i]
    const after = next[i]
    if (!before || !after || before === after) continue
    if (isLiveToolWriteStatChange(before, after)) {
      if (found) return null
      found = after
      continue
    }
    if (isLiveAwaitingStatusResolve(before, after)) continue
    if (next.length === prev.length) return null
    if (!isLivePrefixClose(before, after)) return null
  }
  return found
}

/** 写盘 +/- 只换该工具的 diff 槽，已画正文 / 尾不重拆（对标 ~0.5s / #22860，不复制 #38695） */
function retargetLiveAnswerDiffs(prev: LiveAnswerView, tool: TurnSegment): LiveAnswerView {
  const diffs = liveWriteStatDiffParts(tool)
  const prefix = `${tool.id}-diff-`
  let start = -1
  let end = -1
  for (let i = 0; i < prev.parts.length; i++) {
    const part = prev.parts[i]
    if (part.type === 'diff' && part.id.startsWith(prefix)) {
      if (start < 0) start = i
      end = i + 1
      continue
    }
    if (start >= 0) break
  }
  if (start < 0) {
    if (!diffs.length) return prev
    const parts =
      prev.tail && prev.parts.length && prev.parts[prev.parts.length - 1] === prev.tail
        ? [...prev.parts.slice(0, -1), ...diffs, prev.tail]
        : [...prev.parts, ...diffs]
    return { ...prev, parts, show: true }
  }
  const reused = diffs.map((diff, index) => {
    const old = prev.parts[start + index]
    if (old && old.type === 'diff' && old.id === diff.id && old.diff === diff.diff) return old
    return diff
  })
  if (reused.length === end - start && reused.every((part, index) => part === prev.parts[start + index])) {
    return prev
  }
  const parts = [...prev.parts.slice(0, start), ...reused, ...prev.parts.slice(end)]
  return { ...prev, parts, show: parts.length > 0 }
}

/** 工具后新开一段散文：先收起上一尾，再开新尾，不重跑 buildAnswerParts */
function appendLiveAnswerView(prev: LiveAnswerView, tail: TurnSegment): LiveAnswerView {
  if (prev.tail && prev.tail.id !== tail.id) {
    return growLiveAnswerView({ ...prev, closed: [...prev.closed, prev.tail], tail: null }, tail)
  }
  return growLiveAnswerView(prev, tail)
}

/** tool_start 收束散文：把增长尾封进 closed，不重跑 buildAnswerParts */
function sealLiveAnswerTail(prev: LiveAnswerView, closedSeg: TurnSegment): LiveAnswerView {
  if (!prev.tail || prev.tail.id !== closedSeg.id) return prev
  const content = closedSeg.content ?? prev.tail.content
  const sealed =
    prev.tail.type === 'text' && prev.tail.content === content
      ? prev.tail
      : { type: 'text' as const, id: closedSeg.id, content }
  const closed = [...prev.closed, sealed]
  const copyable = copyableFromAnswerParts(closed)
  return {
    parts: closed,
    closed,
    tail: null,
    show: closed.length > 0,
    copyable,
    hasCopyable: Boolean(copyable)
  }
}

/** 回答切片：正文只加长时续尾；否则闭合块走 reuseAnswerParts */
export function nextLiveAnswerView(
  prev: LiveAnswerView | null,
  snap: LiveStreamUiSnapshot
): LiveAnswerView {
  const segments = snap.liveSegments
  const prevTextTail = prev?.tail?.type === 'text' ? prev.tail : null
  const grow = liveAnswerGrowState(
    segments,
    prevTextTail && answerGrowHold?.view === prev
      ? { content: prevTextTail.content, plain: answerGrowHold.tailPlain }
      : undefined
  )
  const prevSegments = answerGrowHold?.view === prev ? answerGrowHold.segments : null
  if (prev && shouldSkipLiveAnswerIdentity({ prev, prevSegments, segments })) {
    answerGrowHold = { view: prev, segments, tailPlain: Boolean(grow.tail) }
    return prev
  }
  if (prev) {
    const writeStat = findLiveWriteStatTool(prevSegments, segments)
    if (writeStat) {
      const patched = retargetLiveAnswerDiffs(prev, writeStat)
      if (
        isLiveWriteStatAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatThinkAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkAnswerAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedWriteStatAnswerAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedWriteStatThinkAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededThinkAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededAnswerAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededThinkAnswerAppendChange(prevSegments, segments)
      ) {
        const view = appendLiveAnswerView(patched, segments[segments.length - 1]!)
        answerGrowHold = { view, segments, tailPlain: true }
        return view
      }
      if (
        isLiveWriteStatApprovalResolvedAnswerCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments)
      ) {
        const text = segments.slice(prevSegments!.length).find((segment) => segment.kind === 'text')
        if (text) {
          const appended = appendLiveAnswerView(patched, text)
          const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
          answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
          return view
        }
      }
      if (isLiveWriteStatAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (isLiveWriteStatThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length + 1]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (isLiveWriteStatStatusAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length + 1]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (isLiveWriteStatStatusThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length + 2]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (isLiveWriteStatAskNeededAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length + 2]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (isLiveWriteStatAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length + 3]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (isLiveWriteStatStatusAskNeededAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length + 3]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (isLiveWriteStatStatusAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
        const text = segments[prevSegments!.length + 4]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (
        isLiveApprovalAllowedWriteStatAnswerSettledToolAppendChange(prevSegments, segments)
      ) {
        const text = segments[prevSegments!.length]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (
        isLiveApprovalAllowedWriteStatThinkAnswerSettledToolAppendChange(prevSegments, segments)
      ) {
        const text = segments[prevSegments!.length + 1]!
        const appended = appendLiveAnswerView(patched, text)
        const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
        answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
        return view
      }
      if (
        isLiveWriteStatThinkSettledToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkSettledToolAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedWriteStatThinkSettledToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededThinkSettledToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededThinkSettledToolAppendChange(prevSegments, segments)
      ) {
        answerGrowHold = {
          view: patched,
          segments,
          tailPlain: Boolean(prev.tail?.type === 'text')
        }
        return patched
      }
      if (
        isLiveWriteStatDemoFenceAppendChange(prevSegments, segments) ||
        isLiveWriteStatThinkDemoFenceAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusDemoFenceAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkDemoFenceAppendChange(prevSegments, segments)
      ) {
        const view = applyLiveDemoFenceView(patched, segments[segments.length - 1]!)
        answerGrowHold = { view, segments, tailPlain: false }
        return view
      }
      if (
        isLiveWriteStatDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatThinkDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatThinkAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedWriteStatAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedWriteStatThinkAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoCancelCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAnswerDemoAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoCancelAppendChange(prevSegments, segments)
      ) {
        const sealed = findLiveClosedAnswerText(prevSegments, segments)
        const base = sealed ? sealLiveAnswerTail(patched, sealed) : patched
        const view = appendLiveDemoView(base, segments[segments.length - 1]!)
        answerGrowHold = { view, segments, tailPlain: false }
        return view
      }
      if (
        isLiveWriteStatStatusCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatThinkCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatThinkStatusAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkStatusAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatAskNeededThinkCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusAskNeededThinkCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalNeededCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalNeededCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkCompressAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoToolAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleThinkAnswerAskAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleAnswerAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleThinkAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedThinkAnswerAskAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveAskResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveAskResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveAskResolvedErrorAskAppendChange(prevSegments, segments) ||
        isLiveAskResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
        isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveAskResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveAskResolvedAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveAskResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveAskResolvedAskToolAppendChange(prevSegments, segments) ||
        isLiveAskResolvedAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveAskResolvedAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveAskResolvedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveAskResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
        isLiveAskResolvedErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveAskResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalAllowedSettleErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveApprovalDeniedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments)
      ) {
        const sealed = findLiveClosedAnswerText(prevSegments, segments)
        const view = sealed ? sealLiveAnswerTail(patched, sealed) : patched
        answerGrowHold = { view, segments, tailPlain: false }
        return view
      }
      if (
        isLiveWriteStatErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatThinkErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatStatusThinkErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedErrorAppendChange(prevSegments, segments) ||
        isLiveWriteStatApprovalResolvedThinkErrorAppendChange(prevSegments, segments)
      ) {
        const view = appendLiveAnswerView(patched, segments[segments.length - 1]!)
        answerGrowHold = { view, segments, tailPlain: false }
        return view
      }
      answerGrowHold = {
        view: patched,
        segments,
        tailPlain: Boolean(prev.tail?.type === 'text')
      }
      return patched
    }
  }
  if (
    prev &&
    (isLiveToolAppendChange(prevSegments, segments) ||
      isLiveSettledToolAppendChange(prevSegments, segments) ||
      isLiveThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveStatusThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveStatusToolAppendChange(prevSegments, segments) ||
      isLiveThinkToolAppendChange(prevSegments, segments) ||
      isLiveStatusThinkToolAppendChange(prevSegments, segments) ||
      isLiveStatusCancelAppendChange(prevSegments, segments) ||
      isLiveThinkCancelAppendChange(prevSegments, segments) ||
      isLiveStatusThinkCancelAppendChange(prevSegments, segments) ||
      isLiveStatusAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveAskNeededCompressAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededCompressAppendChange(prevSegments, segments) ||
      isLiveAskNeededCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedSettledToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedToolAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkCancelAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededCancelAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedCancelChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedSettleCancelChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleToolAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedCancelChange(prevSegments, segments) ||
      isLiveApprovalDeniedCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkSettledToolAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkCancelAppendChange(prevSegments, segments))
  ) {
    const sealed = findLiveClosedAnswerText(prevSegments, segments)
    if (sealed) {
      const view = sealLiveAnswerTail(prev, sealed)
      answerGrowHold = { view, segments, tailPlain: false }
      return view
    }
  }
  if (prev && isLiveAnswerSettledToolAppendChange(prevSegments, segments)) {
    const text = segments[prevSegments!.length]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (prev && isLiveThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
    const text = segments[prevSegments!.length + 1]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (prev && isLiveStatusThinkAnswerSettledToolAppendChange(prevSegments, segments)) {
    const text = segments[prevSegments!.length + 2]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (
    prev &&
    (isLiveApprovalAllowedAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedAnswerSettledToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerSettledToolAppendChange(prevSegments, segments))
  ) {
    const text = segments[prevSegments!.length]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (
    prev &&
    (isLiveApprovalAllowedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedThinkAnswerSettledToolAppendChange(prevSegments, segments))
  ) {
    const text = segments[prevSegments!.length + 1]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (
    prev &&
    (isLiveAskNeededAnswerAppendChange(prevSegments, segments) ||
      isLiveAskNeededAnswerSettledToolAppendChange(prevSegments, segments))
  ) {
    const text = segments[prevSegments!.length + 2]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (
    prev &&
    (isLiveAskNeededThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkAnswerSettledToolAppendChange(prevSegments, segments))
  ) {
    const text = segments[prevSegments!.length + 3]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (
    prev &&
    (isLiveStatusAskNeededAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededAnswerSettledToolAppendChange(prevSegments, segments))
  ) {
    const text = segments[prevSegments!.length + 3]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (prev && isLiveStatusAskNeededThinkAnswerAppendChange(prevSegments, segments)) {
    const text = segments[prevSegments!.length + 4]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (
    prev &&
    (isLiveAnswerAppendChange(prevSegments, segments) ||
      isLiveThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusThinkAnswerAppendChange(prevSegments, segments))
  ) {
    const added = segments[segments.length - 1]!
    const view = appendLiveAnswerView(prev, added)
    answerGrowHold = { view, segments, tailPlain: true }
    return view
  }
  if (
    prev &&
    (isLiveDemoFenceAppendChange(prevSegments, segments) ||
      isLiveThinkDemoFenceAppendChange(prevSegments, segments) ||
      isLiveStatusDemoFenceAppendChange(prevSegments, segments) ||
      isLiveStatusThinkDemoFenceAppendChange(prevSegments, segments))
  ) {
    const added = segments[segments.length - 1]!
    const view = applyLiveDemoFenceView(prev, added)
    answerGrowHold = { view, segments, tailPlain: false }
    return view
  }
  if (prev) {
    const fenceChange = findLiveDemoFenceChange(prevSegments, segments)
    if (fenceChange) {
      const view = applyLiveDemoFenceView(prev, fenceChange.to)
      answerGrowHold = { view, segments, tailPlain: false }
      return view
    }
  }
  if (prev && isLiveApprovalNeededAnswerAppendChange(prevSegments, segments)) {
    const text = segments[prevSegments!.length + 1]!
    const appended = appendLiveAnswerView(prev, text)
    const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
    answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
    return view
  }
  if (
    prev &&
    (isLiveApprovalResolvedAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerCancelCompressAppendChange(prevSegments, segments))
  ) {
    const sealed = findLiveClosedAnswerText(prevSegments, segments)
    const base = sealed ? sealLiveAnswerTail(prev, sealed) : prev
    const text = segments.slice(prevSegments!.length).find((segment) => segment.kind === 'text')
    if (text) {
      const appended = appendLiveAnswerView(base, text)
      const view = text.status === 'done' ? sealLiveAnswerTail(appended, text) : appended
      answerGrowHold = { view, segments, tailPlain: text.status === 'active' }
      return view
    }
  }
  if (
    prev &&
    (isLiveApprovalResolvedErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoTwoToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskThreeToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskSettledActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorTwoActiveToolCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerDemoAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCancelAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskActiveToolAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskToolAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorCancelCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveWriteStatStatusApprovalNeededThinkAnswerDemoAskCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAskToolCompressAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAskToolCompressAppendChange(prevSegments, segments))
  ) {
    const text = segments.slice(prevSegments!.length).find((segment) => segment.kind === 'text')
    if (text) {
      const view = appendLiveAnswerView(prev, text)
      answerGrowHold = { view, segments, tailPlain: false }
      return view
    }
  }
  if (prev && isLiveApprovalNeededErrorAppendChange(prevSegments, segments)) {
    const view = appendLiveAnswerView(prev, segments[prevSegments!.length + 1]!)
    answerGrowHold = { view, segments, tailPlain: false }
    return view
  }
  if (prev && isLiveApprovalNeededAnswerDemoAppendChange(prevSegments, segments)) {
    const sealed = findLiveClosedAnswerText(prevSegments, segments)
    const base = sealed ? sealLiveAnswerTail(prev, sealed) : prev
    const view = appendLiveDemoView(base, segments[segments.length - 1]!)
    answerGrowHold = { view, segments, tailPlain: false }
    return view
  }
  if (
    prev &&
    (isLiveErrorAppendChange(prevSegments, segments) ||
      isLiveStatusErrorAppendChange(prevSegments, segments) ||
      isLiveThinkErrorAppendChange(prevSegments, segments) ||
      isLiveStatusThinkErrorAppendChange(prevSegments, segments))
  ) {
    const view = appendLiveAnswerView(prev, segments[segments.length - 1]!)
    answerGrowHold = { view, segments, tailPlain: false }
    return view
  }
  if (
    prev &&
    (isLiveDemoAppendChange(prevSegments, segments) ||
      isLiveAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveStatusApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalAllowedSettleErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalDeniedErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedErrorAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveAskResolvedThinkAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkAnswerDemoCancelAppendChange(prevSegments, segments) ||
      isLiveApprovalResolvedThinkErrorAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveAskResolvedAnswerDemoCompressAppendChange(prevSegments, segments) ||
      isLiveAskNeededAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveStatusAskNeededThinkAnswerDemoAppendChange(prevSegments, segments) ||
      isLiveStatusDemoAppendChange(prevSegments, segments) ||
      isLiveThinkDemoAppendChange(prevSegments, segments) ||
      isLiveStatusThinkDemoAppendChange(prevSegments, segments))
  ) {
    const added = segments[segments.length - 1]!
    const sealed = findLiveClosedAnswerText(prevSegments, segments)
    const base = sealed ? sealLiveAnswerTail(prev, sealed) : prev
    const view = appendLiveDemoView(base, added)
    answerGrowHold = { view, segments, tailPlain: false }
    return view
  }
  if (prev) {
    const demoChange = findLiveDemoHtmlChange(prevSegments, segments)
    if (demoChange) {
      const view = growLiveDemoView(prev, demoChange.to)
      answerGrowHold = { view, segments, tailPlain: false }
      return view
    }
  }
  if (prev && shouldGrowLiveAnswerTail({ prev, prevSegments, segments, tail: grow.tail })) {
    const view = growLiveAnswerView(prev, grow.tail!)
    answerGrowHold = { view, segments, tailPlain: true }
    return view
  }
  const parts = reuseAnswerParts(
    prev?.parts ?? [],
    buildAnswerParts(segments, { isStreaming: true })
  )
  const split = splitClosedTail(parts)
  const closed =
    prev && sameRefList(prev.closed, split.closed) ? prev.closed : split.closed
  const copyable = copyableFromAnswerParts(parts)
  const next: LiveAnswerView = {
    parts,
    closed,
    tail: split.tail,
    show: parts.length > 0,
    copyable,
    hasCopyable: Boolean(copyable)
  }
  const view =
    prev &&
    prev.parts === next.parts &&
    prev.closed === next.closed &&
    prev.tail === next.tail &&
    prev.show === next.show &&
    prev.copyable === next.copyable &&
    prev.hasCopyable === next.hasCopyable
      ? prev
      : next
  answerGrowHold = {
    view,
    segments,
    tailPlain: Boolean(grow.tail)
  }
  return view
}

/** 同一帧快照只派生一次回答视图；片段引用没变则不重拆（过程/闭合/尾/操作条共用） */
export function liveAnswerViewFromSnap(snap: LiveStreamUiSnapshot): LiveAnswerView {
  if (answerCache && answerCache.snap === snap) return answerCache.view
  if (answerCache && answerCache.snap.liveSegments === snap.liveSegments) {
    answerCache = { snap, view: answerCache.view }
    return answerCache.view
  }
  const view = nextLiveAnswerView(answerCache?.view ?? null, snap)
  answerCache = { snap, view }
  return view
}

export function nextLiveAnswerActions(
  prev: LiveAnswerActions | null,
  snap: LiveStreamUiSnapshot
): LiveAnswerActions {
  const view = liveAnswerViewFromSnap(snap)
  const next: LiveAnswerActions = {
    show: view.show,
    reserved: view.show && !view.hasCopyable
  }
  if (prev && prev.show === next.show && prev.reserved === next.reserved) return prev
  return next
}

/** 直播行是否该挂（布尔，token 不翻转） */
export function liveHasAssistantBody(
  snap: LiveStreamUiSnapshot,
  approvalWaiting: boolean
): boolean {
  return hasLiveAssistantBody({
    streaming: snap.streaming,
    liveSegmentCount: snap.liveSegments.length,
    thinking: snap.turnThinking,
    approvalWaiting
  })
}
