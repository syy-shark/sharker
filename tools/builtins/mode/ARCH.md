# tools/builtins/mode — 模式切换工具

## 职责

- 计划模式（只读规划 + Build）与 Git worktree 工作目录覆盖

## 同级目录

（无子目录）

## 同级文件

| 文件 | 说明 |
|------|------|
| `plan.ts` | enter / exit 计划模式（`harness-state`） |
| `worktree.ts` | enter / exit worktree，覆盖工具 cwd |
| `ARCH.md` | 本层架构说明 |
